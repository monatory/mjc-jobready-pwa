# MJC-READY 2026-09-06 세션 마무리 (시범 운영 전일 종합)

> 이날 일곱 차례 작업(최종 점검 → 10관점 병렬 점검 → 문구·재테스트 → 권장 수정 10건 → 자동 로그아웃 → 배포 전 리뷰 7건 → 휴대전화 인쇄 대응)의
> 종합. 세부는 각 보고서와 `docs/WORKLOG.md` 09-06 항목 1~7차 참조. 저장소 `main` = `origin/main`, 작업 트리 clean, 개발 서버 종료.
> **라이브 배포본 = 커밋 `3958836`(번들 `index-pUOiGUJb.js`)**, 규칙은 09-05 게시본 그대로(변경 없음).

---

## 1. 오늘 한 일 (시간순)

| 차수 | 내용 | 커밋 | 보고서 |
|---|---|---|---|
| 1차 | 학생 배포 최종 점검 — 게이트·라이브 번들·규칙 403·학생 흐름 완주·상담 신청 재제출 실측 | `4ac634c` `6bc0bb0` | `점검보고서_2026-09-06_학생배포_최종점검.md` |
| 2차 | **10관점 병렬 점검**(읽기 전용) 94건 → 3개 검증자 36건 반박 검증 → 중간 이상 확정 14건. 규칙 `size()` 글자 수 실측(09-05 "바이트" 판단 정정) | `bc323f0` | `점검보고서_2026-09-06_10관점_병렬점검.md` |
| 3차 | 설문 5번 국민취업지원제도 예시 "(예: 1유형은 구직촉진수당 월 60만원 × 6개월 지원)" — 고용노동부 2026 개편 확인 · 사용자 UI 삭제 후 라이브 재테스트 2건(L4·L1) | `81e8a6f` `52275f1` `50350d0` | WORKLOG 3차 |
| 4차 | **권장 수정 10건 전건 반영**(상담사 자동 동기화·카드 변경 필드만 저장·회차 보존·제출 정규화·상담 신청 문구·L3 문구·삭제 재확인·재개 모달 마스킹·iOS 16px·로그인 오류 분기) + CLAUDE.md §7.2.1 원칙 15~17 | `811b57b` `52c3fc4` | WORKLOG 4차 |
| 5차 | **자동 로그아웃** — 로그인 후 6시간, 모든 역할(`useAutoLogout`, 로그인 화면 안내) | `6e9f13e` | WORKLOG 5차 |
| 6차 | 배포 전 리뷰(오늘 변경분) 중간 3·낮음 4 **전건 수정** + 라이브 경로 테스트(진학·창업 Route, 재개 복귀 재제출, 형식 오류 안내, 다시 진단) | `a230176` `0691ee5` | WORKLOG 6차 |
| 7차 | **휴대전화 인쇄 대응** — 인쇄 창 미개방 감지 → 대안 안내 카드 + "결과 요약 공유·복사" | `3958836` | WORKLOG 7차 |

## 2. 시범 첫날 기준 시스템 상태

- **학생 흐름**: 라이브에서 5개 시나리오 완주(L4 취업·L1 미정·L3 혼자준비·L2 진학·창업·L4 혼합 응답). 제출·상담 신청 재제출·휴대전화 정규화·재개 모달·형식 오류 안내·다시 진단 전부 정상. 콘솔 에러 0.
- **상담사 워크스페이스**: 탭 복귀 시 자동 동기화(30초 스로틀), 카드 저장은 바뀐 필드만(저장 후 미편집 필드는 병합본으로 재동기화), 회차 실패 시 입력 보존, 등록부 새로고침 버튼, 6시간 자동 로그아웃.
- **관리자**: 일괄 삭제가 서버 값 재확인 후 실시일이 같은 문서만 삭제(재제출된 학번의 상담 기록 보존), 로그인 오류 코드별 안내, 6시간 자동 로그아웃.
- **규칙**: 09-05 게시본. `string.size()`는 글자 수(성명 ≤30자·학과 ≤60자), 휴대전화 `010-0000-0000` 형식 또는 빈 값. 비인증 5컬렉션 403.
- **문서**: CLAUDE.md §6.4(자동 로그아웃)·§7.2.1(원칙 15~17) 반영. WORKLOG 1~7차. 보고서 4종(상위 폴더 사본 포함).

## 3. 시범 시작 전 사용자 처리 (마스터 로그인)

| # | 항목 | 절차 |
|---|---|---|
| ① | **테스트 응답 5건 삭제** — `99990911`(테스트A) `99990912`(B) `99990913`(C) `99990914`(D) `99990915`(E) | `#/admin` → 명단 → 실시일 종료일 2026-09-06 → "필터 결과 N건 삭제" → "삭제" 타이핑. 실행 직전 ↻ 새로고침 |
| ② | 상담사 안내 | 각자 PC 사용 · 로그인 후 6시간 자동 로그아웃 · 다른 상담사 기록은 탭 복귀나 ↻ 새로고침 때 반영 · 저장 실패 경고가 뜨면 그 자리에서 다시 저장 |
| ③ | 배포 규칙 | 시범 첫 주 근무·홍보 시간대 배포 금지(배포 직후 최대 10분 캐시 창) |
| ④ | 단체 응시 | 실습실·캠퍼스 Wi-Fi로 1시간 100명 이상 응시 예정이면 Firebase 콘솔 Authentication → 설정 → 가입 할당량 임시 상향 |
| ⑤ | 휴대전화 인쇄 실확인 | 카카오톡 링크·크롬·사파리에서 "결과지 인쇄·PDF 저장"을 눌러 안내 카드가 뜨는 경우/안 뜨는 경우 보고 → 감지 시간(1.5초) 조정 |

## 4. 센터 판단으로 남긴 것 (시범과 병행)

- L3 결과 문구 원문(§12-07, 현재는 응답별 치환 표시) · `EMP_CONSULT_JOBCAFE` weak_domains 확장 · Route 표기 표준("취업 Route" vs "취업준비 Route")
- 연구용 익명 CSV의 자유 입력(희망직무·자격증명·학과) 처리 — **연구 제공 전 필수**
- `ready_staff` 목록 읽기 범위·LEAD 동급 권한(규칙 게시 동반) · 시작 화면 "관리자" 링크·`/links.html` 공개 유지 · App Check·학생 인증(§12-11)
- 이월: 상담 신청 별도 필드 vs 설문값 변경 · 다학기 집계(ADM-01) · 스냅샷 vs 재계산 · CNS-01 · 문서 불일치 6건(§8 PWA/Recharts, §11 53/15건, deploy.yml 스텝명, "콘솔 게시" 문구, sessionState.ts:2, §14)

## 5. 검증 기록

```
게이트      매 커밋 validate 76 · 회귀 19 · strict build 통과 (코드 커밋 5건: 811b57b 6e9f13e a230176 3958836 + 52275f1 데이터)
로컬 실동   firebase.ts 임시 로컬 모드 → git checkout 원복 (4회). 상담 카드 diff 저장·재동기화, 재개 모달 마스킹(3자·2자),
            L3 문구 분기, 6시간 만료/유지, 모바일 인쇄 대안 카드·요약 복사, 360px 입력 16px — 콘솔 에러 0
라이브      배포 4회 모두 번들 해시 확인 후 학생 흐름 완주. 최종 배포본 index-pUOiGUJb.js
실측        규칙 size() 글자 수(200/403/200) · Auth 가입 한도 100/IP/시간(문서) · index.html max-age=600
생성 데이터 응답 5건(99990911~99990915) + 실측용 익명 Auth 계정 (정리 대상 §3-①)
```
