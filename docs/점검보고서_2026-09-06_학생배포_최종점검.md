# MJC-READY 학생 배포 최종 점검 (2026-09-06, 시범 운영 전일)

> 09-05 세션 마무리 보고서의 "다음 세션" 항목을 기준으로, 9/7 시범 운영 전 **코드 변경 없이** 라이브 상태를 확인한 결과.
> 개발자 권한으로 확인 가능한 항목은 전부 실측했고, 마스터 로그인이 필요한 항목은 §3에 절차로 정리함.
> 저장소: `main` = `origin/main`(`f782743`), 작업 트리 clean. 이 세션의 코드 변경 0건.

---

## 1. 요약

| 구분 | 결과 |
|---|---|
| 품질 게이트 | 데이터 검증 76건 통과 · 회귀 19건 통과 · strict build 통과 |
| 라이브 번들 | GitHub Pages 배포본 = 최신 코드 커밋 `272576d` 빌드와 동일 (`index-Zd1VMtTR.js` · `index-DZegzmwi.css`) |
| 서버 규칙 | 5개 컬렉션(`ready_responses`·`ready_outreach`·`ready_staff`·`ready_agencies`·`ready_reco_master`) 비인증 REST 조회 전부 **403** |
| 학생 흐름(라이브) | 360px 뷰포트에서 STEP 1 → 2 → 3 → 결과지 완주, **"응답이 안전하게 제출되었습니다"** 확인, 콘솔 에러 0 |
| 휴대전화 규칙(M13) | 하이픈 없이 입력(`01000000000`) → 클라이언트 정규화 `010-0000-0000` → 규칙 통과 · 제출 성공 |
| 상담 신청 이중장치 | 결과지 버튼 클릭 → `counsel_request:{at}` 포함 재제출 성공 → "✓ 상담 신청됨" 전환 |
| 남은 일 | 마스터 로그인 필요 항목 5건(§3) — 전부 화면 조작으로 10분 내 처리 가능 |

## 2. 실측 내역

### 2.1 게이트·배포 정합
```
npm run validate:data   통과 76 / 실패 0
npm test                19건 전부 통과
npm run build           tsc -b + vite build 성공 (1,046 kB · gzip 281 kB)
GITHUB_PAGES=1 재빌드   assets 해시가 라이브 index.html과 일치 → 라이브 = 최신 main
```
- 코드(src·data·index.html·public·firestore.rules)를 마지막으로 바꾼 커밋은 `272576d`(09-05 16:49). 이후 커밋 2건은 문서만.

### 2.2 규칙 게시 상태 (비인증 REST)
`https://firestore.googleapis.com/v1/projects/mjc-ready-pwa/databases/(default)/documents/<컬렉션>?pageSize=1`
→ 5개 컬렉션 모두 403. 로컬 `firestore.rules`의 N5(삭제 = `isCounselSide()`)·M13(학과·학년·휴대전화 형식) 조항이 실제 제출을 통과시켰으므로(§2.3) 게시본과 로컬본이 일치함.

### 2.3 학생 흐름 (라이브 `https://monatory.github.io/mjc-jobready-pwa/`, 360×780)
| 단계 | 확인 |
|---|---|
| STEP 1 | 고지표(수집 항목·목적·보유 기간 "졸업 후 3년까지"·관리 부서 "학생지원처 취·창업팀")·동의 체크·"시범운영 중 — 학교 시스템에 제출" 문구 표시 |
| STEP 2 | 학번 `99990906` · 성명 `테스트0906` · 학과 `테스트학과` · 본과정 1학년 · 휴대전화 `01000000000`(하이픈 없음) + 설문 6문항(상담 희망 포함) + 추가정보 → 칸별 오류 0, 진단으로 이동 |
| STEP 3 | 27문항 전부 "그렇다"(4) → "응답 27 / 27" · "진단 완료 · 결과 보기" 버튼 |
| 결과지 | 취업준비 Route · LEVEL 4 · JAS 100 · 추천활동 3건 · 상담 CTA · 푸터 "응답이 안전하게 제출되었습니다" |
| 상담 신청 | 버튼 클릭 → 재제출 → sessionStorage 업로드 스냅샷에 `counsel_request.at = 2026-09-06T06:25:10Z` 포함 → "✓ 상담 신청됨 — 컨설턴트가 연락드려요" |
| 동의 기록 | `consent.terms_version = 2026-09-03`, 동의 시각 저장 |

- 콘솔 에러 0. 학기 자동 산출은 9월이므로 문서키 `2026-2_99990906`.
- ⚠ **이 테스트 응답은 §3-①에서 함께 삭제해야 함** (실시일 9/6, 학번 99990906).

## 3. 시범 시작 전 마스터 처리 항목 (사용자, 라이브 로그인 필요)

| # | 항목 | 절차 |
|---|---|---|
| ① | **테스트 응답 정리** | `#/admin` 마스터 로그인 → 대상자 필터·명단 → 실시일 기간 종료일 **2026-09-06** → 하단 "필터 결과 N건 삭제" → "삭제" 타이핑. 오늘 만든 `99990906`(테스트0906) 포함. 상담 기록도 함께 삭제됨 |
| ② | 미승인(PENDING)·테스트 계정 정리 | 계정 관리 → 승인 대기 행 "삭제"(tombstone). 잘못 지우면 "삭제된 계정" 구역의 복구 버튼으로 되돌릴 수 있음 |
| ③ | 점검용 기관 정리 | `#/counsel` → 연계기관·취업처 등록부 → 점검용 기관 삭제("(삭제된 기관)" 이력 유지) |
| ④ | 상담사 계정 ACTIVE 확인 | 잡카페 상담사 전원이 승인(ACTIVE) 상태인지 — 첫날 연락 우선 큐를 볼 수 있어야 함 |
| ⑤ | 추천활동 Master ON 확인 | 추천활동 관리 → 운영하지 않는 활동은 OFF (결과지·CSV에 "(종료)" 표기됨) |

학생 배포 자료: `docs/MJC-READY_링크안내_v0.1.docx`(학생용 주소·QR) — 학생에게는 **학생용 링크·QR만** 전달, 링크북(`/links.html`·Word)은 내부 전용.

## 4. 센터 판단으로 남겨 둔 것 (시범 운영과 병행)
- 결과지 상담 신청을 별도 필드(현행)로 둘지, 설문값 변경(JAS +5·Level 변동)으로 할지
- 다학기 중복 집계(ADM-01) · 저장 스냅샷 vs 재계산(배너로 버전 불일치 건수만 표시) · 공유 저장 실패분 재병합(CNS-01)
- 학생 인증(§12-11) · 보유기간 만료 수단 · PWA 설치 여부 · 연구용 준식별자 · §12 확정 체크리스트

## 5. 검증 기록
```
데이터 검증 76 / 회귀 19 / strict build 통과 (코드 무변경, f782743)
라이브 번들 해시 일치 (Pages 모드 재빌드 대조)
비인증 REST 5컬렉션 403
라이브 학생 흐름 완주 + 제출 성공 + 상담 신청 재제출 성공, 콘솔 에러 0 (360px)
```
