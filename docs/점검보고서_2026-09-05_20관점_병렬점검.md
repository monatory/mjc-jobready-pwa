# MJC-READY 역할별 20관점 병렬 점검 보고서 (2026-09-05 오후)

> 2026-09-05 수정분(사용자 요청 5건 + 보고만 항목 13건, 커밋 `bdbf632`·`5c1a664`)을 포함한 전체 시스템을
> 학생 7·관리자 7·상담사 6 = 20개 관점으로 병렬 점검(읽기 전용)하고, 주요 주장은 코드 대조로 반박 검증한 결과.
>
> **처리 현황 (같은 날 2차 세션)**: 사용자 지시 "1순위부터 최종까지"에 따라 §2 치명·높음 17건 · §3 중간 13건 · §4 낮음 16건
> **전건 수정** 및 규칙 게시(N5·M13). §5의 기존 구조 항목 중 판정 엔진 범위 방어만 유효 입력 무변경 조건으로 반영.
> 상세는 `docs/WORKLOG.md` 2026-09-05 (2차).

---

## 1. 요약

| 구분 | 내용 |
|---|---|
| 점검 방식 | 20관점 병렬 코드 정독(점검자는 파일 수정·서버 기동 금지) → 발견 62건 → 중복 통합·반박 검증 |
| 결과 | **확정 46건** (치명 2 · 높음 15 · 중간 13 · 낮음 16) / 기각 4건 / 기존 알려진 구조 항목 8건은 재확인만 |
| 오늘 수정분에서 나온 것 | 상담 신청 이중장치 3건(덮어쓰기 소실·제출 경합·큐 제외), 응답 삭제 4건(규칙 범위·문서키·부분 실패·모달 경합), 진단 라벨 1건, 기관 tombstone 2건, 중복 의심 칩 1건, 길이 검증 1건 |
| 파일럿(9/7) 전 권장 | §2 치명·높음 17건 중 **규칙 게시 1건 포함 12건** (§5 1순위) |
| 게이트 | 점검 전 validate 76 · 회귀 19 · strict build 통과 상태에서 시작, 코드 무변경 |

---

## 2. 확정 — 치명·높음 (17건)

| ID | 역할 | 문제 | 위치 | 최소 수정안 |
|---|---|---|---|---|
| **N1** 치명 | 학생→상담사 | 재응시(새 세션·"처음부터")가 `setDoc` 전체 덮어쓰기라 **이전 상담 신청(counsel_request)·교정 이력·동의가 통째로 사라짐**. 설문 미희망 학생이면 큐·KPI·CSV에서 조용히 빠짐 | saveResponse.ts:57-70, Result.tsx payload | `setDoc(ref, data, { mergeFields: Object.keys(data) })` — 이번 세션이 쓰는 최상위 필드만 교체(맵은 통째 교체되므로 조건부 문항 잔존 없음), 안 쓰는 필드는 보존 |
| **N2** 치명 | 상담사 | 상담 기록이 유일하게 **물리 삭제**(tombstone 아님) + `pullShared`가 원격에 없는 로컬 항목을 영구 보존 → 삭제된 학생의 상담 기록이 다른 상담사 브라우저에 남고, 그 캐시로 저장하면 `fallbackBase`로 **부활** | responsesSource.ts:114-126, cloudStore.ts:30-43·118-134 | pullShared는 "원격이 진실"로 로컬을 통째 교체(실패분 재병합은 어차피 일어나지 않음 §3-K1) + `pushOutreachMerged`는 원격 문서 없으면 빈 base로 시작 |
| N3 | 학생 | 최초 제출(SAVING) 중 상담 신청 클릭 → 두 `saveResponseToCloud`가 동시 진행, 익명 로그인 순서에 따라 **신청 없는 쪽이 나중에 써져 유실** 가능 | Result.tsx:159-210, saveResponse.ts | 모듈 단위 직렬화(진행 중 Promise 체인에 이어 붙이기) |
| N4 | 상담사 | 진학·창업 Route 학생은 결과지에서 **상담 신청을 눌러도 연락 우선 큐·"연락 대기"에 절대 안 뜸**(Route 제외 조건이 wantsCounsel까지 지움) | StudentsPanel.tsx needsOutreachWith | `wantsCounsel(s) \|\| (level>=3 && route!==FSS)` |
| N5 | 관리자 | 응답 삭제 규칙이 `isActiveStaff()` — UI는 마스터 전용이지만 **담당자·상담사도 SDK 직접 호출로 삭제 가능** | firestore.rules:53-55 | `allow delete: if isCounselSide()`(학번 교정 이동은 상담사 계열만 하므로 충분) → 규칙 게시 |
| N6 | 관리자 | 삭제·수정이 문서키를 `semester` 필드로 **재구성** — 결측 문서는 엉뚱한 키를 지우고 `batch.delete`는 무동작이라 **"삭제했습니다" 오보고**, 다학기 보호도 무력화 | responsesSource.ts:83·114-117, StudentsPanel outreachIdsFor | `fetchStudents`에서 `d.id`를 `StudentRecord.doc_id`로 보관하고 삭제·수정·키 비교에 그것을 사용 |
| N7 | 학생 | 진로방향 **미정(UNDECIDED)** 학생 결과지에 "취업준비 Route" 오표기(이진 분기) | Result.tsx:245 | `route_tags` 3분기 — 미정은 "진로탐색 Route" |
| N8 | 학생 | 기본정보(휴대전화 포함)만 입력하고 이탈하면 재개 판정이 NONE → 모달 없이 **다음 사용자(공용 PC)에게 이전 학생 개인정보 프리필** | sessionState.getResumeState, Survey.tsx:35 | profile에 값이 있으면 "SURVEY"로 판정 |
| N9 | 학생 | 380px 이하 `word-break: keep-all` + "보통이다"(공백 없는 4음절)는 줄바꿈 불가 → **320px·시스템 글꼴 확대 시 칸 밖으로 넘침**(S23 증상 재발 가능) | global.css @media 380 | `overflow-wrap: anywhere` 추가(마지막 수단 분절) |
| N10 | 상담사 | 기관 **구분 변경(연계기관↔취업처)을 "삭제"로 오판** → 재선택 강제, 연계 저장 거부 | CounselRecord.tsx:113-118 | stale 판정은 전체 목록 기준, 타입 불일치면 옵션에 포함 |
| N11 | 상담사 | 기관 삭제 직후 archived 이름 캐시는 `pullShared`에서만 채워져 **삭제한 세션에서는 "(삭제된 기관)"만** 표시 | AgencyManager.tsx, agencies.removeAgency | removeAgency가 로컬 archived 맵에 이름 기록 |
| N12 | 상담사 | 회차 배열 원소 형태 미검증 + `pushOutreachMerged`가 원격을 무검증 캐스팅 → `seq` 결측 1건이면 이후 **모든 회차가 NaN회차** | cloudStore.ts:70-82·106 | isValidOutreach가 원소 검사, seq 계산은 `Number.isFinite` 필터 |
| N13 | 관리자 | 추천활동 길이 검증이 문자 수(100/500)인데 규칙 `size()`는 **UTF-8 바이트** → 한글 34자/167자부터 "저장 실패(네트워크 확인)" 오안내 | RecoMasterPanel.tsx:143·153 | `new TextEncoder().encode(s).length`로 검사, 안내 문구도 바이트 기준 |
| N14 | 관리자 | 통합 CSV(기본 다운로드) 추천활동 열에 **종료·삭제 활동 표시 없음**(05시트만 OFF) → 운영 안 하는 프로그램 안내 위험 | csvExport.ts:285 | 비활성은 "(종료)" 접미 |
| N15 | 관리자 | 04_진단점수 `gate_passed`·`critical_unmet`이 문항 무관 **학생 단위 상수**(27행 동일값) | csvExport.ts:140-141 | 문항 `critical`/`gate` 속성 + `answer_at_least` 기준으로 문항별 산출, 해당 없음은 빈칸 |
| N16 | 관리자 | DELETED tombstone 계정: 재가입 안내("로그인하면 복구")가 실제와 다르고(영구 거부), **마스터 UI에 복구 수단 없음** (09-03 A3 해결분에 복구 버튼 누락) | auth.ts:203·336·444, Accounts.tsx | 목록에 "삭제됨" 접힘 구역 + 복구(→PENDING) 버튼, 안내 문구 수정 |
| N17 | 관리자 | 첫 `loadStaffCloud` 실패로 `cloudMode=false`면 **직접 등록이 로컬 계정으로 새고** 이메일 아이디는 이후 영구 로그인 불가 | Accounts.tsx:61-68·122, auth.login | CLOUD_ENABLED인데 cloudMode=false면 직접 등록 잠금 + 재시도 버튼 |

## 3. 확정 — 중간 (13건)

| ID | 문제 | 위치 | 수정안 |
|---|---|---|---|
| M1 | 상담 신청 버튼이 NO_PROFILE·FAIL·로컬(OFF) 상태에서도 "기록되었습니다, 연락드립니다" 확정 안내 | Result.tsx requestCounsel | submitState에 따라 문구 분기(미제출이면 "제출 문제를 먼저 해결") |
| M2 | 6초 타임아웃 뒤 늦게 도착한 추천 Master가 결과지·제출 스냅샷에 미반영(구독 없음) | Result.tsx, recoMaster.ts | `onRecoMasterChanged` 구독 → recoMaster 갱신·stale 해제(payload 변경으로 재제출) |
| M3 | Route 부제(`--orange-deep`, 대비 3.9)가 14.4px 굵은 글자에 사용 — AA 미달 | global.css .level-card__subtitle | `--orange-text` |
| M4 | 저장소 차단(StorageBlockedError) 분기만 `fbSignOut` 누락 → Firebase만 로그인된 채 잔존 | auth.ts login catch | 분기에도 signOut |
| M5 | `ready_staff` name/dept 타입 미검증 → 객체가 오면 계정 화면 렌더 크래시 | auth.loadStaffCloud | 문자열 강제(`typeof === "string"`) |
| M6 | `pullRecoMaster()` FAIL이 명단 소스 상태에 미반영 → "실측 N건" 정상 배너 아래 추천은 시드 기준 | responsesSource.fetchStudents | StudentsData에 `recoStale` + 배너 |
| M7 | "중복 의심" 칩을 켠 채 정리하면 size 0 → 칩 disabled + dupOnly true로 **0명 화면 고착** | StudentsPanel.tsx | size 0이면 dupOnly 자동 해제, disabled 제거 |
| M8 | 배치 삭제 청크 일부 실패 시 `deleted:0` 실패로 오보고(앞 청크는 이미 삭제됨) | responsesSource.deleteStudentResponses | 완료 건수 집계해 부분 성공 안내 |
| M9 | 삭제·수정 진행 중 모달을 닫고 다른 학생을 열면 완료 콜백이 **다른 학생 모달을 닫거나 덮어씀** | StudentsPanel saveEdit/deleteOne | 완료 시 현재 detail 학번 일치 확인 |
| M10 | 콘솔에서 소문자 코드 문서(id=코드 일치)를 만들면 시드와 **별개 활동으로 중복 등록** | recoMaster.applyDocs | 문서 id가 CODE_PATTERN(대문자) 불일치면 제외 |
| M11 | 워크스페이스 로그아웃 후 재로그인 시 배너가 직전 세션 `cloudState`를 먼저 표시 | CounselDesk.tsx | 로그아웃 시 null로 리셋 |
| M12 | 학번 교정에서 서버가 MERGED 해도 `moveOutreachLocal`은 새 학번에 기록이 있으면 옛 기록을 버림 → 교정한 화면에서 회차 누락 | outreach.moveOutreachLocal | 서버와 같은 병합 또는 pullShared 재호출 |
| M13 | `validResponse`가 휴대전화·학과·학년을 검증하지 않음 — 아웃리치 핵심 채널이 서버 무결성 없음 | firestore.rules validResponse | `profile.phone is string && matches('^01[0-9]-[0-9]{3,4}-[0-9]{4}$')` 등 추가 → 규칙 게시(구버전 무연락처 응답의 update는 phone "" 허용 필요) |

## 4. 확정 — 낮음 (16건)

인쇄 시 버튼은 숨고 "이 버튼을 누르면" 힌트만 남음(Result/print) · 자격증 삭제 버튼 38px(44px 미달) · 휴대전화 정규식이 `010.1234.5678`류를 거부(normalizePhone은 처리 가능) · JAS 범위 min>max·집계 기간 from>to 무경고 · 회차 placeholder(`length+1`)와 실제 seq(`max+1`) 불일치 · 회차 Enter가 `saving` 잠금 우회 · 로컬 모드 아이디 maxLength 100 vs 정규식 40 · 구분 이동 메시지가 ACTIVE 계정에도 "승인해 주세요" · ErrorBoundary가 라우트 전환으로 회복 안 됨 · 중복 탐지 동일값 대량 시 O(m³)(1000명 동일 번호 1.3초) · 회귀 테스트 제목 "→ Level 1 (fallback)"인데 단언은 L2 · `color-scheme` 미선언(강제 다크모드 브라우저) · 자격증 개수·길이 상한 없음 · 재개 모달 포커스 트랩 없음 · CSV 버전 컬럼이 저장된 `survey_version` 대신 현재 번들 버전 · mock에 `counsel_requested_at` 없어 로컬 미리보기로 이중장치 검증 불가

## 5. 기각·기존 항목

- **기각(4)**: 재응시 시 이전 상담 신청이 유지되는 것(의도 — 신청 사실은 유지) · 380px 폰트 0.66rem이 과거 폐기값에 근접(주관) · 중복 배지의 표 폭(overflow-x로 수용) · 자격증 취득일·비고 열 공백(§12-06 미확정 필드)
- **기존 알려진 구조 항목(재확인만, 센터 판단)**: 다학기 응답 중복 집계(ADM-01) · 관리자 재계산 vs 저장 스냅샷(§3.1-② 설계, 규칙 개정 시 경고 필요) · 판정 엔진 L4 신호가 진단값 범위(1~5)를 검증하지 않음(**§2 Read-Only 영역 — 승인 필요**) · 공유 저장 실패분이 다음 저장에서 소멸(CNS-01) · 학번 교정 후 학생 세션의 옛 학번 재제출(§12-11) · onCloudSignedOut 초기 null 미감지 · App Check 부재 · 마스터 이메일 이중 하드코딩

## 6. 수정 순서 제안

1. **파일럿 전 필수(규칙 게시 동반)**: N1 mergeFields · N3 직렬화 · N5 삭제 규칙 · N6 doc_id · N2 pullShared/base · N4 큐 · N7 Route 표기 · N8 재개 판정 · N9 overflow-wrap · N12 회차 검증 · N13 바이트 검증 · M13 phone 규칙
2. **같은 커밋에 넣기 쉬운 것**: N10·N11 기관 · N14·N15 CSV · N16·N17 계정 · M1~M12 · 낮음 16건
3. **센터 판단 후**: 엔진 범위 검증(§2) · 다학기 집계 · 스냅샷 vs 재계산 · 공유 실패분 재병합
