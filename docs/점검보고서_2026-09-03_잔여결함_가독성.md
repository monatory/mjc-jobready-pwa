# MJC-READY 잔여 결함 수정 · 가독성 점검 보고서 (2026-09-03 오후)

> 09-02 역할별 점검(`점검보고서_2026-09-02_역할별_전과정.md`)에서 미수정으로 남은 항목을 처리하고,
> 학생·관리자 화면의 가독성을 계측·수정한 세션의 요약. 시간순 상세는 `docs/WORKLOG.md` 09-03 (2차~4차).

---

## 1. 요약

| 구분 | 내용 |
|---|---|
| 착수 기준 | MD 4종(CLAUDE.md·WORKLOG·점검보고서 2건) 대조 → 다음 작업 목록화 → 사용자 "진행" |
| 결함 수정 | 09-02 확정 결함 중 **중간 2건(A1·C2) + 낮음 20건 전건** 수정 (낮음 11건 → 2차, 9건 → 4차) |
| 가독성 | 학생 4화면·관리자 6화면 계측(글자 <13px·WCAG 대비 <4.5·가로 넘침) → CSS 49개 규칙 조정 |
| 서버 규칙 | `ready_staff` 관리자 자기 계정 보호 조항 추가 → `firebase deploy --only firestore:rules` 게시 완료 |
| 게이트 | 매 커밋 validate 76 · 회귀 19 · strict build 통과. 브라우저 실동 확인(로컬 모드) 후 원복 |
| 배포 | 커밋 4건 push (318b398 · 0010db0 · b252d8b + 문서) → GitHub Pages 자동 배포 |
| 미수행 | Firestore 미승인(PENDING) 계정 삭제 — 자동 실행 권한이 조회 스크립트를 차단해 사용자 UI 삭제로 이월 |

---

## 2. 결함 수정 내역

### 2.1 중간 (2건)

| ID | 문제 | 조치 |
|---|---|---|
| A1 | 담당자(행정)가 학번을 교정하면 상담 기록(`ready_outreach`) 이동 권한이 없어 항상 실패, 기록이 옛 학번에 고아로 남음 | 학번 변경은 상담사 계열만 — 담당자는 학번 칸 잠금(성명·학과·학년·연락처만 교정), `updateStudentProfile` 2중 가드 |
| C2 | 상담사·담당자 관리자가 자기 계정을 비활성·삭제 가능(라이브 실증) → 복구 불가 잠금 | 본인 행 "내 계정" 표시(버튼 비노출) + 규칙 LEAD 분기에 `request.auth.uid != uid` 게시 |

### 2.2 낮음 (20건)

**학생**
- S1 결과지 제출 조건을 서버 규칙과 동일(학번 영숫자 4~20·성명 1~30)하게, 형식 오류 시 설문 복귀 버튼
- S2 응답 문서에 `consent:{at, terms_version}` 저장(고지문 버전 `2026-09-03`), CSV 정보열람동의에 일시 병기
- S3 규칙 거부(permission-denied)를 네트워크 실패와 분리 — "학교 시스템이 제출을 받지 않았습니다" 배너
- S5 저장소 차단 브라우저: 동의 저장 자가진단 후 alert 안내
- S6 인쇄 푸터에 미제출 상태 1줄
- S8 기본정보 5칸 개별 오류 표시(`input--error`, 학년 블록 테두리) — "다음" 이후에만
- S9 학생 화면 칩 터치 목표 44px

**관리자**
- A4 연락처 교정 하이픈 정규화(`normalizePhone` 학생·관리자 공용) / A5 CSV 파일명 KST / A8 `ready_staff` 결측 필드 방어
- A9 추천활동 `saveRecoActivity({ isNew, baseUpdatedAt })` — updated_at 없는 오버라이드 영구 CONFLICT 해소
- A10/C12 클라우드 계정 비밀번호 모달 = 재설정 메일 전용, 로그인 화면 "비밀번호를 잊으셨나요?" 신설
- A11/C8 로그아웃 시 명단 캐시 무효화 + `onCloudSignedOut`로 타 탭·기기 로그아웃 전파
- A12 조회 중·실패 시 다운로드 버튼 잠금
- A14 명단 행 키보드 열기, 모달 role=dialog·Esc, ON/OFF role=switch

**상담사**
- C3 성공 토스트 타이머가 FAIL 경고를 지우던 것 / C10 기관 등록 실패 시 로컬 저장 생략·폼 유지 / C11 미저장 입력 시 닫기 확인
- C4 기관 수정 읽기→비교→쓰기(`updated_at`, CONFLICT 시 최신본 재로드)
- C5 상담 기록 형태 검사(`isValidOutreach`) + 라우트 전체 `ErrorBoundary`
- C7 배너 초기 "연결 확인 중", 등록부 실패와 분리 / C9 삭제된 기관 참조 명시 + 재선택 요구

### 2.3 미수정 (구조·센터 판단 필요 — 09-02 보고서 §5와 동일)
개인정보 보유기간 만료 수단(MISS-03) · 학기 구분(ADM-01) · PWA 여부(MISS-04) · 응답 삭제 기능(STU-04) ·
연구용 준식별자(STU-07) · 상담 카드 동시 편집 동기화 · 추천활동 활성기간(§12-08) · 링크북 재공개 ↔ §6.4

---

## 3. 가독성 점검

| 화면 | 발견 | 조치 |
|---|---|---|
| STEP 1 | 브랜드 주황 글자 대비 1.9(히어로·READY) · 동의 고지표 12.8px · 통계 라벨 10.9px | 글자용 `--orange-text`(대비 5.0)·`--orange-deep`(3.9) 신설, 고지표 14px, 라벨 상향 |
| STEP 2 | 기본정보 라벨 12.8px · 안내 12.5px(대비 4.0) · 제도 설명 12.8px | 13.8 / 13.1(대비 5.8) / 13.4~13.8px |
| STEP 3 | 5점 척도 라벨 **9.9px** | 11.2px + 진한 회색, 2줄 허용 |
| 결과지 | 추천 설명 12.8 · 영역 라벨 12.5 · 법적 문구 11.5px | 13.8 / 13.1 / 12.2px |
| 관리자 | `.small` 11.5px · 회색 배지 대비 3.4 · 폼 라벨 11.5px · 승인 대기 배지 4.3 | 12.2px / 5.8 / 12.5px / 5.6 |
| 명단 표 | 0.73~0.8rem 3단계 밀도 | **유지** — 09-02 "한 화면 표시" 요구 우선 |

재계측: 학생 3화면·로그인·추천활동·다운로드·계정 화면 대비 위반 0 (관리자 링크 의도적 흐림·버튼 그라데이션 오탐 제외).

---

## 4. 사용자 확인·정리 필요

- [ ] **미승인(PENDING) 계정 삭제** — 사용자 결정 "그냥 지워도 됨". 라이브 마스터 로그인 → 계정 관리 → 승인 대기 행 "삭제"
      (또는 `firebase firestore:delete ready_staff/<uid> --project mjc-ready-pwa`)
- [ ] 테스트 응답(20260001·20990001~3·20999999·12345·2222·201245 등)·테스트 계정 2건·점검용 기관 정리
- [ ] 라이브 실확인: 학생 정보 수정 / 상담 카드 클라우드 저장 / 추천활동 1건 게시 → 결과지 반영 / 통합 CSV /
      **재설정 메일 수신 / 타 탭 로그아웃 전파 / 기관 동시 수정 CONFLICT** (이번 수정의 클라우드 전용 경로)
- [ ] 링크북 잡카페 주소 재공개 ↔ CLAUDE.md §6.4 "히든 페이지" — 개정 또는 재비공개
- [ ] §2.3 구조 항목 결정, §12 확정 체크리스트 잔여

---

## 5. 검증 기록

```
데이터 검증  76건 통과 / 회귀 19건 통과 / strict build 통과 (커밋마다)
브라우저     로컬 모드(firebase.ts 임시 → git checkout 원복): 계정 본인 행 잠금 · 상담 카드 미저장 확인 3케이스 ·
             설문 칸별 오류 4+1 · 칩 44px · 명단 Enter/Esc · ON/OFF aria · 가독성 재계측 7화면, 콘솔 에러 0
서버 규칙    firebase deploy --only firestore:rules 컴파일·게시 성공
커밋         318b398(결함 13건) · 0010db0(가독성) · b252d8b(낮음 9건) · 이 보고서
```
