# MJC-READY 역할별 전 과정 점검 보고서 (2026-09-02 저녁)

> 학생·관리자(담당자)·상담사 세 입장에서 **처음부터 저장까지** 실제로 밟아 본 기록.
> 오전 점검(`점검보고서_2026-09-02.md`)의 §5 "보고만 24건"은 재보고하지 않았음. 코드 수정 없음(점검만).

---

## 1. 요약

| 구분 | 내용 |
|---|---|
| 점검 방식 | ① 라이브 실동(dev 서버 + 브라우저 자동 조작) ② 3관점 코드 정독(학생/관리자/상담사) ③ 발견 38건 전건 **적대적 반박 검증** |
| 라이브 결과 | 학생 전 과정 **Firestore 저장·재저장 실측 성공**. 관리자·상담사 화면은 로컬(mock) 모드로 전 기능 실동 확인 |
| 코드 점검 | 조사 38건 → **확정 28건** (중간 4 · 낮음 20 · 정보 4) / 기각 5건 / 중복 통합 5건 |
| 게이트 | validate 76 · 회귀 19 · strict build 통과 (점검 전·후 2회) |
| 즉시 조치 필요 | 없음(치명·높음 0건). 파일럿 전 **중간 4건** 수정 권장 (§4) |
| 데이터 정리 | 점검용 응답 `2026-2_20990003` 생성됨 — 기존 테스트 응답과 함께 삭제 필요 (§6) |

---

## 2. 라이브 실측 (실제 Firebase 프로젝트 `mjc-ready-pwa`)

### 2.1 학생 — 클라우드 모드 전 과정

| 단계 | 확인 내용 | 결과 |
|---|---|---|
| STEP 1 동의 | 체크 전 버튼 비활성, 체크 후 진행, 문구 "학교 시스템에 제출" 분기 | ✅ |
| STEP 2 설문 | 학번 앞뒤 공백 → 트림 저장, 전화 `01000000003` → `010-0000-0003` 정규화, 학년 코드 `본과2`, 자격증 행 추가 | ✅ |
| STEP 3 진단 | 27문항 자동 진행, 즉시 세션 저장, 완주 후 "결과 보기" | ✅ |
| 결과지 | L4 판정·JAS 96·보완영역·추천 3건·상담 CTA, 푸터 "안전하게 제출되었습니다" | ✅ |
| **저장 실측** | REST로 문서 `ready_responses/2026-2_20990003` 직접 조회 — profile·survey·diag·result·recommendations 3코드·버전 3종·schema 2·auth_uid 전부 정상 | ✅ |
| 새로고침 | 결과지 새로고침 → 재제출 없이 DONE 유지(스냅샷 비교) | ✅ |
| 이어서 진행 | 시작 화면 복귀 시 모달 → 결과지로 복귀 | ✅ |
| **수정 후 재제출** | 설문에서 성명 변경 → 진단 → 결과지 자동 재제출 → REST에서 `name` 갱신·`saved_at` 갱신 확인 | ✅ |
| 다시 진단하기 | confirm 후 세션 전부 삭제·시작 화면 | ✅ |
| 진입 가드 | `/survey`→`/`, `/diagnostic`→`/`, `/result`→빈 안내, 오타 주소→`/` | ✅ |
| 콘솔 | 에러 0 | ✅ |

### 2.2 관리자(담당자) — 로컬 모드(mock 40명), 점검용 로컬 계정

| 확인 내용 | 결과 |
|---|---|
| 로그인 → `#/admin`, 담당자 메뉴 4종(계정 관리 비노출), `#/counsel` 접근 시 `#/admin`으로 강제 복귀 | ✅ |
| 종합 현황 KPI·Level 분포·학과별 표 | ✅ |
| 명단: 프리셋 AND(우선대상 12 → +상담희망 9), Level 3 필터 6, 희망지역 "서울" 17, 12열 표 1366px에서 가로 스크롤 없음 | ✅ |
| 학생 상세 모달 → "정보 수정" 폼 (로컬 모드라 저장은 미리보기 안내) | ✅ |
| 추천활동: 등록 → ON/OFF → 삭제 → 같은 코드 재등록 폼 | ✅ |
| CSV 5종 캡처: UTF-8 BOM(EF BB BF) 확인, 통합 41행(헤더+40), 연구용은 성명·연락처 없음·R001 치환, 담당자용에 상담사 컬럼 없음, 자격증 Long Format 53행 | ✅ |

### 2.3 상담사 — 로컬 모드, 점검용 상담사·상담사 관리자 세션

| 확인 내용 | 결과 |
|---|---|
| `#/counsel` 진입, 메뉴 2종(상담사)/3종(관리자), `#/admin` 접근 시 `#/counsel`로 복귀 | ✅ |
| 연락 우선 큐 23 · 연계 사후관리 0, 상세 필터에 연락 상태·외부연계·취업상태 3종 추가, 15열 표 1366px 가로 스크롤 없음 | ✅ |
| 기관 등록 → 상담 카드 연계 select에 즉시 노출 | ✅ |
| 상담 카드 5종 저장: 연락함+메모 / 회차 추가(seq 1) / 최종 요약 / 연계 완료(기관) / 취업(취업처) → 요약 스트립·헤더 카운트(대기 22, 사후관리 1) 즉시 갱신, 로컬 저장본 필드 전부 정상 | ✅ |
| 상담사용 CSV: 연락상태·상담메모·상담회차·최종요약·외부연계단계·연계기관·취업상태·취업처 컬럼 포함, 익명 CSV에는 제거 | ✅ |
| 상담사 관리자 계정 패널: **본인 행에 비활성·삭제 버튼 노출** (§4 C2 실증) | ⚠ |

> 관리자·상담사의 **클라우드 저장 경로**(학생 정보 수정·상담 카드 Firestore 반영·추천활동 게시)는 마스터/교직원 비밀번호가 필요해 자동화하지 않았음 — 오전 보고서 §7과 동일하게 사용자 라이브 확인 항목으로 남김.
> 로컬 모드 전환은 `firebase.ts`를 임시 수정해 수행했고 점검 후 `git checkout`으로 원복·재빌드 확인함(작업 트리 clean).

---

## 3. 점검 방식

```
1단계 라이브   학생 흐름 실제 제출 → Firestore REST 검증 / 관리자·상담사 화면 로컬 모드 실동
2단계 정독     학생 / 관리자 / 상담사 3관점 병렬 코드 정독 → 38건 발견
3단계 반박     발견 건별 독립 검증자가 "결함 아님"을 기본 입장으로 코드로 반박 시도
               → 기각 5 · 하향 4 · 확정 28 (중복 5건은 통합)
```

기각 사유: 명세대로 설계(재응시 덮어쓰기·집계 기간 분리) / 실해 없음(타임아웃 뒤 늦은 성공은 멱등) /
사실 오인(규칙 `size()`는 문자 수, 이벤트 핸들러 예외는 백지 아님) / 낭비 아님(캐시 공유).

---

## 4. 확정 결함

### 4.1 중간 — 파일럿 전 수정 권장 (4건)

> **처리 현황 (2026-09-03)**: A2·A3 → 09-03 오전 세션 해결(계정 체계 수정 `218b3ba`·`af351d6`·`5512104`). A1·C2 → 09-03 오후
> 세션 해결(학번 변경 상담사 계열 한정 / 관리자 본인 행 잠금 + 규칙 게시). §4.2 낮음 20건 중 S1·S2·S3·A4·A5·A8·A9·A12·C3·C10·C11
> 11건 해결(2차), 나머지 S5·S6·S8·S9·A10/C12·A11/C8·A14·C4·C5·C7·C9도 09-03 (4차)에서 해결 — **§4.2 낮음 20건 전건 처리**. `docs/WORKLOG.md` 참조.

| ID | 위치 | 내용 | 최소 수정안 |
|---|---|---|---|
| **A1** | `responsesSource.ts:191-233`, `firestore.rules:75-77` | 담당자(행정)가 학번을 교정하면 응답은 새 학번으로 옮겨지지만 상담 기록(`ready_outreach`) 이동은 권한이 없어 **항상 실패** — 기록이 옛 학번 밑에 고아로 남고, 기록이 없어도 같은 경고가 나옴 | 학번 변경은 마스터·상담사 관리자만 허용(담당자는 성명·학과·연락처만) |
| **A2** | `auth.ts:144,173-188`, `Login.tsx:123` | 클라우드 모드에서 `@` 없는 아이디로 가입하면 **이 브라우저에만** PENDING 계정이 생기고 "접수되었습니다" 안내 → 마스터·상담사 관리자 목록에 영원히 안 보임. 가입 라벨이 "영문 소문자·숫자 4~20자"로 이 경로를 유도 | 클라우드 모드에서는 이메일만 허용 + 라벨 "이메일 주소" |
| **A3** | `auth.ts:161,244,274,367,423`, `Accounts.tsx` | 담당자↔상담사 역할 전환 UI 없음. 삭제는 DELETED tombstone이라 재가입은 "이미 등록된 이메일", 재로그인은 "삭제된 계정" → **콘솔 없이는 복구 불가**. 코드 주석의 공식 복구 절차("삭제 후 재신청")가 이 체인 때문에 작동 안 함 | 마스터 화면에 역할 변경 select + "삭제됨" 접힘 목록·복구 버튼(규칙 변경 불필요) |
| **C2** | `Accounts.tsx:50,118-135`, `firestore.rules:140-145` | 상담사 관리자가 **자기 계정**(과 다른 관리자)을 비활성·삭제할 수 있고 서버 규칙도 허용 → 자기 잠금. DELETED는 어느 화면에도 안 떠 마스터도 UI로 못 살림 (라이브 실증) | 본인 행 버튼 비노출 + 규칙 lead 분기에 `request.auth.uid != uid` |

### 4.2 낮음 (20건)

**학생**
- S1 결과지에서 뒤로가기·"이어서 진행"으로 설문 "다음" 버튼을 건너뛰면 무검증 자동저장값(잘못 고친 학번 등)이 그대로 재제출 → 규칙 403이 "네트워크 확인"으로 안내되고 재시도 영구 실패 (`Result.tsx:241` profileOk를 규칙과 동일 조건으로)
- S2 응답 문서에 동의 시각·고지문 버전이 없음 — CSV "정보열람동의"는 하드코딩 "동의". 구조적 증거(동의 없이는 진입 불가)는 있으나 명세 §7.2 "별도 Consent 값"과 불일치 (`consent:{at, terms_version}` 1필드 추가)
- S3 제출 실패 원인이 전부 삼켜져 권한 거부와 네트워크가 같은 문구 (`permission-denied` 분리)
- S5 브라우저 저장소 차단 환경에서 "다음"이 아무 안내 없이 시작 화면으로 되돌아옴 (동의 저장 자가진단 후 alert)
- S6 인쇄 CSS가 실패 배너를 숨기고 푸터도 FAIL을 표시하지 않아 PDF에 미제출 흔적이 없음 (푸터 1줄)
- S8 필수 오류 시 기본정보 5칸에 개별 오류 표시 없음 (input--error 클래스)
- S9 학년·지역 등 chip 버튼 높이 약 38px — 같은 파일의 `.opt`는 44px (학생 화면 chip만 min-height 44)

**관리자**
- A4 관리자 연락처 교정은 하이픈 정규화 없음 → 학생 입력분과 형식 불일치, Excel 앞 0 손실 (Survey와 같은 3줄)
- A5 CSV 파일명 날짜가 UTC(`toISOString().slice(0,10)`) — §7.2.1-5 위반, 새벽 다운로드가 전날 이름 (`todayStr()`)
- A8 `ready_staff` 문서에 `created_at`이 없으면 정렬·렌더에서 throw → 목록 로컬 폴백 또는 백지 (콘솔 수기 문서 한정, `?? ""`)
- A9 `updated_at` 없는 추천활동 오버라이드는 수정·ON/OFF가 항상 CONFLICT — "최신 내용 불러오기"로도 해소 안 됨(삭제→재등록으로 우회 가능) (`recoMaster.ts:247` 조건을 신규 폼 플래그로)
- A10/C12 클라우드 계정 "비밀번호 변경" 모달이 현재·새 비밀번호를 요구하지만 입력값을 무시하고 재설정 메일만 발송, 로그인 화면에 "비밀번호 찾기" 없음
- A11/C8 로그아웃이 학생 명단 메모리 캐시를 비우지 않음. 탭 간: Firebase 로그아웃은 전파되나 세션은 탭별이라 다른 탭은 관리자 UI인 채 모든 저장이 "네트워크 확인"으로 실패 (`onAuthStateChanged` 구독)
- A12 조회 중·실패 상태에서도 다운로드 버튼 활성 → 헤더만 있는 CSV (`disabled`)
- A14 명단 행 키보드 접근 불가, 모달 role/Esc 없음, ON/OFF 토글 aria 없음 (저장소 전체 aria 0건)

**상담사**
- C3 성공 토스트 타이머가 뒤이어 온 FAIL 경고를 조기 삭제 (`flash` 타이머 clear, AgencyManager 동일)
- C4 기관 수정이 로컬 캐시 기준 전체 덮어쓰기 — §7.2.1-12 "읽기→비교→쓰기" 미적용 (`updated_at` + CONFLICT)
- C5 `ready_outreach`는 규칙·클라이언트 모두 형태 검사 없음 + ErrorBoundary 부재 → 깨진 문서 1건이 명단·카드 백지 (§7.2.1-11 미적용)
- C7 워크스페이스 배너 초기값이 "미연결"이라 로그인 직후 매번 "이 브라우저에만 보관" 경고가 먼저 뜸; 등록부 조회만 실패해도 LOCAL 표시 (null → "연결 확인 중")
- C9 삭제·구분변경된 기관을 참조한 연계 기록은 select가 빈칸으로 보이지만 옛 id로 저장 통과 (CON-09 부분 중복)
- C10 기관 등록이 클라우드 실패해도 로컬에 남아 연계에 선택 가능 → 다른 상담사에겐 "(삭제된 기관)" (FAIL 시 로컬 저장 생략)
- C11 상담 카드 입력 중 모달 바깥 클릭이 확인 없이 닫혀 메모·요약 소실 (dirty 확인)

### 4.3 정보 (4건, 조치 선택)

S11 카카오톡 인앱 브라우저 `window.print()` 무동작 가능성(힌트 1줄) · A7 저장소 차단 브라우저에서 로그인 버튼 고착(운영 경로 무관) · A15 `dept/grade` 결측 문서는 "정보 수정" 버튼만 무반응(콘솔 수기 한정) · C6 회차 Enter 키의 IME 이중 발화(Windows Chrome/Edge 재현 불가, 예방 1줄)

### 4.4 기각 (5건)

S4 타임아웃 뒤 늦은 성공(멱등·무해) · S7 재응시 덮어쓰기로 교정 이력 소실(§7.1 설계) · S10 규칙 `size()` 바이트 의혹(문자 수) · A6 상담사의 `#/admin` 한 프레임 조회(캐시 공유·낭비 없음) · A13 집계 기간 vs 실시일 기간 분리(§6.1 명세)

---

## 5. 정상으로 확인된 것 (요지)

- 학생: 가드 4종·재개 모달·새로고침 복원·설문 검증(규칙과 동일 정규식)·조건부/복수선택·진단 타이머·엔진 결정론·Master 6초 폴백·제출 effect(무한 루프 없음)·payload↔규칙 정합·익명 세션 격리·XSS 없음·인쇄 CSS
- 관리자: 역할 라우팅·PENDING/DISABLED/DELETED 분기·authReady 선행·KPI 정의(프리셋과 동일)·필터 AND·정보 수정 트랜잭션·추천활동 검증↔규칙 정합·CONFLICT·tombstone/archived·캐시 무효화·CSV(BOM·주입 방어·OUTREACH 제거·R번호 결정론·Long Format)·연타 방지
- 상담사: 큐 정의 단일 함수 공유(헤더=칩=필터)·tel: 링크·정렬 5종·회차 seq 원격 기준 연산·patch가 ops를 덮는 경로 없음·`undefined`로 필드 해제 가능·FAIL 5초 경고·기관 id 충돌 없음·tombstone 전파·lead UI가 규칙에 거부될 호출 없음·CSV 컬럼

---

## 6. 사용자 확인·정리 필요

- [ ] **Firestore 테스트 응답 삭제**: 이번 점검 `2026-2_20990003` + 기존 `20990001·20990002·20999999·12345·2222·201245` 등 (전체 32건 중 실응답 선별 필요). 테스트 계정 `mjc.admin.test@`·`mjc.counselor.test@` 2건, 점검용 기관 여부 확인
- [ ] 라이브 마스터 로그인 실확인(오전 보고서 §7 동일): 학생 정보 수정 / 상담 카드 클라우드 저장 / 추천활동 1건 게시 → 학생 결과지 반영 (`ready_reco_master` 현재 0건)
- [ ] §4.1 중간 4건 수정 여부 결정 — 실제 외부 상담사 3명(innosjob)이 이미 승인돼 있어 A2·A3·C2는 운영 중 실제로 밟을 수 있는 경로임

---

## 7. 검증 기록

```
데이터 검증  76건 통과 / 회귀 19건 통과 / strict build 통과 (점검 전·원복 후 2회)
라이브       학생 제출 REST 검증 2회(신규·갱신) · 관리자 CSV 5종 캡처 · 상담 카드 5종 저장 · 표 폭 1366px 2종
작업 트리    코드 변경 없음 (firebase.ts 임시 수정 → git checkout 원복 확인)
```
