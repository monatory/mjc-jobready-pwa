# MJC-READY 10관점 병렬 점검 보고서 (2026-09-06, 시범 운영 전일 최종)

> 학생 배포 최종 점검(같은 날 오전 보고서) 뒤 사용자 지시 "마무리로 병렬 점검 10번"에 따라, 시범 첫날(9/7) 위험 중심으로
> 10개 관점을 읽기 전용으로 병렬 점검하고, 주요 주장 36건을 3개 검증자가 코드로 반박 검증한 결과.
> **코드 변경 0건** — 이 보고서는 판정과 권고만 담는다. 저장소 `main` = `origin/main`.

---

## 1. 요약

| 구분 | 내용 |
|---|---|
| 관점 | 학생 3(최초 응시·재응시/세션·제출/클라우드) · 엔진 정합 1 · 규칙 대조 1 · 관리자 2(명단/삭제·대시보드/CSV) · 계정 1 · 상담사 1 · 운영/배포 1 |
| 발견 | 94건 (치명 0) → 중복 통합 후 치명·높음·중간 후보 36건 반박 검증 |
| 확정 | **중간 이상 14건** · 낮음(시범 후) 30여 건 · 기각·범위 축소 9건 |
| 실측 | 규칙 `string.size()`는 **글자 수** 기준(한글 11자 성명·21자 학과 → 200, 31자 → 403) — 09-05 보고서의 "바이트 기준" 판단 정정 |
| 내일 전 판단 | 코드 수정 없이 시범 시작 가능. 다만 **상담사 간 동기화(§2-①)** 는 첫날 중복 연락 위험이라 운영 규칙(작업 전 ↻ 새로고침)이라도 반드시 공유 |
| 테스트 데이터 | 오늘 실측으로 응답 2건(`2026-2_99990906`, `2026-2_99990907`) + 익명 Auth 계정 2개 생성 → 시범 전 정리 대상 |

## 2. 확정 — 시범 첫날 실질 위험 (수정 권장, 전부 소폭·저위험)

| # | ID | 문제 | 위치 | 최소 수정안 | 심각도 |
|---|---|---|---|---|---|
| ① | C9-2 | 상담 기록 공유가 **로그인 시 1회 pull + 수동 ↻ 새로고침** 뿐(onSnapshot·visibilitychange 0건). 여러 상담사가 각자 PC에서 새로고침 없이 작업하면 이미 연락한 학생이 큐에 남아 **중복 연락**. 배너는 "기록이 상담사 간에 공유됩니다"로 실시간처럼 안내. 등록부(AgencyManager)는 새로고침 버튼 자체가 없음 | CounselDesk.tsx:56-58,159 · AgencyManager.tsx:19 | `visibilitychange`/`focus`에 스로틀된 `pullShared()` + 배너 문구 "다른 상담사 기록은 ↻ 새로고침 시 반영" + 등록부 새로고침 버튼 | **중~상** |
| ② | C9-1 + C9-6 | 연락 기록 저장이 `{status, memo}`를 통째로 전송하고 병합은 필드 존재만 봄 → 옛 캐시(memo "")로 상태만 바꿔 저장하면 **다른 상담사가 방금 쓴 메모가 빈 값으로 덮임**. 카드가 열린 채 pull이 와도 편집 필드는 useState 초기값이라 옛 값 유지(거짓 "미저장" 경고 + 덮어쓰기 유도) | CounselRecord.tsx:49-65,125 · cloudStore.ts:121 | 마운트 시점 스냅샷(useRef) 대비 **바뀐 필드만** patch. dirty 판정도 스냅샷 기준 | 중 |
| ③ | C9-3 + C9-4 | 회차 추가가 결과를 기다리지 않고 입력창을 비움 → FAIL이면 재타이핑. FAIL로 로컬에만 남은 기록은 다음 성공 저장·새로고침·로그아웃에서 **무경고 소실**되는데 안내는 "이 브라우저에는 보관됐다" | CounselRecord.tsx:108,129-130 · cloudStore.ts:144-152 · auth.ts:392 | `await doSave` 후 FAIL 아닐 때만 비움 + 문구를 "이 화면을 떠나기 전 다시 저장" 으로 정정 | 중 |
| ④ | S1-1 (S2-7·S3-1·E4-7 통합) | 정규화·조건부 정리가 "다음" 버튼에서만 실행. 결과지 도달 후 설문으로 돌아가 휴대전화를 하이픈 없이 고치고 "다음" 없이 재개 모달 "이어서 진행"으로 결과지에 오면 **DENIED**(안내는 "학번·성명 확인"). 부수 발견: 같은 경로로 휴대전화를 지우면 규칙이 `''`를 허용해 **연락처 없는 응답이 조용히 제출** | Survey.tsx:44-47,99-116 · Result.tsx:138-143,176 · saveResponse.ts:66 | `saveNow`에 `normalizePhone` + Result `profileOk`에 휴대전화(빈 값 불허)·학과 ≤60·학년 패턴 검사 + NO_PROFILE 문구에 휴대전화 언급 | 중 |
| ⑤ | S2-9 + S3-5 | "잡카페 상담 신청하기"가 SAVING·DONE 상태에서 즉시 "연락드립니다" 확정 alert → 직후 재제출 FAIL이면 버튼은 "✓ 상담 신청됨" 유지, FAIL 배너에 상담 언급 없음 → 학생이 신청됐다고 믿고 이탈 | Result.tsx:150-169,377,386-397 | SAVING이면 "제출 완료 후 전달" 문구, FAIL/DENIED 배너에 "상담 신청도 아직 전달되지 않았습니다" 한 줄 | 중 |
| ⑥ | E4-1 | L3 결과지 본문 "학교의 취업지원 활용에도 **적극적**이에요" — 엔진은 school_support 무관(전수 열거: L3+ 68조합 중 36건이 "혼자"·"필요할 때"). 회귀 테스트 :180-193이 바로 그 조합 | result_templates.json:21 · Result.tsx:289 | 템플릿은 §2 Read-Only·§12-07 미확정 → **센터 확인**. 소비자 측 임시안: `school_support !== "ACTIVE"`면 앞 절만 치환 | 중 |
| ⑦ | A6-1 | 일괄 삭제가 화면 캐시 기준, 삭제 직전 서버 재확인 없음(자동 재조회 0). 명단 적재 후 같은 학번(같은 문서키)이 실제 제출되면 그 **실응답이 삭제** | responsesSource.ts:136-146 · StudentsPanel.tsx:249 | 운영: **테스트 정리는 오늘(9/6) 완료, 실행 직전 ↻ 새로고침**. 후속: `runTransaction`으로 `saved_at` 일치 시만 delete | 중(비가역) |
| ⑧ | S2-1 | 재개 모달에 누구의 진단인지 표시 없음. 같은 탭을 공유하는 PC에서 다음 학생이 "이어서"를 누르면 앞 학생의 결과지·개인정보 노출 | Start.tsx:145-168 | 모달에 성명 마스킹(김*수)·학번 뒤 4자리 표시 | 중(공용 PC) / 낮음(개인 폰) |
| ⑨ | S1-2 (O10-3) | `.input` 14.08px, 학년 연도 13.6px → **iOS Safari·인앱에서 입력 포커스 시 화면 자동 확대** | global.css:333-341,910-914 | `@media (max-width:480px){ .input, select.input, .grade-picker__year { font-size:16px } }` | 낮음~중 |
| ⑩ | U8-4 | 로그인 실패가 `too-many-requests`·`user-disabled`·`invalid-credential` 구분 없이 "아이디 또는 비밀번호가 올바르지 않습니다" → 첫날 오타 반복 후 임시 차단을 모르고 계속 재시도 | auth.ts:283-293 | 코드별 문구 2~3줄 분기 | 낮음~중 |

## 3. 확정 — 운영 규칙·콘솔 설정으로 흡수 (코드 불필요)

| ID | 내용 | 조치 |
|---|---|---|
| S3-3 | Firebase Auth **신규 계정 생성 100건/IP/시간** 한도(공식 문서 확인). 학생 첫 제출마다 익명 계정 1개 생성 → 실습실·캠퍼스 Wi-Fi(NAT 동일 IP)에서 1시간 내 100명 초과 시 101번째부터 FAIL("네트워크 확인") | 단체 응시 계획이 있으면 Firebase 콘솔 Authentication → 설정 → **가입 할당량 임시 상향** 예약(문서: "schedule a temporary increase"). 또는 LTE 병행 안내 |
| O10-1 | 라이브 `index.html`·에셋 `max-age=600`, 단일 청크, 배포 시 구 에셋 즉시 404 → 배포 직후 최대 10분간 캐시된 index로 여는 사용자만 백지(F5로 복구). 진행 중 학생은 무영향 | **시범 첫 주 근무·홍보 시간대 배포 금지** 원칙(CLAUDE.md §8 기재 권장) |
| U8-3 | 로그아웃 없이 탭을 닫으면 Firebase 세션(IndexedDB)과 `mjc_ready_outreach`(학번·메모 평문) 잔존 | 상담사 각자 PC 사용, **퇴근·자리 이탈 시 로그아웃**. 후속: Login 마운트 시 캐시 제거+signOut |
| O10-2 | Web API 키 리퍼러 무제한(getProjectConfig 200) + App Check 부재 + 익명 인증 → REST로 형식만 맞춘 가짜 응답 생성 가능. 리퍼러 제한은 curl에서 위조 가능해 **완화책일 뿐**이고 근본은 App Check/학생 인증(§12-11) | 시범 전일 설정 변경은 비권장(오설정 시 전면 장애). 시범 중 §12-11과 함께 검토 |
| A6-1 | (§2-⑦ 운영 부분) | 오늘 정리 + 실행 직전 새로고침 |

## 4. 확정 — 시범 후 처리 (낮음, 문제 없음 확인 포함)

- **학생**: S2-4 두 탭에서 옛 탭 상담 클릭이 같은 학생의 최신 응답을 옛 값으로 덮음(부분 병합으로 축소, 초기 제출 실패 분기 필요) · S2-5 동의만 하고 떠나면 다음 학생 문서 `consent.at`이 앞 학생 시각(공용 PC) · S3-2 Master pull 6초 타임아웃 시 익명 로그인 이중 호출(고아 익명 계정 1개, DENIED는 3중 조건 극저확률) · S3-4 15초 타임아웃 뒤 늦은 성공이 FAIL로 표시(유실 없음) · S1-4 `/diagnostic` 가드가 설문 1문항만 확인(URL 조작 필요) · S1-5/O10-6 aria-pressed·aria-live·모달 라벨 · S1-6 필수 누락 시 최상단 스크롤 · S1-7 학번 inputMode
- **엔진·결과지**: E4-2 취업 L2 학생에게 "취업준비 Route"(L3 단계명)와 "진로설정 단계" 동시 표시, `route_tags` 미참조, CSV Route 영문 코드 · E4-3 영역 3.47이 "3.5"로 보이며 보완영역(4개 영역에서 3.45~3.49 도달 가능; 표시만 2자리로) · E4-4 L3+직무정보 취약 시 취업컨설턴트 상담 활동이 추천 3건에서 탈락(Master weak_domains 센터 판단), CSV 추천 번호가 순위가 아닌 priority 값 · E4-5 희망시기 "기타·미정"의 판정 근거 문구 "6개월 초과" · E4-6 04시트 gate/critical이 규칙 미참조 G2(CD01)에도 표기 · E4-8 빈 스냅샷 재계산 · E4-9 회귀 미커버 경계 5건 · E4-10 validate-data 미검사 4건
- **관리자**: A6-2 형식 오류(skipped) 문서 UI 삭제 불가 · A6-3 삭제 배치 순서(200건 초과 시 고아 상담 기록) · A6-6 집계 기간 ≠ 실시일 기간 · A6-7 재조회 중 중복 칩 해제 · A6-8 row key · A6-9 학과 표기 편차 · A7-1 **연구용 익명 CSV에 자유 입력(희망직무·자격증명·학과) 잔존 — 연구 제공 전 필수 수정** · A7-2 학과 자유 입력 → 학과별 현황 분산 · A7-3 스냅샷 복원 시 활성기간 미검사(시드는 2027-01-01부터) · A7-4 통합 CSV 버전 열 없음 · A7-7 구버전 학년 라벨 · A7-8 0건 다운로드 · A7-9 수정일시 · A7-10 설문 희망+결과지 신청 구분
- **계정·상담사**: U8-1 다른 탭 다른 계정 로그인 시 세션 불일치 · U8-2 비활성·구분 이동이 진행 중 세션 미반영(저장은 FAIL 경고 표시됨) · U8-5 강제 로그아웃 안내 없음 · U8-6 복원 null 고착(조건 좁음) · U8-7·8·9·10 문구 · C9-5 학번 단일 키 기록의 학기 전환 영향(첫 학기 무관) · C9-7 "취업 확정 자동 제외" 안내 vs 큐 · C9-8·9·10
- **규칙**: R5-3 `ready_staff` 목록 읽기가 ACTIVE 전원(화면은 LEAD/마스터만) · R5-4 LEAD가 동급 LEAD 비활성 가능(주석과 불일치, 센터 설계 확인) · R5-5~7 outreach·agencies·staff 쓰기 형태 검증 없음(클라이언트 방어 있음) · R5-8 익명이 reco_master `updated_by` 열람
- **운영·문서**: O10-5 문서↔코드 불일치 6건 확정 — CLAUDE.md §8 `vite-plugin-pwa`·`Recharts`(둘 다 미사용), §11 "검증 53·회귀 15"(실제 76·19), deploy.yml 스텝명 53/15, deploy.yml·links.html "콘솔 게시"(실제 CLI), sessionState.ts:2 "서버 전송 없음" 주석, §14 "저장소 (예정)" · O10-4 시작 화면 "관리자" 링크(대비 1.76) · O10-8 404.html · O10-9 favicon·OG 없음(카톡 링크 카드) · O10-10 인앱 인쇄 안내

## 5. 기각·정정

| ID | 판정 | 근거 |
|---|---|---|
| S1-3 · S3-6 · R5-1 | **기각** | 규칙 `size()`는 글자 수. 실측: 성명 11자·학과 21자 한글 → 200, 성명 31자 → 403. 클라이언트 학과 길이 미검증(≤60자)만 낮음으로 남김 |
| (09-05 N13 정정) | **오류 정정** | 추천활동 화면의 UTF-8 바이트 검사는 규칙보다 **엄격**(한글 활동명 100자 → 33자 제한). 시범 중 문제 없으나 글자 수 검사로 되돌리는 것이 정확 |
| S2-2 | 부분 기각 | 시드 활성기간이 전부 2026-08-01~12-31이라 재방문만으로 재제출 없음. 상담 클릭 시 `saved_at` 갱신은 사실(낮음) |
| E4-2 | 범위 축소 | 취업 Route 학생은 L2~L4만(JAS 최저 33) — L1 표기 사례 없음 |
| S3-2 · A6-3 · A7-3 · U8-2 · U8-6 · S1-4 | 조건부 | 발생 조건이 좁거나(3중 경합·200건 초과·2027년 이후·URL 조작) 표현 일부 부정확(U8-2: 저장 실패는 FAIL 경고 표시됨) |

## 6. 센터 판단 항목 (개발 임의 변경 불가)

- E4-1 L3 결과지 문구(§12-07) · E4-4 `EMP_CONSULT_JOBCAFE` weak_domains에 JOB_INFO·COMPETENCY 추가 여부(관리자 화면에서 가능) · E4-2 Route 표기 표준("취업 Route" vs "취업준비 Route")
- R5-4 상담사 관리자 동급 비활성 허용 여부 · R5-3 교직원 목록 읽기 범위(규칙 게시 동반)
- O10-4 시작 화면 "관리자" 링크·`/links.html` 공개 유지 여부(09-03 사용자 지시로 재공개) · O10-2 App Check·학생 인증(§12-11)
- A7-1 연구용 CSV 자유 입력 처리 방식(제거 vs 분류 라벨) — 연구 제공 전 필수
- 기존 이월: 상담 신청 별도 필드 vs 설문값 변경 · 다학기 집계(ADM-01) · 스냅샷 vs 재계산 · CNS-01

## 7. 검증 기록

```
점검      10관점 병렬(읽기 전용, 파일 수정·서버 기동 금지) → 94건
검증      3개 검증자 36건 반박 검증(코드 인용·node 전수 열거·curl 실측) → 확정 27 · 조건부 8 · 기각 1(3건 통합)
실측      규칙 size() 단위: 익명 REST 제출 3회(200/403/200) → 글자 수 기준 확정
          Firebase Auth 가입 한도 100/IP/시간(공식 문서), 라이브 캐시 max-age=600·단일 청크(curl -I)
게이트    코드 무변경 — 오전 보고서의 validate 76 · 회귀 19 · strict build 통과 상태 유지
```
