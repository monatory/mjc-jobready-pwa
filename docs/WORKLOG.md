# MJC-READY 작업 일지 (WORKLOG)

> 세션 단위 작업 기록. 최신이 위. 상세 명세는 루트 `CLAUDE.md`(살아있는 명세)가 진실 기준이며,
> 이 파일은 "언제 무엇을 왜 했는지"의 시간순 이력만 담는다.

---
## 2026-09-05 (2차) — 20관점 병렬 점검 확정 46건 전면 수정 · 규칙 게시

사용자 지시 "1순위부터 최종까지 수정, 권한 자동, 안정성 최우선". 오후 20관점 병렬 점검
(`docs/점검보고서_2026-09-05_20관점_병렬점검.md`)에서 확정한 치명 2·높음 15·중간 13·낮음 16건을 전부 처리했다.
센터 판단 항목 중 판정 엔진 범위 방어(§2 영역)는 유효 입력의 판정을 바꾸지 않는 방어 코드만 넣고 CLAUDE.md §2에 명시.

### 치명·높음 (17건)
- **N1** 학생 제출 `setDoc(..., { mergeFields })` — 새 세션 재응시가 `counsel_request`·교정 이력을 지우던 전체 덮어쓰기 제거
- **N2** 상담 기록: `pullShared`가 로컬을 원격 스냅샷으로 통째 교체(로컬 잔존분 보존 폐지) + `pushOutreachMerged`는 원격
  문서가 없으면 빈 기록에서 시작 — 삭제된 학생 기록이 다른 브라우저 캐시로 부활·잔존하던 경로 차단
- **N3** 학생 제출 직렬화(모듈 Promise 체인) — SAVING 중 상담 신청 클릭 경합으로 신청이 유실될 수 있던 것
- **N4** 연락 우선 큐: Route 제외를 L3+ 자동 발굴에만 적용 — 상담을 직접 신청한 진학·창업 학생이 큐에서 빠지던 것
  (mock 계측: 큐 23 → 28명)
- **N5** 규칙 `ready_responses` delete를 `isCounselSide()`로 축소(담당자 SDK 직접 삭제 차단) → **게시 완료**
- **N6** `StudentRecord.doc_id`(실제 문서키) 도입 — 삭제·정보 수정·다학기 보호가 semester 재구성 키 대신 실제 키 사용
- **N7** 결과지 Route 3분기 — 미정(UNDECIDED)이 "취업준비 Route"로 찍히던 것 → "진로탐색 Route"
- **N8** `getResumeState`가 기본정보만 입력한 상태도 "진행 중"으로 판정 — 공용 PC 다음 학생에게 개인정보 프리필되던 것
- **N9** 380px 이하 척도 라벨 `overflow-wrap: anywhere` — "보통이다"가 320px·글꼴 확대에서 칸 밖으로 넘치던 것 (320px 계측 넘침 0)
- **N10** 기관 구분 변경을 삭제로 오판하던 stale 판정 수정 — 구분 변경된 기관은 "(취업처로 구분 변경됨)"으로 선택지 유지
- **N11** `removeAgency`가 로컬 archived 캐시에 이름 즉시 기록 — 삭제한 세션에서도 "(삭제된 기관) 이름" 표시
- **N12** `sanitizeOutreach`: 회차 원소 형태 검사(seq 유한 수·content 문자열) + 저장 base 정리 + seq 계산 NaN 방어
- **N13** 추천활동 길이 검증을 UTF-8 바이트(`TextEncoder`)로 — 규칙 `size()`와 단위 일치, 안내는 한글 기준 글자 수
- **N14** 통합 CSV 추천활동에 "(종료)" 표기 · **N15** 04시트 gate_passed/critical_unmet을 문항별(`gate`/`critical` 속성 × answer_at_least)로
- **N16** 삭제(tombstone) 계정 복구: `loadStaffCloud`가 DELETED 포함, 계정 화면에 "삭제된 계정" 접힘 구역 + 복구(승인 대기로) 버튼,
  재가입·로그인 안내 문구를 실제 동작에 맞춤
- **N17** 클라우드 설정인데 조회 실패로 로컬 폴백이면 직접 등록 잠금 + "다시 연결" 버튼

### 중간 (13건)
M1 상담 신청 버튼 안내를 제출 상태별 분기(미제출·로컬 모드) · M2 결과지가 `onRecoMasterChanged` 구독(늦은 Master 반영) ·
M3 Route 부제 색 `--orange-text`(대비 5.0) · M4 저장소 차단 분기도 `fbSignOut` · M5 `loadStaffCloud` 필드 타입 강제 ·
M6 `StudentsData.recoStale` + 배너(관리자·워크스페이스) · M7 중복 의심 칩 자동 해제 · M8 배치 삭제 부분 성공 건수 보고 ·
M9 삭제·수정 완료 콜백이 현재 모달(doc_id) 확인 · M10 문서키가 코드 형식이 아니면 병합 제외 · M11 로그아웃 시 cloudState 리셋 ·
M12 `moveOutreachLocal` 병합 · M13 규칙 `validResponse`에 학과·학년·휴대전화 형식 → **게시 완료**

### 낮음 (16건)
인쇄 시 힌트 숨김 · 자격증 삭제 버튼 44px · 휴대전화 검증을 숫자 기준으로 · JAS min>max·기간 from>to 경고 · 회차 placeholder max+1 ·
Enter의 saving 우회 차단 · 로컬 아이디 maxLength 40 · 구분 이동 메시지 상태별 · ErrorBoundary hashchange 리셋 · 중복 탐지 선형화
(1000명 동일 번호 1283ms → 37ms) · 회귀 테스트 제목 정정 · `color-scheme: light only` 메타 · 자격증 20개 상한 · 재개 모달 autoFocus ·
CSV 버전 컬럼을 응답 저장값으로 · mock에 `counsel_requested_at` 5명 부여 · 관리자 배너에 규칙 버전 불일치 건수

### 검증
validate 76 · 회귀 19 · strict build 통과. 규칙 컴파일·게시 성공. 브라우저(로컬 모드 → 원복): 재개 모달·autoFocus, 미정 Route
"진로탐색 Route" + LEVEL 1, 320px 척도 5라벨 넘침 0, 로컬 모드 상담 신청 안내 분기, 워크스페이스 "신청" 열 2명·배지·상세 일시,
기관 등록→구분 변경→선택지 유지(stale false)→삭제→archived 즉시 기록·배지 "(삭제된 기관) 점검기관A", 콘솔 에러 0.
node: 엔진 JR01=6·"5" → required_ok false, 유효 L4 유지 / 중복 탐지 7케이스 + 성능.

### 남은 것 (사용자·센터)
- [ ] 라이브 확인: 학생 재응시 후 상담 신청 유지(mergeFields) · 마스터 응답 삭제·복구 · 삭제된 계정 복구 · 규칙 게시 후 학생 제출 정상(휴대전화 형식)
- [ ] 센터 판단: 다학기 중복 집계(ADM-01) · 저장 스냅샷 vs 재계산(배너로 불일치 건수만 표시) · 공유 실패분 재병합(CNS-01) · 학생 인증(§12-11)

---

## 2026-09-05 — 사용자 요청 5건 반영 + 09-02 "보고만" 항목 12건·정보 4건 정리

사용자 지시 "MD 확인 → 작업 진행" 후 세션 중 요청 5건이 추가됨. 요청 5건을 먼저 처리하고, 09-02 점검에서
"구조·센터 판단 필요"로 남겼던 항목 중 **판단 없이 개발로 닫을 수 있는 것**을 함께 처리했다.
요약 보고서: `docs/점검보고서_2026-09-05_사용자요청_보고만항목.md` (상위 폴더 사본 동기화).

### 사용자 요청 5건
1. **진학·창업 Route 결과 카드에 Level 미표기** (`Result.tsx`): Route 오버라이드 템플릿의 제목("진학·창업 준비 Route")이
   Level 제목을 대체하고 있었다. Level 제목("LEVEL 2 · 진로설정 단계")을 항상 표시하고 Route 제목은 부제
   (`.level-card__subtitle`)로 내림. 엔진은 비취업 Route를 항상 Level 2로 판정(§3.5) — 판정 무변경.
2. **갤럭시 S23 등에서 진단 척도 라벨("그렇지 않다·보통이다·그렇다") 미표시** (`global.css`): 380px 이하 미디어쿼리가
   가운데 3개 라벨을 `display:none`으로 숨기던 것(감사 S2-06의 의도적 축약)이 360px 폰에서는 "글씨가 안 나오는
   오류"로 보였다. 5개 라벨 전부 표시(0.66rem, 2~3줄 허용, keep-all) — 360px 계측: 5개 모두 block, 가로 넘침 0.
3. **마스터의 학생 응답 삭제** (`responsesSource.deleteStudentResponses`, `StudentsPanel`): 상세 모달 "🗑 응답 삭제"
   (1건, confirm) + 명단 하단 "필터 결과 N건 삭제" (실시일 기간 필터로 범위를 좁힌 뒤, "삭제" 타이핑 확인).
   `ready_responses` + 상담 기록 `ready_outreach`를 writeBatch(200건 단위)로 삭제. 상담 기록은 학번 단일 키라
   **그 학번의 응답이 전부 삭제 대상일 때만** 함께 지운다(다학기 보호). 마스터 계정·실측 모드에서만 노출.
   규칙 변경 없음(responses delete = 승인 교직원, outreach write = 상담사 계열 — 마스터 포함).
4. **중복 등록 방지 방안** (`duplicates.ts`): 같은 학기 같은 학번은 문서키가 같아 이미 덮어쓰기(중복 불가). 문제는
   **학번 오기입 재응시** → 명단에서 ① 휴대전화(숫자만 비교) 동일 ② 성명+학과 동일인데 학번이 다른 응답을
   "⚠ 중복 의심" 칩·배지·상세 안내(상대 응답 목록)로 표시. 자동 병합·삭제는 하지 않고 정보 수정(학번 교정)·응답 삭제로
   사람이 정리. 학생 STEP 2 학번 칸에 "같은 학번으로 진행하면 갱신됨" 안내 1줄. 단위 검증(node) 6케이스 통과.
5. **결과지 "잡카페 상담 신청하기" → 상담 희망 기록 (이중장치)**: 설문 `counsel_wish`는 배점 항목(§2 Read-Only)이라
   바꾸지 않고 **별도 필드 `counsel_request:{at}`** 를 응답 문서에 저장(버튼 클릭 시 payload 변경 → 자동 재제출).
   관리자·상담사 화면은 `wantsCounsel()`(설문 희망 OR 결과지 신청)로 통일 — 명단 "상담" 열(희망/신청), 프리셋
   "상담희망", 연락 우선 큐, 상세 필터, KPI·학과별 현황, 상담 카드 배지, CSV(희망/희망(결과지 신청)/미희망),
   상세 모달에 신청 일시. 버튼은 클릭 후 "✓ 상담 신청됨"으로 바뀌고 미희망 학생에게는 안내 문구.
   ※ 응답값을 직접 바꾸면 결과지를 본 뒤 JAS(+5)·Level이 달라지는 문제가 있어 별도 필드로 설계 — 사용자 확인 필요.

### 09-02 "보고만" 항목 처리 (§5.3 기타 + §4.3 정보)
- **ADM-03** 계정 패널 첫 조회 중 "불러오는 중…" 표시 (빈 로컬 목록·"연결하지 못해" 문구가 먼저 뜨던 것)
- **ADM-04** 상세 필터에 **학년**(구버전 "1"~"3"은 본과 N학년으로 간주)·**JAS 범위** 추가 — 계측: 60~69 → 5명 전부 범위 내
- **ADM-05** 등록 폼 길이 상한 — 추천활동 활동명 100·설명 500(규칙과 동일, 검증 문구), 기관·계정 폼 maxLength
- **CNS-04** 삭제된 기관 수정 시 "수정됨 ✓" 위장 → `pushAgency` DELETED 반환, 화면 경고 + 로컬 목록 제거
- **CNS-10** 연계 "해당 없음"·취업 "미등록" 저장 시 기관·취업처 잔여 값 제거 + CSV도 단계 NONE이면 빈칸
- **CON-09** 기관 tombstone에 이름·구분 보존 + `mjc_ready_agencies_archived` 캐시 → 과거 기록 "(삭제된 기관) 이름"
- **RECO-04** 문서키≠recommendation_code 문서는 병합에서 제외하고 관리자 경고에 "(문서키≠코드)" 표시
- **STU-08** 삭제된 활동을 스냅샷에서 복원할 때 active:false → 05_추천활동 시트 "OFF"
- **ADM-02** 학번 검증 문구 — A1 수정으로 규칙·학생·관리자 동일 조건 확인, 추가 조치 없음
- 정보 4건: **A7** 저장소 차단 시 로그인 버튼 고착(storeSession 예외 → 안내 문구, try/finally) · **A15** 학과·학년 결측
  방어 · **C6** 회차 Enter의 IME 조합 중 무시 · **S11** 인앱 브라우저(카카오톡 등) 인쇄 안내 1줄
- 미처리(센터 판단): **SEC-08**(탭 닫아도 Firebase 세션 잔존 — 세션 지속성으로 바꾸면 A11/C8 타 탭 로그아웃 전파와
  상충) · **UI-03**(정렬 select 폭 — 재현 안 됨)

### 문서·정리
- CLAUDE.md §6.4 "히든 페이지" 서술을 실제 통제 방식(로그인 게이트+역할 라우팅+Rules)으로 정정, 링크북 재공개 경위 명시
  (주소 재비공개 여부는 여전히 센터 판단). §6.1·§7.1에 응답 삭제·중복 의심·counsel_request 반영.
- OneDrive 동기화 충돌 사본 3건 삭제 (`docs/WORKLOG-DESKTOP-PCI5E8P.md` 구버전, `scripts/make-linkbook-DESKTOP-PCI5E8P.cjs`
  CRLF만 다름, `tsconfig-DESKTOP-PCI5E8P.tsbuildinfo` 빌드 캐시) — 전부 대조 후 제거.

### 검증
validate 76 · 회귀 19 · strict build 통과. 브라우저(dev 5174, `firebase.ts` 임시 로컬 모드 → `git checkout` 원복):
360×780 결과지(진학·창업 Route) Level 제목+부제 표시 · 상담 신청 버튼 → sessionStorage 기록·버튼/문구 전환 ·
진단 척도 5라벨 전부 표시(가로 넘침 0) · 관리자 명단 중복 의심 칩(0, 비활성)·학년/JAS 필터 동작·응답 삭제 버튼(mock에선 잠금) ·
워크스페이스 상담 카드 배지·기관 폼 maxLength. 콘솔 에러 0. 중복 탐지 단위 검증 6케이스.

### 남은 것 (사용자)
- [ ] **라이브 확인**: 결과지 상담 신청 → Firestore `counsel_request` 저장·명단 "신청" 표시 / 마스터 응답 삭제(1건·필터 일괄) /
      중복 의심 표시 (실데이터) — 클라우드 전용 경로라 로컬에서는 미실측
- [ ] 9/7 시범 시작 전 테스트 응답 정리: 관리자 명단 → 실시일 기간 ~9/6 → "필터 결과 N건 삭제" (마스터)
- [ ] 상담 신청 이중장치의 설계 확인 — 별도 필드(현행) vs 설문값 변경(JAS·Level 변동)
- [ ] 이월: 미승인 계정 삭제 · §2.3 구조 항목(보유기간·학기 구분·PWA·준식별자) · SEC-08 · §12 체크리스트

---

## 2026-09-03 (4차) — 낮음 잔여 9건 수정 · 관리 화면 가독성 마무리

사용자 지시 "진행". 09-02 역할별 점검의 낮음 잔여 9건(UI·접근성·동시성 계열)을 전부 처리하고,
3차에서 미계측이던 로그인·계정·추천활동·다운로드 화면을 계측했다.

### 수정 (9건)
- **S5** 저장소 차단 환경: STEP 1 "다음"이 동의 저장 자가진단 후 alert 안내 (예전엔 설문 가드가 말없이 되돌림)
- **S6** 인쇄 시 미제출 흔적: 결과지 푸터에 FAIL·DENIED·NO_PROFILE 1줄 ("아직 학교에 제출되지 않았습니다")
- **S8** 기본정보 칸별 오류 표시: `fieldOk` 5종으로 분리, `input--error`·`grade-picker--error` — "다음" 이후에만
- **S9** 학생 화면 칩 터치 목표 44px (`.page .chip`, 관리자 밀도 칩과 분리)
- **A10/C12** 비밀번호: 클라우드(이메일) 계정은 모달이 "재설정 메일 보내기"만 표시(입력 무시 혼란 제거),
  로그인 화면에 "비밀번호를 잊으셨나요? 재설정 메일 받기" 신설 — `auth.sendResetMail` 공용
- **A11/C8** 로그아웃 시 학생 명단 메모리 캐시 무효화 + `onCloudSignedOut`(Firebase 세션이 있다가 사라진
  전이만 감지)으로 다른 탭·기기 로그아웃 시 이 탭도 로그인 화면으로 — 관리자·워크스페이스 공통
- **A14** 접근성: 명단 행 tabIndex/Enter·Space, 상세 모달 role=dialog + Esc 닫기(미저장 확인 포함),
  추천활동 ON/OFF role=switch·aria-checked, 비밀번호 모달 role=dialog
- **C4** 기관 수정 읽기→비교→쓰기: `Agency.updated_at` 신설, `pushAgency(agency, baseUpdatedAt)`가 원격과
  다르면 CONFLICT → 화면이 최신본을 받아 폼에 다시 채우고 안내 (§7.2.1-12)
- **C5** `pullShared`가 상담 기록 형태 검사(`isValidOutreach`) 후 깨진 문서는 건너뛰고 콘솔 경고 +
  `components/ErrorBoundary.tsx`로 라우트 전체 감싸 백지 대신 새로고침 안내 (§7.2.1-11)
- **C7** 워크스페이스 배너 초기값 null("연결 확인 중…") — 로그인 직후 "미연결" 경고 깜빡임 제거,
  등록부 조회만 실패해도 LOCAL로 뒤집히던 것 분리
- **C9** 삭제된 기관을 참조한 연계 기록: select에 "(삭제된 기관 — 다시 선택)" 명시 + 연계 완료 이후
  단계 저장 시 재선택 요구
- 가독성 추가: 관리자 폼 라벨(`.adv-filter__field`) 11.5→12.5px, 승인 대기 배지 대비 4.3→5.6

### 검증
validate 76 · 회귀 19 · strict build. 브라우저(로컬 모드 → 원복): 설문 빈 제출 시 4칸+학년 빨간 표시·칩 44px,
명단 행 Enter로 모달 열림·Esc 닫힘, ON/OFF aria-checked, 로그인·추천활동·다운로드·계정 화면 대비 위반 0.
클라우드 전용 경로(재설정 메일·타 탭 로그아웃·기관 CONFLICT)는 타입·코드 검토만 — 라이브 확인 항목.

### 남은 것
- [ ] 라이브 확인: 재설정 메일 수신, 다른 탭 로그아웃 전파, 기관 동시 수정 CONFLICT 안내
- [ ] Firestore 미승인 계정·테스트 데이터 정리 (사용자 UI 삭제), 라이브 마스터 실확인 4종
- [ ] 링크북 재공개 ↔ §6.4 히든 페이지 서술 판단, 구조 항목(보유기간 만료·학기 구분·PWA·응답 삭제·준식별자)

---

## 2026-09-03 (3차) — 가독성 점검 · CSS 47개 규칙 조정 (학생 화면 중심)

사용자 지시 "가독성을 확인하면서 점검해 보고 미흡한 점을 수정하자". 스크린샷(390px 세로 긴 뷰포트)과
계측 스크립트(글자 크기 <13px · WCAG 대비 <4.5 · 가로 넘침)로 학생 4화면 + 관리자 종합 현황 + 상담 카드
모달을 점검했다. 코드 변경은 `src/styles/global.css`만.

### 발견 → 수정
- **주황 글자 대비 1.9** (`--orange #f5a623` on 흰 배경): 히어로 문구·"READY" 제목 — 글자용 변수 신설
  `--orange-text #a35f00`(대비 5.0, 작은 글자) / `--orange-deep #b8700a`(대비 3.9, 큰 제목). 바·배경·그라데이션은
  브랜드 `--orange` 유지.
- **진단 5점 척도 라벨 9.9px**("전혀 그렇지 않다") → 11.2px + 진한 회색, 2줄 허용.
- **동의 고지표 12.8px**(수집 항목·목적·기간·관리 부서 — 법적 고지가 가장 작았다) → 14px, dt 폭 64→72px.
- 기본정보 라벨 12.8→13.8px, 보조 안내(휴대전화) 12.5→13.1px + 대비 4.0→5.8, 제도 설명 펼치기·선택지 안내
  12.5→13.4~13.8px, 동의 체크 문구 13.1→14.1px, 시범운영 안내 11.5→12.5px.
- 결과지: 추천활동 설명 12.8→13.8px, 영역 바 라벨 12.5→13.1px, 학생 표기 12.5→13.4px, 법적 문구 11.5→12.2px,
  JRS/CDS 라벨·담당 배지·게이지 단위 소폭 상향.
- 헤더 부제·STEP 표시·통계 라벨·필수/선택 표식·문항 번호 등 10.9~11.5px 계열 전부 11.8~12.5px로.
- 관리자: `.small` 11.5→12.2px(모달·힌트 전반), KPI 라벨·표 헤더·판정 근거 목록·Level 배지 소폭 상향,
  "해당 없음·미등록·무응답·비활성" 회색 배지 글자색 #7a7f8c→#5f6675(대비 3.4→5.8). **명단 표 밀도
  (`.admin-table--list`)는 09-02 사용자 요구대로 유지** — 한 화면 표시 우선.

### 검증
validate 76 · 회귀 19 · strict build 통과. 재계측: STEP 1·2·결과지 13px 미만 텍스트는 배지·캡션(11.5~12.2px)만
남고 대비 위반 0(관리자 링크는 의도적 흐림, 버튼 그라데이션은 계측 오탐). 진단 화면 라벨 2줄 wrap 정상,
가로 넘침 0. `firebase.ts` 임시 로컬 모드는 `git checkout` 원복, 브라우저 주입 데이터 정리.

### 남은 것
- [ ] 관리자 명단 표 자체(0.73~0.8rem 3단계)는 사용자 판단 — 넓히면 한 화면 표시와 상충
- [ ] 로그인·계정 관리·추천활동·다운로드 화면은 계측 미실시 (공용 `.small`·`.muted` 상향으로 간접 개선)

---

## 2026-09-03 (2차) — 점검 잔여 결함 13건 수정 (A1·C2 + 낮음 11건) · 규칙 게시 · 문서 정합

사용자 지시 "MD 확인 → 다음 작업 목록화 → 진행". 09-02 역할별 점검(`docs/점검보고서_2026-09-02_역할별_전과정.md`)
에서 미수정으로 남아 있던 중간 2건과 소규모 낮음 11건을 한 커밋으로 처리했다. 미승인(PENDING) 계정은
"그냥 지워도 된다"는 사용자 결정 — 단, 이 세션의 자동 실행 권한으로는 Firestore 조회·삭제 스크립트가
차단돼 아래 "남은 것"으로 넘김.

### 중간
- **A1 학번 교정 권한** (`StudentsPanel.tsx`, `responsesSource.ts`): 학번 변경은 상담 기록(`ready_outreach`)을
  함께 옮겨야 하는데 그 컬렉션은 상담사 계열만 쓸 수 있어, 담당자(행정)가 학번을 바꾸면 기록이 옛 학번에
  고아로 남고 "항상 실패" 경고가 났다. 담당자는 학번 칸 readOnly(성명·학과·학년·연락처만 교정),
  `updateStudentProfile(rec, patch, { canMoveId })`로 2중 가드. 학번 검증도 규칙과 동일 정규식으로.
- **C2 관리자 자기 계정 잠금** (`Accounts.tsx`, `firestore.rules`): 상담사/담당자 관리자가 자기 행을
  비활성·삭제할 수 있었다(라이브 실증). 본인 행은 "내 계정"으로 버튼 비노출 + 규칙 LEAD 분기에
  `request.auth.uid != uid`. **`firebase deploy --only firestore:rules` 게시 완료**.

### 낮음 (11건)
- S1 결과지 `profileOk`를 규칙과 동일 조건(학번 영숫자 4~20·성명 1~30)으로 — 검증 안 거친 자동저장값 재제출 시
  "네트워크 확인" 오안내·영구 실패 차단, NO_PROFILE 안내에 "설문으로 돌아가기" 버튼
- S2 응답 문서에 `consent:{at, terms_version}` 저장 (`sessionState.setConsent`가 최초 동의 시각 기록,
  `CONSENT_TERMS_VERSION="2026-09-03"`) — CSV 정보열람동의 컬럼에 일시 병기, 구버전은 "동의"
- S3 `saveResponseToCloud` 결과에 `DENIED`(permission-denied) 분리 — 결과지에 "학교 시스템이 제출을
  받지 않았습니다" 배너(설문 확인 버튼) — 네트워크 실패와 구분
- A4 관리자 연락처 교정 하이픈 정규화 — `sessionState.normalizePhone` 신설, 학생 설문 제출과 공용
- A5 CSV 파일명 날짜 `todayStr()`(KST) — 4곳 UTC slice 제거
- A8 `ready_staff` 결측 필드(created_at 등) 방어 — 목록 throw → 로컬 폴백·백지 방지
- A9 `saveRecoActivity(activity, editor, { isNew, baseUpdatedAt })` — 신규 여부를 baseUpdatedAt 결측으로
  추정하지 않음. updated_at 없는 오버라이드 수정·ON/OFF가 영구 CONFLICT 나던 문제
- A12 조회 중(LOADING)·실패(ERROR) 상태에서 다운로드 버튼 6종 잠금 + 안내 문구
- C3 성공 토스트 타이머가 뒤이은 FAIL 경고를 지우던 것 — `CounselRecord`·`AgencyManager` 타이머 교체
- C10 기관 등록 클라우드 실패 시 로컬 저장 생략(유령 기관 방지) + 폼 유지·재시도 안내
- C11 상담 카드 미저장 입력 시 모달 닫기 확인(`onDirtyChange` → `closeDetail` confirm)

### 문서
- CLAUDE.md: §6.1 정보 수정(학번 변경 상담사 계열 한정), §7.2 C2 규칙·consent 필드, §5·§8 검증 수 76/19,
  §8·§9 "서버 전송 없는 프로토타입" 폐기 전제 정정(DOC-02)
- 점검보고서_2026-09-02_역할별_전과정.md §4.1 처리 현황 추기 (상위 폴더 사본 동기화)

### 검증
validate **76** · 회귀 **19** · strict build 통과. 브라우저(dev 5174, `firebase.ts` 임시 로컬 모드 → `git checkout`
원복): 상담사 관리자 세션에서 계정 목록 본인 행 "내 계정" 표시·타 행 버튼 정상, created_at 빈 계정 렌더 OK,
상담 카드 메모 입력 후 바깥 클릭 → 확인창(취소 시 유지·확인 시 닫힘), 미입력 상태는 확인 없이 닫힘. 콘솔 에러 0.
규칙 컴파일·게시 성공.

### 남은 것
- [ ] **Firestore 미승인(PENDING) 계정 삭제** — 사용자 결정 "그냥 지워도 됨". 이 세션은 자동 실행 권한
  분류기가 Firestore 조회 스크립트를 차단해 미수행. 라이브 마스터 로그인 → 계정 관리 화면에서 승인 대기
  행 "삭제"가 가장 빠름(DELETED tombstone). 또는 `firebase firestore:delete ready_staff/<uid>`
- [ ] 테스트 응답·계정·기관 정리, 라이브 마스터 실확인 4종(이월)
- [ ] 낮음 잔여 9건: S5·S6·S8·S9·A10/C12·A11/C8·A14·C4·C5·C7·C9 (UI·접근성·동시성 계열)
- [ ] 링크북 잡카페 주소 재공개 ↔ CLAUDE.md §6.4 "히든 페이지" 서술 — 개정 또는 재비공개 센터 판단 (오전 세션 이월)

---


## 2026-09-03 — 모바일 반응형·설문 안내·계정 체계 수정 (라이브 결함 6건, 전부 배포 완료)

사용자가 실사용 중 발견한 문제를 순차 수정. **전 건 커밋·push·GitHub Pages 배포 + 라이브 실측 확인**.
09-02 점검에서 "확정했지만 미수정"으로 남아 있던 A2·A3가 실제 증상으로 터져 이번에 함께 해결됐다.

### 1. 모바일에서 성명·휴대전화 입력창이 짤림 (`51a0635`)
- **원인 3중**: ① `.grid-2`가 380px 이하에서만 1열로 접힘 — 요즘 폰은 390~430px라 2열 유지
  ② `.input`에 `width:100%`가 없어 input 고유 폭(약 180px)이 칸을 넘침
  ③ 그리드 칸(`.field`)에 `min-width:0`이 없어 축소 불가
- **수정**: 위 3가지 + 1열 전환 기준 380px→480px. 실측 390px 1열 전폭·가로 스크롤 0,
  데스크톱 2열 유지 확인. PC에서만 테스트해 놓친 전형적 케이스.

### 2. 진단 미응답 안내·바로가기 + 상담희망 문항 안내 (`6d6277f`)
- **진단(STEP 3)**: 미응답이 있으면 완료 버튼만 조용히 사라져 이유도 경로도 없었다 →
  하단 안내 카드 추가(남은 문항 수 + **건너뛴 문항 번호 칩으로 바로 이동** + 첫 미응답 이동 버튼).
  "건너뜀"은 마지막 응답 문항보다 앞의 미응답만 — 시작 직후 27개 칩이 뜨는 노이즈 방지.
- **설문 6번(상담 희망)**: 5번(gov_link) `info` 패턴을 확장해 `option_notice`(선택지별 안내) 신설.
  희망한다 → 컨설턴트가 먼저 연락 / 지금은 괜찮다 → 잡카페 방문 권유.
  문구는 `survey_items.json`에 두어 센터가 코드 없이 수정 가능(§4). **배점·선택지 무변경**.

### 3. 계정 신청이 관리자에게 전달되지 않음 (`218b3ba`) — 09-02 A2 확정
- **원인**: 가입 화면은 아이디를 "영문 소문자·숫자"로 안내하는데 `registerAccount`는 아이디에
  `@`가 있을 때만 공유 저장소(`ready_staff`)에 기록 → **안내대로 가입하면 신청자 브라우저
  localStorage에만 계정이 생겨** 마스터 화면에 영영 안 뜸. 신청자는 승인을 기다리고 마스터는
  신청이 없다고 본다.
- **수정**: 클라우드 모드에서 비이메일 신청 차단(라벨·type·안내 문구도 이메일로) +
  **마스터 직접 등록**(`createStaffCloud`/`createLocalAccount`, "＋ 계정 직접 등록") 신설.
  Auth 사용자 생성은 격리 signup 인스턴스, staff 문서는 마스터 세션으로 기록
  (Rules상 `status=ACTIVE` 생성은 `isMaster()`만).

### 4. 담당자 신청이 상담사 승인 대기열로 접수됨 (`af351d6`)
- **원인**: `cloudLogin`의 **고아 계정 복구**가 역할을 `COUNSELOR`로 하드코딩. 가입 시 Auth
  사용자는 생겼으나 신청 문서 쓰기가 실패(고아)하면, 로그인 시 무조건 상담사로 접수됐다.
- **수정**: ① 고아 방지 — `setDoc` 전 `authReadyFor(signupAuth)`로 토큰 반영 대기(대기 없이 쓰면
  비인증 요청으로 나가 거부) ② 역할 보존 — 신청 구분을 로컬에 기억(`rememberRequestedRole`)해
  복구가 사용 ③ **구분 이동 버튼**(담당자↔상담사) — 이미 잘못 접수된 계정을 삭제·재신청 없이 이동
  ④ 처리 결과 메시지 성공/실패 구분(기존엔 성공도 오류 스타일).

### 5. 담당자 관리자(ADMIN_LEAD) 역할 신설 (`5512104`) — 09-02 A3 관련
- 상담사 관리자와 **대칭 구조**. 마스터가 임명하면 행정 페이지에 "담당자 계정 관리" 노출.
- `leadCounterpart()`로 임명·해제 일반화(담당자↔담당자 관리자 / 상담사↔상담사 관리자 공용).
- **권한 경계**: 담당자 관리자는 승인·중지·삭제만. 직접 등록·구분 이동·관리자 임명은 마스터 전용,
  상담사 워크스페이스는 계속 차단(`isCounselSide` 미포함 → `#/admin` 리다이렉트, §6.4 유지).
- **firestore.rules 개정·게시**: `isAdminLeadOrMaster()` 추가, `ready_staff` update/delete를
  담당자 관리자에게 `ADMIN`/`ADMIN_LEAD` 문서에 한해 허용하되 **role 불변 조건**으로 역할 변경은
  마스터 전용 유지. `firebase deploy --only firestore:rules` 컴파일·게시 성공.

### 6. 개인정보 고지에 관리 부서 명시 (`2fe449f`, `956e37a`)
- "응답 전 안내" 고지표에 **관리 부서 = 학생지원처 취·창업팀** 추가(사용자 확정).
  모바일에서 라벨이 "관리 부/서"로 끊기던 것도 `word-break: keep-all`로 수정.
- CLAUDE.md에 기록: §1 주관(AI융합진로지원센터)은 **사업 주관 조직**, 개인정보 **처리 부서는
  취·창업팀** — 서로 다른 값이며 화면 표기를 "불일치"로 보고 되돌리지 말 것.

### 7. 링크북 — Word 다운로드 + 잡카페 워크스페이스 주소 재공개 (`50305a5`)
- **Word 파일**: `public/MJC-READY-linkbook.docx` 신설(docx-js 생성). 링크 5종의 대상·용도/주소/
  유의사항 표 + 배포 시 유의사항(학생 배포는 1종만·내부 주소 외부 배포 금지·실명 시스템 개인정보 취급).
  `public/`은 Vite가 `dist/`로 복사하므로 Pages에서 그대로 서빙된다.
  파일명은 **ASCII(`MJC-READY-linkbook.docx`)로 두고 `download` 속성으로 한글명 저장** —
  한글 파일명이 URL에 들어갈 때의 인코딩 문제를 피하면서 사용자에게는 한글로 저장되게 함.
  .docx는 한글(HWP)에서도 열리므로 HWP 별도 생성은 하지 않음.
- links.html 머리말 아래 "⬇ Word 파일 내려받기" 버튼(인쇄 시 `display:none`).
- **잡카페 워크스페이스 주소 재공개** — 2026-09-02에 "공개 배포 페이지에서 히든 주소 제거"로
  일부러 뺐던 것을 **사용자 지시로 되돌림**. 다른 카드와 동일하게 주소·복사 버튼·QR 추가.
  ⚠ **되돌린 배경을 알고 판단할 것**: links.html은 noindex일 뿐 주소를 아는 사람은 누구나 열 수 있는
  공개 페이지다. 워크스페이스 자체는 로그인 게이트가 유지되므로 주소 노출이 접근 권한을 주지는
  않으나, **링크북 문서·페이지의 배포 경로를 내부로 한정**해야 한다. 카드에 "학생·외부 배포 금지" 명시.
  (CLAUDE.md §6.4의 "어떤 화면에도 링크하지 않는 히든 페이지" 서술과는 이제 어긋나므로,
  §6.4를 개정하거나 다시 감출지는 센터 판단 필요 — **미결**)
- 실측: 라이브에서 다운로드 200·10,655B·ZIP 시그니처(PK) 확인, 상담사 주소·QR 렌더 확인,
  QR 인코딩은 동일 입력 재생성 대조로 검증.

### 검증·잔여
- 매 건 `validate:data 76` · `test 19` · `build` 통과, 브라우저 실측 + 라이브 번들 확인.
  테스트로 만든 로컬 계정·세션은 그때마다 정리.
- **잔여**: ① 기존 비이메일 아이디 신청자는 그 브라우저에만 있어 복구 불가 — 이메일 재신청 또는
  직접 등록으로 처리 ② 계정 관리 화면에 "☁ 공유 저장소 연결됨"이 뜨는지 확인 필요(로컬 표시면
  별개 문제) ③ 09-02 A1·C2(학번 교정 시 상담 기록 고아, 상담사 관리자 자기 계정 비활성) 미수정
  ④ 링크북에 잡카페 주소를 재공개하면서 CLAUDE.md §6.4의 "히든 페이지" 서술과 어긋남 —
  §6.4 개정 또는 재비공개 여부 센터 판단 필요.
- 링크북 Word는 주소가 바뀌면 **수동 재생성** 대상 → 생성 스크립트를 `scripts/make-linkbook.cjs`로
  저장소에 보존했다(실행법은 파일 상단 주석). 프로젝트가 ESM이라 확장자는 `.cjs`,
  `docx`는 문서 생성 전용이므로 `npm i --no-save docx`로 그때만 설치 — package.json 무변경(CI 영향 없음).

---

## 2026-09-02 (저녁) — 역할별 전 과정 점검 (학생·관리자·상담사, 저장까지)

사용자 요구: MD(오전 보고서) 확인 후 세 역할 입장에서 처음부터 저장까지 전부 점검.
**상세 보고서: `docs/점검보고서_2026-09-02_역할별_전과정.md`** (코드 수정 없음 — 점검만)

- **라이브**: 학생 전 과정 실제 제출 → REST로 `ready_responses/2026-2_20990003` 확인, 성명 수정 후
  자동 재제출 갱신 확인. 관리자·상담사는 `firebase.ts` 임시 로컬 모드로 전 화면 실동(필터·CSV 5종·
  추천활동 등록/OFF/삭제·상담 카드 5종 저장·기관 등록·역할 게이트) 후 `git checkout` 원복·재빌드.
- **코드 정독 3관점 → 38건 → 반박 검증**: 확정 28(중간 4·낮음 20·정보 4) / 기각 5 / 중복 통합 5.
- **중간 4건**: A1 담당자 학번 교정 시 상담 기록 고아(권한 없어 항상 실패) · A2 비이메일 아이디
  가입이 로컬 계정 데드엔드 · A3 역할 전환 불가+DELETED 복구 UI 없음 · C2 상담사 관리자 자기
  계정 비활성·삭제 가능(라이브 실증). 실제 외부 상담사 3명이 승인된 상태라 파일럿 전 수정 권장.
- 정리 필요: 점검용 응답 `2026-2_20990003` 삭제(기존 테스트 응답과 함께).

---

## 2026-09-02 — 명단 한 화면 표시 + 전면 병렬 점검(100건 조사·34건 확정) → 확정 결함 수정

사용자 요구: ① 상담사 명단이 한 화면에 보이도록 화면 크기 조정(글자 깨짐 없이) ② 시스템 전반 점검
③ 100회 규모 병렬 점검 ④ 요약을 MD로 저장.
**상세 보고서: `docs/점검보고서_2026-09-02.md`** (이 항목은 요점만)

### 1) 명단 레이아웃 (실측 기반)

원인: 15열 명단의 자연 폭 1265px > 가용 1210px(1440 모니터) — `.admin` max-width 1440 고정이 원인.
조치: max-width 1760 · 사이드바 210 · 본문 여백 18 + 명단 전용 밀도 `.admin-table--list`(셀 여백·
배지만 축소, **글자 축약·줄바꿈 없음**) 3단계 + `.table-wrap--list` + 메뉴 접기(`useSidebarFold`).
실측: 1280·1366·1440 및 접힘 상태 **전부 가로 스크롤 0**, 행 높이 43→37px(한 화면 학생 수 +20%).
밀도 규칙은 명단 표에만 적용 — 다른 표 4종 무영향(선택자 범위 확인).

### 2) 전면 점검 (에이전트 104개)

10개 관점 병렬 조사 → 건별 **적대적 반박 검증** → 완결성 점검. 조사 100건 → **확정 34건 /
반박 기각 59건** + 완결성 7건. 기각 사유는 기존 방어 존재·도달 불가·의도 명시·심각도 과장.

### 3) 수정 완료 (17건)

- **[치명] STU-01 무한 자동 재제출 루프** — 오늘 추천 Master를 붙이며 만든 회귀.
  `getSurvey/getDiag`가 매 렌더 새 객체 → analysis 참조 흔들림 → 제출 effect 매 렌더 재실행 →
  실패 시 FAIL↔SAVING 무한 반복(학생 화면은 "제출 중…" 영구 표시). 세션 읽기를 마운트 1회로
  고정 + dep를 불리언으로 교체.
- **[높음] CNS-03/CON-05** 학번 교정 시 대상 학번에 상담 기록이 있으면 원본을 복사 없이 삭제 →
  회차 합산·재번호 **병합** 후 삭제, 이동/병합/실패 구분 안내.
- **[높음] MISS-01** 시드 활동 전량 2026-12-31 만료 → validate-data에 "오늘 노출 가능"·
  "60일 뒤 잔존" 검증 추가(11월부터 CI가 먼저 실패). 검증 68→**76건**.
- **[중간]** MISS-02 공개 links.html의 히든 워크스페이스 URL·QR 제거 / SEC-02 로그인 후 조회
  (`useStudents(enabled)`) / STU-03 빈 프로필 영구 403 차단 / RECO-02 삭제 충돌 검사·archived
  원격 최신본 / CON-07 tombstone 부활 차단 / CNS-06 KST 시각 표시(`localDateTimeStr`).
- **[낮음]** RECO-03 "최신 내용 불러오기" / MISS-06 라우터 catch-all / ENG-06 템플릿 필드 검증 /
  UI-05 로그인 로고 / 연타 방지·숫자 정렬·만료일 검증·경고 초기화.
- **문서**: CLAUDE.md §7.1 실제 Collection 5종, §5·§11 상태 정정, **§7.2.1 원칙 11~14 신설**.

### 4) 보고만 (24건 — §5 of 보고서)

학기 구분(ADM-01/MISS-07) · **개인정보 보유기간 만료 수단 부재(MISS-03)** · PWA 미구현(MISS-04) ·
응답 삭제 기능 부재(STU-04) · 연구용 준식별자(STU-07) · 상담사 동시 편집 동기화(CNS-01/02, CON-01/02/03,
MISS-05) 등. 구조 변경·센터 판단이 선행되어야 하는 항목.

### 검증

validate **76** · 회귀 **19** · strict build · 브라우저 실측(명단 폭 4종·메뉴 접기·전 화면 렌더,
콘솔 0). 커밋 603f5d6 · efad215 · e34a3c7 push 완료.

---

## 2026-09-01 (7차) — 🔓 firestore.rules 게시 완료 (누적 대기분 일괄) + 실측 검증 8종

사용자 지시 "게시하도록하자". Firebase CLI가 monatory82@gmail.com으로 로그인돼 있어
콘솔 수동 게시 대신 **CLI 게시로 전환**: `firebase.json` 추가(rules 경로) →
`firebase deploy --only firestore:rules --project mjc-ready-pwa` 성공.
게시 내용 = 8/31 update 완화(재응시)·delete 확장(학번 교정) + 9/1 validResponse 강화(F02)
+ 9/1 ready_reco_master(추천활동 관리) — 3세션 누적 대기분 일괄 반영.

### REST 실측 검증 (전부 기대값 일치)

1. 비인증 응답 읽기 403 ✅ 2. 비인증 추천 Master 403 ✅ 3. 익명 토큰 발급 OK
4. **익명 인증 추천 Master 읽기 200** ✅ (학생 결과지 경로 개통)
5. 익명의 실명 응답 읽기 403 ✅ 6. 익명의 상담 기록 403 ✅
7. **다른 익명 uid로 기존 응답 갱신 200** ✅ — 8/31 핵심 수정 라이브 반영
   (타 기기·브라우저 재응시 갱신, 학번 교정 삭제 경로 개통)
8. **학번 위조(docId≠학번) 쓰기 403** ✅ — 9/1 validResponse 강화 동작
(검증 중 삽질 1건: bash 예약변수 UID에 대입해 auth_uid가 "197609"로 들어가 403 —
 변수명 ANON_UID로 바꿔 재검증. 테스트는 삭제 예정 문서 2026-2_20999999만 사용)

### 남은 것

- [ ] 라이브 마스터 로그인 실확인(학생 정보 수정·상담 카드·통합 CSV·추천활동 등록 1건)
- [ ] Firestore 테스트 데이터 정리(20999999 포함) + 이월 항목들

---

## 2026-09-01 (6차) — 추천활동 관리: 등록·수정·삭제·ON/OFF 구현 (계획서 §3.1-⑤)

사용자 질의 "관리자 페이지에서 추천활동 직접 등록·변경 가능한가?" → 예정 기능이라 구현.
기존은 mock ON/OFF(화면 한정)뿐이었음.

- **저장소** `src/lib/recoMaster.ts`: 시드(recommendation_master.json) + Firestore
  `ready_reco_master` 오버라이드 병합(문서키=recommendation_code). 삭제=tombstone(§7.2.1-9),
  저장 실패는 결과 반환+경고 표시(§7.2.1-2), pull은 원격이 진실. LOCAL 모드(설정 전)는
  브라우저 캐시만.
- **관리자 UI** `RecoMasterPanel.tsx`(#/admin 추천활동 관리 교체): ＋활동 등록 / ✏수정(코드
  수정 불가 — 판정·이력 키) / 삭제(시드 활동은 OFF 권장 경고) / ON/OFF 클라우드 반영.
  칩 UI(담당·우선순위 1~5·Level·취약영역 6+범용), 유효성 검증(코드 형식·중복·기간·필수),
  상태 배너(CLOUD/LOCAL/FAIL).
- **학생 결과지 연동**: Result가 익명(student) 세션으로 최신 Master를 조회(6초 타임아웃) 후
  추천·제출 스냅샷 계산 — 실패 시 시드+캐시 폴백(결과 표시 안 막음). 조회 대기 중
  "결과를 준비하고 있어요" 표시, 이중 제출 방지(master 확정 후 payload 계산).
- **관리자 명단**: restoreRecs/재계산이 병합 Master 사용 — 등록 활동이 명단·모달에도 반영.
  mock 40명 생성은 시드 고정 유지(결정론).
- **firestore.rules**: `ready_reco_master` read=인증(익명 포함— 비개인 데이터), write=승인
  교직원+문서키·코드 일치. ⚠ **콘솔 게시 필요 (기존 대기분과 일괄)**
- 검증: validate 68 · 회귀 19 · strict build. 브라우저 실동(#/admin) — 폼 검증(Level 미선택
  차단), 비인증 등록 시도 → "저장 실패" 경고(로컬 오염 없음), 수정 폼 프리필·코드 잠금,
  FAIL 배너, 시드 10건 표. 앱 콘솔 에러 0(예상된 403 리소스 로그만).
- 남은 확인: 규칙 게시 후 라이브에서 마스터 로그인 → 등록·ON/OFF → 학생 결과지 반영 실측.

---

## 2026-09-01 (5차) — 링크북 게시 + 부서 전달용 링크 안내문(docx)

사용자 요구: "링크를 한곳에서 관리 + 각 부서 전달용 워드".

1. **링크북 정적 페이지** `public/links.html` → 라이브 `…/mjc-jobready-pwa/links.html`:
   전 링크 5종(학생·관리자·워크스페이스·GitHub·Firebase 콘솔) + QR 코드(흰 타일 고정·인쇄 대응).
   앱 어디에도 링크하지 않는 관리용 주소, robots noindex. Claude 아티팩트 버전과 동일 내용.
2. **부서 전달용 안내문** `docs/MJC-READY_링크안내_v0.1.docx` (센터 표준 서식·개조식):
   시스템 개요 / 학생용 검사 링크+QR / 교직원용 관리자 페이지+QR / 유의사항.
   **§6.4 원칙에 따라 상담사 워크스페이스 링크는 미포함** (부서·행정 노출 금지) —
   links.html 주소 자체도 워크스페이스 링크가 담겨 있어 문서에 비기재.
3. QR 생성: `qrcode`(npm) 로컬 생성 — SVG(웹)·PNG(docx). Word COM으로 PDF/HTML 변환 검수.

---

## 2026-09-01 (4차) — STEP 2 기본 정보 레이아웃 정리 (사용자 요구)

사용자 지적: 학과와 휴대전화 사이가 비정상적으로 벌어짐. 원인 = 2열 그리드에서 학년(과정 구분)
3줄 블록이 학과와 같은 행에 있어 행 높이를 끌어올리고 휴대전화가 아래로 밀림.

- **필드 재배치**: 학번|성명 / 학과|휴대전화 짝 유지, **학년(과정 구분)은 전체 폭 행으로 분리**
  (`.field--full` — grid-column: 1 / -1)
- **과정명 인라인 복귀**: 전체 폭이 되면서 `grade-picker__track`을 flex-basis 100%(칩 위 한 줄)
  → **고정 폭 92px 인라인**으로 — 행 간 칩 시작점 정렬, 세로 3줄로 압축
- 검증: validate 68 · 회귀 19 · build 통과. 브라우저 실동 — 데스크톱 2열 균형·공백 제거,
  모바일 375px 과정명+칩 한 줄 유지·졸업 연도 입력 자연 줄바꿈, 콘솔 에러 0

---

## 2026-09-01 (3차) — 9/1 감사 수정분 배포 완료

전 세션 미배포 상태였던 9/1 작업 전체(감사 96건 수정 + 학년 표기·통합 다운로드·잡카페 위치)를
커밋(6157f11) → push → Actions 배포 성공. 사전 게이트 재확인: validate **68** · 회귀 **19** ·
strict build 통과. 라이브 확인: 새 번들 반영(noindex 포함), 학생 STEP 1 → STEP 2 스모크 —
동의 문구(클라우드 모드 고지)·학년 3트랙 라벨(본과정/전공심화과정/졸업) 정상 렌더, 콘솔 에러 0.
(실제 제출은 테스트 데이터가 남으므로 스모크에서 제외 — 기존 테스트 데이터 정리 항목과 함께 관리)

### 잔여 (사용자 수동 — 자동화 불가 경로)

- [ ] **firestore.rules 콘솔 게시** (최우선): Firebase 콘솔 → mjc-ready-pwa → Firestore → 규칙 탭에
  로컬 `firestore.rules` 전체 붙여넣기 → 게시. 8/31 완화분+9/1 강화분 누적 — 게시 전까지
  타 기기 재응시 갱신·학번 교정·validResponse 신규 검증 미동작
- [ ] 라이브 마스터 로그인 실확인: 학생 정보 수정 1회·상담 카드 저장(공유 반영)·통합 CSV
- [ ] 추천활동 활성기간·목록 센터 확정 (전부 2026-12-31 만료 예정 — §12-08)
- [ ] 기존 이월: 테스트 데이터 정리, 상담사 계정 승인, 마스터 로컬 예비 해시 제거 결정,
  §12 잔여, PWA manifest·OG 카드, 파일럿 설계, Firestore 정기 백업 절차

---

## 2026-09-01 (세션 종료) — 하루 총정리

사용자 수정 요구 3건 → 울트라코드 전면 감사(96건) → 확정 결함 전면 수정까지 완주한 세션.
**전부 로컬 작업 완료 상태 — 커밋·push·규칙 게시가 안 된 미배포 상태로 종료** (아래 다음 세션 참조).

### 오늘 완성된 것 (시간순)

1. **학년 과정 구분 정리**: 본과정/전공심화과정/졸업 표기 + 과정명 상단 배치, gradeLabel 통일
   (코드값 무변경 — 데이터 호환)
2. **통합 다운로드**: 관리자 데이터 다운로드를 단일 시트(1 학생=1행) 통합 CSV 기본으로 개편
   (실명/익명 2종, 개별 5시트는 접힘 메뉴로 유지). 잡카페 위치 "본관 1층" 수정
3. **울트라코드 전면 감사**: 6관점(동시접속·학생 흐름·관리자·상담사·통계·엔진) 13에이전트 —
   발견 96건, 반박 검증 사실상 전건 CONFIRMED
4. **확정 결함 전면 수정**: 상담 카드 트랜잭션 병합(동시 편집 유실 제거), Rules validResponse
   강화, R번호 결정론, mock 폴백 제거, KST 날짜 통일, 학기 자동 산출, CSV 수식 주입 방어,
   학번 교정 원자화+상담기록 동반 이동, 제출 타임아웃, 완주 가드 등 — 상세는 아래 (2차) 항목.
   원칙 10개를 CLAUDE.md §7.2.1로 명문화
5. **동시접속 수용량 분석** (사용자 질의): 작성은 무제한(정적+로컬), 제출도 수백 명 동시 무방.
   병목은 ①교내 단일 IP의 익명 계정 생성 제한(시간당 ~100 수준, 첫 접속자만 해당 — 실패 시
   재시도 배너로 유실 없음) ②Spark 일일 쓰기 2만(하루 수천 명 여유) ③관리자 읽기 5만/일
   (응답 1천 건 시 하루 ~50회 조회). 파일럿은 학과·교시 분산 실시로 충분, 전학과 확대 시
   Blaze 전환+조회 최적화

### 세션 종료 시점 상태

- 게이트 전부 통과: 데이터 검증 **68건** · 회귀 테스트 **19건** · strict build · 브라우저 실동(콘솔 0)
- ⚠ 오늘 작업 전체가 **커밋·push 안 된 로컬 상태** — 라이브는 8/31 버전 그대로
- ⚠ firestore.rules **콘솔 미게시** (8/31 완화분 + 9/1 강화분 누적 대기)

### 다음 세션 — **배포부터 진행 (사용자 지시 2026-09-01)**

1. [ ] **커밋 → push → Actions 배포 확인** (validate 68 → 테스트 19 → 빌드 → Pages) —
   새 번들 라이브 반영·학생 흐름 1회 스모크
2. [ ] **firestore.rules 콘솔 게시** (push와 한 세트 — 게시 전까지 재응시 갱신·학번 교정·
   신규 검증 미동작)
3. [ ] 라이브 실확인: 마스터 로그인 → 학생 정보 수정 1회, 상담 카드 저장(공유 반영), 통합 CSV
4. [ ] 추천활동 활성기간·목록 센터 확정 (전부 2026-12-31 만료 — 방치 시 2027-01-01 추천 소멸)
5. [ ] 기존 이월 항목: Firestore 테스트 데이터 정리, 상담사 신청 계정 승인, 마스터 로컬 예비
   해시 제거 결정, §12 체크리스트 잔여, PWA manifest·OG 카드, 파일럿 운영 설계,
   Firestore 정기 백업(export) 절차 수립

---

## 2026-09-01 (2차) — 울트라코드 전면 감사 (96건) → 확정 결함 전면 수정

**감사**: 6관점(동시접속·학생 흐름·관리자·상담사·통계·엔진) 병렬 감사 + 관점별 반박 검증 +
완결성 점검 = 워크플로 13에이전트. 발견 88건 + 완결성 8건, 반박 검증에서 사실상 전건 CONFIRMED
(과장 4건만 하향). 이후 사용자 지시("검증에서 문제가 된 부분 모두 수정")로 일괄 수정.

### 수정 완료 (심각도순 — CLAUDE.md §7.2.1 원칙으로 명문화)

- **[C] 상담 카드 동시 편집 유실**(F01/C4-01): setDoc 전체 덮어쓰기 → `pushOutreachMerged`
  트랜잭션 병합(원격 최신 + 변경 필드만, 회차는 add/removeSeq 연산). pull은 원격 우선.
- **[C] Rules 임의 문서 쓰기**(F02): validResponse에 docId=="{학기}_{학번}" 일치·학번 영숫자
  4~20·학기 형식·survey/diag map 강제. ⚠ **콘솔 재게시 필요 (기존 대기분과 함께)**
- **[C] 연구용 R번호 비결정론**(P5-01 계열 4건): mock 인덱스 → 추출 세트 학번 오름차순 부여
  (시트 간 join 보장, 행 순서 재식별 완화)
- **[H] 공유 저장 실패 은닉**(C4-02·F04): 저장 결과 반환 + 실패 경고 표시(상담 카드·기관·계정)
- **[H] 학번 교정**(F05/F06): runTransaction 원자화 + ready_outreach·로컬 캐시 동반 이동
- **[H] mock 폴백 위장**(F07/ENG-02): 클라우드 모드에서 mock 금지 — 0건/ERROR 배너+새로고침,
  스키마 불일치는 skipped 건수 표시
- **[H] 제출 고착**(S2-01): saveResponse 15초 타임아웃 → FAIL 배너. 학번 trim 일치(ENG-06),
  버전·추천 스냅샷 저장(ENG-09/P3-11)
- **[H] 클라이언트↔Rules 검증 불일치**(S2-02/F12): 학번 영숫자 4~20 사전 검증 + 안내 문구
- **[H] UTC 날짜**(ENG-05 계열 4건): src/lib/dates.ts(localDateStr/todayStr) 신설 — 실시일·기간
  필터·발급일·회차 날짜·CSV 전부 KST 기준
- **[H] SEMESTER 하드코딩**(P5-04): computeSemester() 날짜 기반 자동 산출
- **[H] CSV 수식 주입**(P5-02): csvCell에 =,+,-,@,탭 선행 아포스트로피 + \r 이스케이프(P5-12)
- **[H] counsel_status 유출**(P5-03): OUTREACH_KEYS에 추가 — 담당자·연구용 CSV에서 제거
- **[H] 추천 리졸버 ANY 자기잠식**(ENG-08): 직접 매칭 판정에서 ANY 제외 — 회귀 테스트 고정
- **[M] 결과지 완주 가드**(S2-03/ENG-07): 진단 전 문항 완주 검사, 미완주 직접 진입 시
  #/diagnostic 복귀 (부분 응답 자동 제출 차단) — 브라우저 실동 확인
- **[M] STEP2 입력 즉시 저장**(S2-05) · 자동진행 타이머 취소+완료 후 이동(S2-07/08) ·
  졸업연도 2000~내년(S2-10) · 전화 하이픈 정규화(P5-11) · 375px 척도 양끝 라벨 유지(S2-06)
- **[M] 계정 체계**: 삭제 tombstone(DELETED — 부활 차단 P3-12/C4-10), 가입·로그인 네트워크
  폴백 금지(P3-08/F11), 액션 실패 표시+명시적 목표 상태(P3-10/F16), 로그아웃 시 상담 캐시
  제거(F15), 구버전 무전화 응답 교정 허용(P3-09)
- **[M] 워크스페이스**: 리마운트 제거+변경 통지 구독(C4-07/C4-11), 배너 정확화(C4-06),
  마스터 #/counsel 로그인 유지(C4-14), 연계 완료 시 기관 필수(C4-15), 기관 tombstone·
  "(삭제된 기관)" 표시(F14/C4-08), tel 빈값 표시(C4-16)
- **[M] CSV 시트 정비**: 04 domain 실제 코드+영역평균+진단버전(P5-07), 02 결측 빈칸 통일
  (P5-09), 03/05 학기 컬럼(P5-08), 명단 key 학기 포함(P3-14), KPI 조건 level_rules 주입(P3-13)
- **[기타]** deploy.yml npm ci 강제+구 문구 정정(EXT-02/05), index.html noindex(EXT-07),
  validate-data 15건 보강(53→68), 회귀 테스트 4건 추가(15→19 — JAS=70 동치·결측 결정론·
  기간 경계·ANY 회귀)

### 검증

validate **68건** · 회귀 **19건** · strict build 통과. 브라우저 실동: STEP2 렌더·콘솔 0,
부분 진단 #/result 진입 → #/diagnostic 복귀 확인, 관리자 미인증 → "조회 실패" 배너+0명+새로고침
(mock 위장 제거 확인).

### 수정하지 않고 보고만 (구조·운영·센터 판단 영역)

- F03 잔여: 타 학생 학번 오기입 덮어쓰기의 완전 차단 — history 보존·학생 인증(§12-11) 필요.
  Rules 강화·형식 검증으로 완화만 됨
- F09/F10: 전체 컬렉션 풀스캔·리스너 부재 — 파일럿(수백 명) 규모는 무방, 전교생 확대 전
  페이지네이션·캐시 TTL 설계 필요 (Spark 읽기 50K/일)
- F17: saved_at 클라이언트 시계 — serverTimestamp 전환은 스키마 변경 동반, 파일럿 후 검토
- ENG-01: 엔진의 결측 무음 처리 — Read-Only(§2), rules map 검증+완주 가드로 유입 차단으로 갈음
- ENG-04: 추천활동 시드 활성기간 전부 2026-12-31 종료 — **센터가 기간·활동 확정 필요(§12-08)**,
  방치 시 2027-01-01부터 추천 전면 소멸
- P5-13/ENG-09 잔여: 관리자 화면은 여전히 현행 규칙으로 재판정(결정론 §3.1-② 의도) —
  버전 개정 시 과거 학기 소급 변경은 저장된 버전 필드로 식별 가능해짐
- EXT-01: PWA manifest·service worker 부재 (기존 잔여 작업), EXT-03: 마스터 해시 공개 저장소
  노출(기지 — 로컬 예비 해시 제거 검토 중), EXT-04: **백업·복구 절차 부재** — Firestore 정기
  export 운영 절차 수립 권장, EXT-08: OneDrive 내 .git 동기화 충돌 위험

### 사용자 필수 조치

- [ ] **firestore.rules 콘솔 재게시** — 8/31 완화분 + 9/1 validResponse 강화분 일괄 (최우선)
- [ ] 추천활동 활성기간·목록 확정 (2026-12-31 일괄 만료 예정 — §12-08)
- [ ] 배포(push) — 이번 수정 전부 미배포 상태

---

## 2026-09-01 — 학년 과정 구분 표기 정리 · 통합 다운로드 · 잡카페 위치 수정 (사용자 요구 3건)

1. **학년 과정 구분 표기**: STEP 2 선택 UI 트랙 라벨을 **본과정 / 전공심화과정 / 졸업**으로 변경
   + 과정명을 칩 위 한 줄로 배치(`grade-picker__track` flex-basis 100% — 긴 라벨 줄바꿈 해소).
   표시 라벨 `gradeLabel()`은 "본과정 N학년"·"전공심화 N학년"으로 통일(명단·모달·CSV·정보 수정
   폼 공통). **코드값("본과1"·"심화1"·"졸업YYYY")과 GRADE_PATTERN은 무변경** — 저장 데이터 호환.
2. **통합 다운로드 (관리자 데이터 다운로드 개편)**: 시트별 5종 분할 다운로드 → **단일 시트
   (1 학생 = 1행) 통합 CSV가 기본**. 컬럼 = 01_학생상태 전체 + 검사 실시일 + (01에 없는) 잔여
   설문 문항(취업준비 의지·학교지원·연구 문항 등 동적 주입) + 진단 6영역 점수 + 보완영역 +
   자격증(보유/준비 중/목표 상태별 요약) + 추천활동. 운영(실명)/연구(익명 R번호·성명 제거) 2종,
   상담사 전용 컬럼은 기본 제외(§6.4 유지). `csvExport.ts`: 01 행 매핑 `studentStateRow()` 공용화
   + `anonymize()` 헬퍼 분리 + `exportIntegrated`/`exportIntegratedForResearch`. 기존 5개 Sheet
   (Long Format)는 "개별 시트 다운로드" 접힘 영역으로 이동(통계분석용 유지). CLAUDE.md §6.3 갱신.
3. **잡카페 위치**: 결과지 상담 CTA 문구 "잡카페(학생회관)" → **"잡카페(본관 1층)"**.

검증: validate 53 · 회귀 15 · strict build 통과. 브라우저 실동(dev 5174) — STEP 2 과정 구분
3행 렌더 확인, 통합 CSV 실명 40컬럼·40행(blob 가로채기 검증: 연락상태 등 아웃리치 컬럼 부재,
실시일·영역점수·자격 요약·추천활동 포함), 연구용 38컬럼·R001 치환·성명/연락처 제거, 콘솔 에러 0.

---

## 2026-08-31 (세션 종료) — 하루 총정리

저장 신뢰성부터 운영 편의까지 한 번에 정리한 세션. **전부 push·라이브 배포 완료** (6545e02
+ 이 문서 커밋, Actions 검증 53→테스트 15→빌드 통과, 새 번들 반영 확인).

### 오늘 완성된 것 (시간순)

1. **학생 응답 저장 신뢰성 전면 수정**: 재응시 403 실측·규칙 수정, Auth 세션 3분리(student/
   signup 보조 앱), 제출 실패 배너+재시도, 스냅샷 기반 재제출, 가입 시 마스터 로그아웃 버그
   수정, 고아 계정 자동 복구
2. **학년 3트랙 세분화**(본과 1~3/심화 1~2/졸업+연도) + gradeLabel 표시 통일
3. **명단 개선**: 실시일 컬럼, 정렬 5종(최근 검사순 기본), 표 셀 줄바꿈 정리(nowrap+가로 스크롤)
4. **F5 깜빡임 제거**(mock 선표시 → LOADING 상태) + KPI NaN 방어
5. **기간별 데이터 취합**: 관리자 집계 기간(현황·다운로드 공통) + 명단 실시일 기간 필터
6. **배포 전 전체 점검 ~60항목** — 결함 2건 발견·수정(Start 구 문구 고지 모순, 결과지 학년 표기)
7. **학생 정보 교정 기능**: 상세 모달 "✏ 정보 수정"(오기입 대응, 학번 변경 시 문서 키 이동,
   감사 필드, 실측 모드 전용) + rules delete를 승인 교직원으로 확장

### 세션 종료 시점 상태

- 라이브 가동 중 (클라우드 모드): https://monatory.github.io/mjc-jobready-pwa/ — 오늘 작업 전부 반영
- ⚠ **firestore.rules 재게시 대기** — update 완화(재응시)·delete 확장(학번 교정)이 게시 전이라
  해당 두 기능은 라이브에서 아직 미동작 (콘솔 게시 즉시 활성)

### 다음 세션 이어갈 거리

- [ ] **firestore.rules 콘솔 재게시** (최우선 — 재응시 갱신·학번 교정의 선행 조건)
- [ ] 라이브에서 마스터 로그인 → 학생 정보 수정 저장 1회 실확인 (자동화 불가 경로)
- [ ] Firestore 테스트 데이터 정리 — 응답 20260001·20990001·20990002·**20999999(8/31 검증용)**,
  테스트 교직원 계정 2개, 기관 3건, 상담 기록
- [ ] 상담사 신청 계정 확인·승인 (#/counsel → 상담사 계정 관리. 안 보이면 해당 상담사가 로그인
  1회 시도 → 고아 복구로 승인 대기 재접수됨)
- [ ] 마스터 로컬 예비 해시 제거 여부 결정 (클라우드 로그인 안정 확인 후)
- [ ] 확정 체크리스트 §12 잔여 (00 명칭·01~09 문항/배점/활동·10~12 구현 반영 정리·13·15)
- [ ] 검사 설명서 v0.1 센터 검토 / PWA manifest·카톡 OG 카드 / 파일럿 운영 설계

---

## 2026-08-31 (2차) — 학년 세분화 · 명단 정렬 선택 · 새로고침 깜빡임 제거

사용자 요구 4건 반영 (스크린샷 지시 기반):

1. **학년 세분화 (STEP 2)**: 단일 1~3학년 → **본과 1~3 / 심화 1~2 / 졸업생(졸업 연도 4자리)**
   행 구성 선택 UI. 코드 값 `본과1`·`심화2`·`졸업2025` (`sessionState.GRADE_PATTERN`),
   표시 라벨은 `gradeLabel()`("본과 1학년"·"졸업(2025)") — 명단·상세 모달·CSV(01시트) 공통.
   구버전 값 "1"~"3"은 "N학년"으로 하위 호환.
2. **명단 정렬 선택 (StudentsPanel)**: JAS 고정 → 셀렉트 5종 — **최근 검사순(기본)** /
   JAS 높은 순 / 취업 희망시기 빠른 순(선택지 순서 기반, 하드코딩 없음) / 미연락 우선 /
   외부연계 단계순 (뒤 2종은 워크스페이스 전용). **실시일 컬럼을 학번 앞에 신설**(saved_at).
3. **사이드바 이름**: "잡카페 상담사 워크스페이스" → **"잡카페 워크스페이스"** (줄바꿈 깨짐 해소)
4. **F5 깜빡임 제거**: useStudents가 mock을 먼저 그리고 실측으로 교체하던 구조 →
   클라우드 설정 시 **"데이터 불러오는 중"(빈 목록) → 결과** 순서로 표시(`source: "LOADING"`).
   조회 완료본은 모듈 캐시(lastKnown)로 화면 전환 시 동기 재사용. KPI 0명 NaN 방어.

검증: validate 53 · 회귀 15 · build 통과. 브라우저 실동 — 학년 3행 UI·졸업 연도 입력 노출,
실시일 컬럼+최근 검사순 정렬, JAS 전환(100·100·100·95…), 관리자 배너·KPI NaN 없음, 콘솔 0.

### 추가 4: 배포 전 전체 기능 점검 (~60항목) + 발견 결함 수정 + 학생 정보 교정 기능

**전수 점검** (브라우저 실동, mock 40명 + 실제 데이터/규칙 REST 프로브):
게이트(validate 53·회귀 15·strict build) / 진입 가드 3종 / 동의→설문(빈 제출 차단·학년 3트랙·
졸업 연도 필수·전화 형식)→진단 27문항 자동진행→결과지(JAS 96 예시 재현·L4·추천 3·인쇄 머리글) /
제출 실패 배너+재시도 / 이어서 진행·다시 진단(초기화) / 역할 4종 접근 제어(담당자 워크스페이스
차단·계정 메뉴 마스터 전용·LEAD만 상담사 계정 메뉴·상담사 #/admin 리다이렉트) / 프리셋 AND·
Level·검색·상세 필터·초기화 / 정렬 5종 실측(RECENT·JAS 내림차순 확인) / 학생 모달(판정근거·
원응답·추천) / 상담 카드 메모 저장·재오픈 유지 / 연계기관 폼·표 / CSV 클릭 스모크 / 집계 기간·
실시일 필터 / 앱 콘솔 에러 0 (Vite HMR ws 에러는 개발 전용). 서버: 비인증 읽기 403(보호 정상),
라이브 200. ⚠ 규칙 재게시 아직 안 됨(크로스 uid 갱신 403 실측) · push 대기 커밋 존재.

**점검 중 발견·수정한 결함 2건**:
1. Start 하단 "서버로 전송되지 않습니다" 구 문구 — 클라우드 모드와 고지 모순 → CLOUD_ENABLED
   분기("결과 확인 시 학교 시스템에 안전하게 제출…")로 교체
2. 결과지 학생 표기 "졸업2025학년" — gradeLabel 미적용 → "졸업(2025)"로 수정

**학생 정보 교정 기능 (사용자 요구 — 학생 오기입 대응)**:
- 학생 상세 모달(관리자·워크스페이스 공통)에 "✏ 정보 수정" — 학번·성명·학과·학년(3트랙)·
  연락처 교정 폼. 응답·판정·검사 실시일(saved_at)은 불변. 검증(학번 4~20·성명 30자·전화 형식·
  졸업 연도) + 학번 변경 시 경고 문구.
- 저장은 실측(CLOUD) 모드 전용(미리보기에선 버튼 비활성). `updateStudentProfile()`:
  학번 변경 시 문서 키("{학기}_{학번}") 이동 — 새 키 복사 후 기존 삭제, 대상 학번 중복 시 거부.
  `profile_edited_by/at` 감사 필드 기록.
- **firestore.rules 변경**: ready_responses delete를 마스터 → **승인 교직원**으로 확장(키 이동용).
  ⚠ 콘솔 재게시 필요 (이전 update 완화와 함께 한 번에).
- 검증: 폼 프리필(구버전 학년 "기존값" 표시)·전화 검증·학번 경고·취소·비로그인 가드 실동 확인.
  실측 저장 경로는 라이브에서 마스터 로그인 후 1회 확인 필요(콘솔 인증이라 자동화 불가).

### 추가 2: 기간별 데이터 취합 (검사 실시일 기준)

사용자 요구 "2026.1.1~2026.12.6 식으로 기간을 설정해 학생 데이터를 취합" 반영:

- **관리자 집계 기간 바** (`Dashboard`): 종합 현황·데이터 다운로드 두 섹션 공통의 from~to
  날짜 설정 — KPI 5종·Level 분포·학과별 현황·Sheet 5종 CSV(운영/익명)가 전부 기간 기준으로
  전환. "적용 중 — N명 / 전체 M명" 표시 + "전체 기간으로" 리셋. 비우면 전체.
- **명단 상세 필터 "실시일 기간"** (`StudentsPanel`, 관리자·워크스페이스 공통): from~to 날짜
  입력 — 다른 필터와 AND 결합, 필터 결과 CSV에 자동 반영.
- 공용 판정은 `responsesSource.inPeriod()` 1곳 — 기간 설정 시 실시일 없는 레코드는 제외.
- 브라우저 검증: 8/13~16 설정 → KPI 40→17명 재계산, 명단 8/19~20 → 11명·해당 날짜만 표시.

### 추가: 표 셀 줄바꿈 정리

관리자·워크스페이스 표가 컨테이너 폭에 맞추느라 모든 셀을 쥐어짜 "테스트(삭제 요망)"·날짜·
희망시기가 어색하게 줄바꿈되던 문제 → `.admin-table td { white-space: nowrap }`(넘치면
`.table-wrap` 가로 스크롤) + 긴 서술 셀만 `.cell-wrap` 예외(추천활동 활동명·설명, 기관 비고,
min 220 / max 340px). 브라우저 확인: 명단 전 셀 한 줄, 추천활동 설명만 자연 줄바꿈.

---

## 2026-08-31 — 학생 응답 저장 신뢰성 전면 수정 (조용한 유실 4건 제거)

배경: 사용자 보고 "학생 페이지에서 입력·저장이 잘 안 되는 것 같다" + 상담사 계정 신청이
어디에도 안 보이는 문제. 점검 결과 두 증상 모두 **인증 세션 공유·조용한 실패**가 뿌리.

### 발견 (라이브 실측 포함)

1. **재응시 갱신 403 (실증)**: 새 익명 토큰으로 기존 응답(`2026-2_20990001`) REST 갱신 →
   PERMISSION_DENIED. Rules가 "최초 작성 익명 uid 일치"를 요구하는데 익명 uid는 기기·세션마다
   달라져 **다른 기기/브라우저/로그아웃 후 재응시는 전부 거부** → 클라이언트는 조용히 무시.
   (8/30 재응시 테스트는 같은 브라우저=같은 uid라 통과했던 것)
2. **인증 세션 공유 오염**: 학생 제출이 기본 Auth의 currentUser를 그대로 사용 — 마스터 로그인
   상태로 테스트하면 auth_uid가 마스터 uid로 저장, 관리자 로그아웃은 익명 세션까지 파괴.
   교직원 가입 신청(createUser)도 같은 인스턴스라 **신청 순간 마스터 Firebase 인증이 끊김**
   (상담사 신청이 계정 목록에 안 보이던 원인 — 클라우드 조회 실패 → 로컬 폴백).
3. 제출 실패 무표시·재시도 불가, 제출 성공 후 응답 수정해도 재제출 안 함(플래그 방식).

### 수정

- `firestore.rules` — ready_responses **update를 create와 동일 조건**(인증+validResponse)으로.
  auth_uid는 마지막 제출자 uid로 강제(추적 유지). 위·변조 방지는 §12-11에서. **⚠ 콘솔 재게시 필요**
- `src/lib/firebase.ts` — 보조 앱 인스턴스 분리: `"student"`(학생 익명 제출 전용 — 관리자
  로그아웃 무영향) / `"signup"`(가입 신청 전용 — 현재 세션 안 갈아탐) + `authReadyFor()`
- `src/lib/saveResponse.ts` — 학생 전용 세션으로만 제출, 결과를 `"OK"|"FAIL"|"OFF"`로 반환
- `src/student/Result.tsx` — 제출 상태 표시(제출 중…/완료/실패 배너+**다시 제출** 버튼).
  "제출 완료" 판단을 플래그 → **응답 스냅샷 비교**로: 수정 후 재방문 시 자동 재제출
- `src/admin/auth.ts` — 가입 신청을 signup 인스턴스로 격리 + **고아 계정 복구**(Auth만 있고
  staff 문서 없는 계정이 로그인하면 PENDING 문서 즉석 재생성 — "이미 등록된 이메일 ↔ 계정
  정보 없음" 데드엔드 제거, 기본 역할 COUNSELOR)
- `src/styles/global.css` — 실패 배너 스타일(인쇄 제외)

### 검증

validate 53 · 회귀 15 · build 통과. 브라우저 실동(dev 5174, 실제 Firestore):
① 기존 문서 재제출(구 규칙) → 실패 배너+재시도 버튼 정상 표시 ② 새 학번 제출 → "안전하게
제출되었습니다" ③ 응답 수정 후 재방문 → 자동 재제출(스냅샷에 수정값 반영 확인) · 콘솔 에러 0

### 후속 조치 (사용자)

- [ ] **firestore.rules 콘솔 재게시** (재응시 403 해소는 게시 후 적용)
- [ ] 테스트 응답 `2026-2_20999999`(테스트(삭제요망)) 삭제 — 기존 테스트 데이터 정리와 함께
- [ ] 상담사 신청 계정: 마스터 재로그인 → `#/counsel` → 상담사 계정 관리에서 확인·승인.
  안 보이면 해당 상담사가 한 번 로그인 시도(고아 복구로 승인 대기 재접수됨)

---

## 2026-08-30 (세션 종료) — 하루 총정리

로컬 프로토타입 → **실운영 가능한 클라우드 시스템**까지 하루에 완주한 대형 세션.

### 오늘 완성된 것 (시간순)

1. 관리자 계정·권한(마스터/담당자/상담사) + 프리셋 AND·운전면허 필터
2. 전체 점검 20회 + 수정 7건 (조건부 응답 오염·익명 R번호·인쇄 머리글 등)
3. **상담사 중심 재정렬**: 휴대전화 필수 수집 + 아웃리치(연락상태·큐) + "먼저 연락드립니다" 고지
4. **상담사/담당자 완전 분리**: 히든 워크스페이스 `#/counsel` + 4역할 (핵심: 담당자는 상담사
   페이지·연락 기록 접근 불가)
5. 마스터 = 개발자 본인 계정 고정 (전 목록 비노출·삭제 불가)
6. **통합 상담 카드**: 요약 스트립·회차 기록·외부연계 파이프라인·취업상태 + 연계기관 등록부
7. **Firebase 연동 가동**: 별도 프로젝트(mjc-ready-pwa) 생성·설정 완료 → 학생 응답 중앙 저장,
   상담 기록 상담사 간 공유, 권한 서버(Rules) 강제. 라이브 실전 테스트 10종 전부 통과
8. **검사 설명서 v0.1 (docx)**: 배점·가중치·Level 판정·연계 체계 보고용 매뉴얼 — 개조식 문체

### 세션 종료 시점 상태

- 라이브 가동 중 (클라우드 모드): https://monatory.github.io/mjc-jobready-pwa/
- 전부 push 완료 (5a4c50c) · validate 53 · 회귀 15 · build 통과 · 콘솔 에러 0

### 다음 세션 이어갈 거리

- [ ] Firestore 테스트 데이터 정리 (응답 3건·테스트 계정 2개·기관 3건·상담 기록) — 파일럿 전 콘솔에서
- [ ] 마스터 로컬 예비 해시 제거 여부 결정 (클라우드 로그인 안정 확인 후 — 보안상 제거 권장)
- [ ] 검사 설명서 v0.1 센터 검토 → 필요 시 v0.2 (문구·결재 서식 조정)
- [ ] 확정 체크리스트 §12 잔여 (00 명칭·01~09 문항/배점/활동·13 level1_fallback·15 연구 문항)
- [ ] PWA manifest·카톡 OG 카드 (본격 홍보 전)
- [ ] 파일럿 운영 설계 (대상 학과·기간·상담사 계정 실제 발급)

---

## 2026-08-30 (마감 5) — 라이브 실전 테스트 10종 통과 + 유실 버그 1건 수정

라이브(monatory.github.io + 실제 Firestore)에서 실전 시나리오 10종 전부 통과:

1. 배포 반영 ✅ 2. 학생A 모바일 완주·제출(승인 도메인 검증) ✅ 3. 학생B 진학·창업 Route+
조건부 문항 ✅ 4. 같은 학번 재응시 → 중복 없이 갱신(JAS 100→96, 계획서 예시값 재현) ✅
5. 마스터 로그인 + 실측 3건 명단 ✅ 6. 상담사 이메일 가입→승인 전 차단 ✅ 7. 마스터 승인(클라우드)
→ 상담사 워크스페이스 자동 진입 ✅ 8. 상담 카드(회차·연계)+기관 등록 → 캐시 삭제 후 클라우드
복원, **계정 간 공유 실증**(마스터가 만든 기관·기록을 상담사가 봄) ✅ 9. 담당자 가입·승인 →
실측은 보이되 연락 요소 0·워크스페이스 차단 ✅ 10. **보안 침투 시도**: 담당자의 실제 인증 토큰으로
ready_outreach REST 직접 읽기 → 403(서버 규칙이 차단), 응답 읽기는 200(정상 업무), 비인증 전부 403 ✅

버그 수정(테스트 8에서 발견): 선택 필드(연계일·메모) 비움 → undefined 포함 → Firestore setDoc
조용한 실패로 연계 기록 유실 → `initializeFirestore(ignoreUndefinedProperties)` 근본 수정(83a4c43).

테스트 데이터(파일럿 전 콘솔에서 정리 가능): 학생 응답 3건(20260001·20990001·20990002),
교직원 mjc.counselor.test@ / mjc.admin.test@, 기관 3건(서대문·서울서부·OO시스템), 학생A 상담 기록.

---

## 2026-08-30 (마감 4) — 🎉 Firebase 클라우드 모드 가동 (전 구간 실측 검증 통과)

사용자가 콘솔 설정 완료(프로젝트 mjc-ready-pwa 생성 → config 제공 → Auth 이메일/익명 +
마스터 계정 → Firestore(서울, Standard) → Rules 게시 → 승인 도메인). config 코드 반영 후
**엔드투엔드 실측 검증 전부 통과**:

1. 학생 흐름 완주 → 익명 인증으로 `ready_responses/2026-2_20260001` 제출 성공
   ("응답이 안전하게 제출되었습니다" 표시)
2. 마스터 Firebase Auth 실로그인 (IndexedDB에 이메일 세션 확인 — 로컬 폴백 아님)
3. 관리자 화면 "실측 데이터 — 응답 1건" 전환, 명단에 실측 학생(엔진 재판정 L4·JAS 100)
4. 워크스페이스 "☁ 공유 저장소 연결됨" + 연락 기록 저장 → **로컬 캐시 삭제 후 리로드 시
   클라우드에서 기록 복원** (상담사 간 공유 실증)
5. 비인증 REST 읽기 403 PERMISSION_DENIED — 실명 데이터 보호 규칙 실동 확인

버그 3건 즉석 수정: ① 로그인 후 학생 데이터 캐시 미갱신 → storeSession에서 무효화+리스너
재조회 ② 새로고침 시 Auth 복원 전에 조회돼 권한 거부 → `authReady()` 대기 후 조회
(fetch/pullShared/loadStaffCloud 공통) ③ 공유 동기화 완료 후 명단 배지 미반영 →
StudentsPanel key=cloudState 리마운트

---

## 2026-08-30 (마감 3) — Firebase 연동 구현 (별도 프로젝트, 설정 즉시 활성화)

결정: 기존 Firebase **계정**을 쓰되 **프로젝트는 분리**(`mjc-ready-pwa` 신규) — MJC-CAT Rules(익명
설계, 학번·이름 차단)와 실명 시스템이 상충하고, 공용 배포는 실서비스 리스크 (사용자 승인).
"별도 가입 대기"였던 보류 사유 해소 — 기존 계정에서 프로젝트만 추가하면 됨.

### 구현 (전부 폴백 구조 — config 비면 로컬 모드로 기존과 동일 동작)

- `src/lib/firebase.ts` — config 붙여넣는 곳(CLOUD_ENABLED 스위치) + `ready_*` 컬렉션 네임스페이스
- **학생 응답**: 결과지 도달 시 익명 인증으로 `ready_responses/{학기_학번}` 제출(재응시=본인 문서
  갱신), 성공 시 "응답이 안전하게 제출되었습니다" 표시
- **실측 데이터 소스**: 관리자·워크스페이스가 응답 있으면 실측 표시(원응답을 엔진으로 재판정 —
  결정론), 없으면 mock 미리보기 + 배너로 모드 구분 (`responsesSource.ts` useStudents)
- **상담 기록·등록부 공유**: localStorage를 캐시로 유지하며 Firestore write-through + 워크스페이스
  진입 시 최신 병합(`cloudStore.ts`) → 상담사 간 공유. 배너에 "☁ 공유 저장소 연결됨" 표시
- **교직원 인증 하이브리드**: 이메일 아이디+설정 완료 → Firebase Auth(가입→PENDING→승인) /
  그 외 → 기존 로컬 인증. 마스터는 콘솔 생성 계정(이메일 일치 판정), 비번 변경은 재설정 메일
- **Security Rules** (`firestore.rules`): 실명 응답 읽기=승인 교직원만, 상담 기록·등록부=상담사
  계열만(담당자 차단을 서버에서 강제!), staff 자가 승격 금지, 마스터=이메일 고정
- `docs/FIREBASE_SETUP.md` — 콘솔 설정 10분 가이드 (프로젝트 생성→config→Auth 2종+마스터 계정
  선점 생성→Rules 게시→승인 도메인)

### 검증

validate 53 · 회귀 15 · build 통과(번들 +Firebase SDK). 로컬 모드 스모크: 마스터 로컬 로그인·
명단 40명·큐 22(기존 기록 유지)·학생 전 흐름 결과지 도달(제출 플래그 없음=정확한 no-op)·콘솔 0

---

## 2026-08-30 (마감 2) — 통합 상담 카드: 회차·외부연계·취업관리 (상담사 요구 전면 반영)

사용자 요구: "학생을 클릭했을 때 상담희망·정부지원 연계희망·연락 진행·간단 메모·1/2회차·최종 요약이
한눈에. 외부 연계는 희망→연계 확인→별도 필터→사후관리. 취업상태 등록·취업처 관리도.
외부기관은 기관명·연락처·담당자·사업명을 등록해 관리."

### 통합 상담 카드 (`CounselRecord.tsx` — 워크스페이스 학생 모달)

① **요약 스트립**: 상담희망·정부지원 의향(설문) + 연락상태·외부연계(기관명)·취업상태·상담 횟수·
   최근 메모 — 모달 열자마자 한 줄 요약 ② 연락 기록(기존) ③ **상담 회차 기록**: 날짜+내용 입력
   (Enter 지원) → 1·2회차… 자동 번호, 상담사·삭제 지원 + **최종 요약** ④ **외부기관 연계**:
   단계 칩(희망→연계 완료→사후관리→종결) + 등록부 기관 선택(선택 시 담당자·연락처 즉시 표시) +
   연계일·메모 ⑤ **취업상태**: 구직중/취업(취업처명, 등록부 자동완성)/진학/창업 + 확정일·메모

### 연계기관·취업처 등록부 (`agencies.ts` + `AgencyManager.tsx` — 새 섹션)

- 구분(연계기관/취업처)·기관명·연락처·담당자·사업명·비고 — 등록·수정·삭제, 유형 필터
- 상담사 공유 자원: 연계 기록의 기관 드롭다운·취업처 자동완성 소스

### 명단·필터 확장 (워크스페이스만)

- 열 추가: 외부연계·취업 배지 / 큐 추가: **🔗 연계 사후관리**(연계 완료+사후관리 중) /
  상세 필터: 외부연계 단계·취업상태 / 헤더: "연락 대기 N · 연계 사후관리 N"
- CSV(01 시트): 상담회차·최종요약·외부연계단계·연계기관·취업상태·취업처 컬럼 — **상담사 추출에만
  포함**, 담당자·연구용에서는 컬럼 자체 제거(OUTREACH_KEYS 확장)

### 검증

validate 53 · 회귀 15 · build 통과. 실동: 기관 2곳 등록 → 학생 카드에서 회차 2건·최종 요약·
연계 완료(기관 정보 표시)·구직 중 저장 → 요약 스트립 실시간 갱신("연계 완료 (서대문…)·구직 중·
상담 2회") → 명단 배지 반영 → 사후관리 큐 1명·외부연계 필터 1명 → 담당자 화면·필터에 신규 요소
0개 재확인. 기존 연락 기록과 하위 호환(스키마 확장 merge). 콘솔 에러 0

---

## 2026-08-30 (마감) — 상담사/담당자 완전 분리: 히든 워크스페이스 구축

사용자 확정: "담당자(행정)를 다 믿을 수 없다 — **담당자는 상담사 페이지를 볼 수 없어야 한다**.
상담사들은 별도(히든) 페이지에서 공유하며 관리하고, 상담사 계정은 상담사 관리자가 등록·관리."

### 구조 재편 (4역할)

| 역할 | 진입 | 범위 |
|---|---|---|
| 마스터(개발자=사용자) | 양쪽 전환 | 전체 + 상담사 관리자 지정/해제 |
| 상담사 관리자 (신설) | `#/counsel` | 연락 관리 + 상담사 계정 승인·관리 |
| 상담사 | `#/counsel` | 연락 관리 (연락상태·메모 공유) |
| 담당자(행정, 구 일반 관리자) | `#/admin` | 종합 현황·명단·추천활동·다운로드만 |

- **`#/counsel` 상담사 워크스페이스 신설** (`CounselDesk.tsx`, 초록 테마) — 어디에도 링크 없는
  히든 라우트, 상담사 계열 로그인 시 자동 진입. 연락 우선 큐·연락 기록·상담사 계정 관리가 여기로 이동
- **담당자 차단 3중**: ① `#/admin`에서 연락상태/큐/편집기/필터 제거 ② `#/counsel` 접근 시
  `#/admin` 강제 리다이렉트 ③ CSV에서 연락상태·상담메모 컬럼 제거 (`exportSheet` 기본값)
- **계정 관리 분리**: 담당자 계정 관리(`#/admin`, 마스터) ↔ 상담사 계정 관리(`#/counsel`,
  상담사 관리자+마스터) — 서로의 계정 목록이 보이지 않음
- 리팩터: 명단 공용화 `StudentsPanel.tsx`(`showOutreach` 스위치), `PasswordModal.tsx` 분리
- **마스터 계정 = 개발자 본인으로 고정** (사용자 지시): 아이디·비밀번호를 코드에 SHA-256 해시로
  고정(원문 비저장·문서 비기재), 매 실행 시 강제 복원(변조·구버전 "master" 잔재 자동 제거),
  어떤 계정 목록에도 비노출, 삭제·비활성·화면 비밀번호 변경 전부 불가. 로그인 화면의 마스터 언급 제거.
  ⚠ 공개 저장소·배포 번들에 해시 노출 — 해당 비밀번호는 타 서비스와 절대 공유 금지

### 검증

validate 53 · 회귀 15 · build 통과. 실동 전 시나리오: 담당자 로그인(메뉴 4종·연락 요소 0·
`#/counsel` 강제 접근 → 리다이렉트), 상담사 로그인 → 워크스페이스 자동 진입(연락 관리만),
마스터가 상담사 관리자 승격 → 재로그인 시 상담사 계정 관리 메뉴 표시(승격 버튼은 마스터 전용),
담당자/상담사 계정 목록 상호 미노출. 콘솔 에러 0

⚠ 시범 한계: 클라이언트 권한 분리는 UI 수준 — 실명 수집 전 Firebase Auth + Security Rules로
서버 강제 필수(§7.2). 상담사 간 실시간 공유도 Firestore 연동 시 활성화.

---

## 2026-08-30 (최종) — 상담사 중심 재정렬: 아웃리치 체계 구축 + 심화 점검

사용자 의도 확정: **1차 사용자는 잡카페 상담사. 학생은 스스로 오지 않으므로 상담사가 먼저
연락하는 것을 돕는 것이 시스템의 존재 이유** → CLAUDE.md §1 운영 철학으로 명문화.

### 핵심 구축 — 아웃리치(연락 관리) 체계

1. **휴대전화 필수 수집** (기존엔 연락 수단이 아예 없었음 — 아웃리치 시스템의 결정적 공백):
   STEP 2 기본정보에 휴대전화 필수 입력(형식 검증) + STEP 1 수집항목 고지 갱신 + 기존 저장분 호환 처리
2. **연락 관리 모듈** (`src/admin/outreach.ts`): 학생별 연락상태 5단계(미연락/연락함/상담예약/
   상담완료/무응답) + 상담 메모 + 처리자·일시 기록. 시범 localStorage → Firestore 교체 구조
3. **연락 우선 큐** (📞 버튼): 상담희망 또는 L3+ 이면서 미연락·무응답(진학·창업 제외)만 즉시 추출
   — "오늘 연락할 학생" 목록. KPI "연락 대기" + 학과별 현황 연락대기 열 동반
4. **명단 강화**: 연락처 열(tel: 링크, 행 클릭과 분리)·연락상태 배지 열 + 상세 필터 "연락 상태"
5. **학생 상세 모달**: 연락처 표시 + 연락 기록 편집기(상태 칩+메모+저장, 마지막 기록 표시)
6. **CSV**: 01 시트에 연락처·연락상태·상담메모, 05 시트 상담연계 상태를 실제 기록으로.
   연구용 익명 추출에서는 성명·연락처·메모·연락상태 컬럼 자체 제거
7. **상담사 UX**: 상담사 로그인 시 명단으로 바로 진입
8. **학생 참여 유도**: 시작화면·결과지에 "따로 찾아가지 않아도 돼요 — 컨설턴트가 먼저 연락드립니다"
   고지 (참여 문턱 낮춤 — 참여 저조 대응)

### 기타

- **학생 헤더 취·창업팀으로 변경** (사용자 확정 — 관리자와 통일). 인쇄 머리글도 동일
- 프로젝트 의도를 Claude 메모리에 저장 (향후 세션 자동 인지)

### 검증

validate 53 · 회귀 15 · build 통과. 실동: 휴대전화 미입력 차단→입력 통과→저장 확인,
연락 우선 큐 23명=KPI 일치, 기록 저장 시 큐 23→22 실시간 반영, 연락상태 필터(상담예약=1명),
상담사 로그인 시 명단 직행, 콘솔 에러 0

---

## 2026-08-30 (후반) — 전체 점검 20회 (디자인~내용) + 수정 7건

학생·관리자 전 화면과 데이터·엔진·문서를 20개 관점으로 점검. 발견 즉시 수정, Read-Only 영역은 보고만.

### 수정 반영 (7건)

1. **Survey 조건부 응답 오염 방지** — 상위 응답 변경으로 visible_if가 깨진 문항(전환 시기·계기,
   진학·창업 구분)을 제출 시 저장에서 제외 (실동 검증: 방향전환→전공진출 변경 시 잔존값 제거 확인)
2. **결과지 인쇄 머리글** — 인쇄 시 화면 헤더가 숨겨져 기관명·발급일이 없던 것을 print 전용 머리글로 보완
3. **아이디 소문자 정규화** — 대소문자만 다른 유사 계정("Master" 등) 생성 차단
4. **학생 상세 모달 보강** — 설문 원응답에 항목명 prefix, 연구 문항 5종·자격증 상태(보유/준비/목표) 표시
5. **연구용 익명 R번호 고정** — 내보내기 목록 순서 기준이던 것을 전체 명단 기준으로 고정
   (필터 명단·시트별 추출 간 같은 학생 = 같은 R번호 → 시트 간 결합 가능)
6. **하드코딩 제거 3곳** — 시작 화면 문항 수(6·27), 결과지 게이지 초록 기준(70), getResumeState의 27
   → 전부 data/에서 주입 (§4 원칙)
7. Diagnostic 자동 진행 주석(500ms→실제 350ms) 정정

### 이상 없음 확인

- STEP 1~3·결과지 문구/디자인, 모바일(375px) 학생 전 화면 + 관리자 로그인, 인쇄 스타일,
  KPI 수치 자체 정합(Level 분포 합=40), 프리셋 AND·상세 필터 조합, 데이터 무결성(27문항·6영역·
  JR 신호 문항·id 중복 없음), level_rules ↔ CLAUDE.md 정합, 콘솔 에러 0, lang=ko·viewport
- clearAll()은 학생 키만 제거 — 관리자 세션과 충돌 없음 확인

### 보고 사항 (수정하지 않음 — 센터 판단 필요)

- 학생 헤더 "AI융합진로지원센터" vs 관리자 "취·창업팀" 표기 이원화 유지 여부 (기존 미결)
- result_templates.json note의 "{닉네임} 치환" 언급은 실제 미사용 (문구 확정 때 정리 권장)
- OG 메타태그·PWA manifest 미착수 (기존 잔여 작업)
- 접근성: Survey 라벨-입력 연결(htmlFor) 미적용 — 본격 배포 전 개선 권장

### 검증

validate 53 · 회귀 15 · build 통과. 브라우저 실동: 학생 전 흐름(동의→설문→27문항→L4 결과지),
관리자(마스터 로그인→명단→학생 상세 모달 신규 표시 확인). 콘솔 에러 0

---

## 2026-08-30 — 관리자 계정·권한 체계 + 필터 보완 (사용자 요구 2건)

### 1) 관리자 계정·권한 (CLAUDE.md §6.4 신설)

- 3역할 설계: **마스터 관리자**(내장 계정, 등록 불요 — 이후 마감 세션에서 개발자 본인 계정으로 교체·고정) /
  **일반 관리자·상담사**(로그인 화면에서 가입 신청 → 마스터 승인 후 로그인 가능)
- 권한 매트릭스: 마스터=전체+계정관리 / 일반 관리자=계정관리 제외 전체 / 상담사=종합 현황·대상자 필터·명단만
- 신규: `src/admin/auth.ts`(로컬 계정 저장 + SHA-256 해시 + 권한 매트릭스, Firebase Auth 교체 대비 분리) ·
  `Login.tsx`(로그인/가입 신청 탭) · `Accounts.tsx`(마스터 전용 승인·비활성·삭제) ·
  Dashboard 인증 게이트 + 사이드바 사용자 정보·로그아웃 + 비밀번호 변경 모달(전 역할)
- ⚠ 시범 한계: localStorage 로컬 계정(브라우저별 독립) — 실명 수집 전 Firebase Auth + Security Rules 교체 필수(§7.2)

### 2) 대상자 필터 보완 (취·창업팀 실무 요구)

- **프리셋 칩 복수 선택 AND 결합** (기존 1개만 선택 가능) + "취업희망" 프리셋 신설,
  "정부지원 연계 후보"→"정부지원 연계희망" 라벨 정리 → 취업희망+상담희망+정부지원 동시 추출 가능
- 상세 필터에 **진로방향·상담 희망** 추가 (총 10종)
- 자격 분류에 **운전면허(DRIVER)** 신설 — `survey_items.json` category_values(사용자 지시로 Read-Only 영역 변경),
  학생 화면·관리자 필터·CSV는 데이터 주입 구조라 자동 반영. mock 자격 풀에 1종·2종 보통 추가

### 검증

- validate 53 · 회귀 15 · build 통과. 브라우저 실동: 마스터 로그인 → 프리셋 3종 AND(40→9명) →
  운전면허 필터(8명) → 상담사 가입 신청 → 승인 전 로그인 차단 확인 → 마스터 승인 → 상담사 로그인 시
  메뉴 2종만 노출 확인. 콘솔 에러 0
- 루트 `.claude/launch.json` 추가 (dev 서버 미리보기용, `npm run dev --prefix`)

---

## 2026-08-28 (마감) — 샘플 데이터 보강 + 세션 마무리

- **샘플 보강**(f13e111): 자격증 보유율 40%→75%(0~3건), 자격 풀 13종 분류 균형(OA 4·전공 5·어학 2·기타 2),
  미정 학생도 직무분야 "아직 모르겠음" 생성. 상세 필터 단독 적용 실측(40명): 방향전환형 16 ·
  OA 17 · 어학 11 · 자격보유 20 · 잔류형(본가 그외+희망 서울) 3 — 전 필터 유의미 인원 확인.
- **세션 종료 시점 상태**: 라이브 `https://monatory.github.io/mjc-jobready-pwa/` 가동 중.
  ⚠ 마지막 커밋(f13e111 샘플 보강 + 이 문서 커밋)은 **푸시 대기** — 다음 push 시 자동 배포.
  push는 사용자 실행(Claude 측 git push는 권한 정책상 차단, 2026-08-28 확인).

### 다음 세션 이어갈 거리
- [ ] 밀린 커밋 push → 라이브 반영 확인
- [ ] 학생 화면 헤더 "학생지원처 AI융합진로지원센터" 표기 유지 여부 확인 (관리자는 취·창업팀으로 변경됨)
- [ ] 카톡 미리보기 OG 카드 (MJC-CAT처럼 1200×630 이미지) — 필요 시
- [ ] 확정 체크리스트 §12 (00~15) 순차 확정, 특히 11(인증 방식)·12(배포 보안) → Firestore 연동(STEP 3) 선행 조건
- [ ] Firebase: 운영센터 별도 가입 대기 중 (§7.3 — 생성 보류)

---

## 2026-08-28 (후반) — 취·창업팀 요구 반영: 상담사 대상자 추출 정밀화

배경: 취·창업팀의 핵심 운영 목표 = **취업의향·학교지원 수용성·전공/비전공·취업시기가 확인된
학생을 빠르게 추출해 잡카페 컨설턴트가 즉시 지원**(사용자 10년 경력 기반 요구). 이에 맞춰
설문의 분류 정밀도와 관리자 추출 도구를 강화.

### 설문(STEP 2) 변경
- **자격증 분류 신설**: 입력 행에 분류 선택(OA·사무/전공/어학/기타) 추가 — `category_values`
- **본가(거주지) 지역 신설**: 희망 취업 지역과 교차 → 수도권 잔류형/연고지 복귀형 판별
  (예: 본가 대전 + 희망 서울 = 잔류취업형). 기존 "희망지역" 라벨 → "희망 취업 지역"
- **정부지원 문항(gov_link)**: 문항에 미래내일 일경험 예시 병기 + 제도 설명 펼치기(`info.programs`)
  + "활용 희망 시 잡카페 취업컨설턴트가 참여방법 안내·연계" 고지(`info.notice`). 배점 무변경

### 관리자 변경
- **운영관리자 표기**: AI융합진로지원센터 → **학생지원처 취·창업팀** (사이드바)
- **상세 필터 8종** (대상자 필터·명단): 전공연계/본가지역/희망 취업 지역/희망직무 분야/
  희망시기/정부지원 의향/자격증 보유상태/자격 분류 — 프리셋·Level·학과·검색과 AND 결합
- **필터 결과 CSV 다운로드** (운영용 실명/연구용 익명) — 현재 필터 적용된 명단만 추출
- 명단 테이블 전공연계 열 추가, 학생 상세 모달에 본가→희망 지역·직무분야·자격 분류 표시
- 01_학생상태 시트 컬럼 확장(전공연계·희망직무분야·본가지역·정부지원의향), 03 시트 분류 채움

### 검증
- validate 53 · 회귀 15 · build 통과. 브라우저 실동: 필터 40→12(방향전환형)→1(+OA 자격) 정밀 추출,
  STEP 2 신규 요소 전부 렌더 확인

---

## 2026-08-28 — 운영 계정·동의·연구 활용 방안 확정 (§7.3) + 코드 반영

인프라·개인정보 Q&A 세션(선행 MJC-CAT 컨텍스트에서 시작)을 거쳐 3가지를 최종 확정하고 반영.

### 확정 사항 (CLAUDE.md §7.3 신설, 체크리스트 14번 ✅)

1. **계정 구조** — Firebase(신규 별도 프로젝트)·GitHub 소유는 센터 계정(`mjcaicareer@gmail.com`),
   개발 멤버로 `monatory82@gmail.com` 추가. 유료 기능 필요 시 센터 기존 Blaze 결제 계정 연결
   (프로젝트는 절대 합치지 않음 — 결제만 공유).
2. **동의 화면** — 필수 동의 1개(체크박스 1개) + 4요소 고지(항목·목적·보유기간·거부권)
   + 익명 통계의 교내 연구 활용 고지. 학생 화면은 설문 안내 수준으로 단순 유지.
   보유기간 "졸업 후 3년"은 잠정(센터 확정 필요).
3. **연구 활용 투트랙** — 운영(실명) / 연구(익명 추출). 교내 교수 연구 설문 제공은 익명 추출본만.

### 코드 반영

- `src/student/Start.tsx` — "응답 전 안내" 카드: 4요소 고지 `<dl class="consent-terms">` + 동의 문구 교체
- `src/styles/global.css` — `.consent-terms` 스타일 추가
- `src/admin/csvExport.ts` — `exportSheetForResearch()`: 학번→R001… 익명 일련번호, 성명 컬럼 제거
- `src/admin/Dashboard.tsx` — 다운로드 카드에 "운영용 CSV (실명) / 연구용 CSV (익명)" 2버튼 + 취급 안내

### 추가: 교내 연구(직무로드맵) 연계 문항 5종 통합 (같은 세션 후반)

교내 연구 「산업체 직무분석 기반 직무로드맵 구축 연구」(연구보고서 초안 v1.2, `B 연구과제/`)를
분석해, 연구에 필요한 학생 수요 데이터를 별도 설문 없이 MJC-READY STEP 2에 통합 수집하기로 결정.

- 연구 대조 결과: 기존 항목(전공연계 여부=3트랙, 자격증 상태=자격 로드맵, 학과·학년) 이미 정합.
  추가로 필요한 것은 ① 직무군 축(4대 범용직무군) ② 전환 시점·동기 ③ 준비 장애요인 ④ 로드맵 수요.
- `data/survey_items.json` — RESEARCH 태그 비점수 문항 5종 추가(전부 선택, Level 판정 무영향):
  `desired_job_group`(8지선다) / `career_shift_timing`·`career_shift_reason`(방향전환형 N 조건부) /
  `prep_difficulty` / `roadmap_demand`
- `src/student/Survey.tsx` — 비점수 렌더러 일반화(visible_if 지원), 섹션 C에 5문항 배치
- 관리자 CSV(02_설문원자료)·학생상세는 일반 로직이라 자동 대응. 연구 제공은 연구용 익명 추출만(§7.3)
- CLAUDE.md §3.3 표·연구 연계 주석 + 체크리스트 15번(문구 확정 대기) 등록

### 추가 2: 섹션 C 복수 선택 + GitHub Pages 개발기 배포 가동 (같은 세션 최종)

- **복수 선택**(f05c437): 희망지역·희망직무 분야·전환 계기·장애요인 4종 multi:true —
  콤마 결합 문자열 저장, `surveyAnswerLabel` " · " 연결. 전공연계·전환시기·로드맵의향은 단일 유지.
- **배포 가동**(6ad286a): 저장소 `monatory/mjc-jobready-pwa`(Public) 신설 + Actions 워크플로
  (validate 53 → test 15 → build → Pages). 라이브: **https://monatory.github.io/mjc-jobready-pwa/**
  카톡 공유 가능. 서버 전송 없는 프로토타입이라 공개 무해(§7.3·§8).
- 시행착오 기록: ① Claude 측 git push·GitHub API 호출은 권한 분류기가 차단 → 저장소 생성은
  사용자 웹, push는 사용자 실행(run 버튼)으로 해결 ② 첫 배포에서 configure-pages(enablement)가
  권한 부족으로 실패 → 사용자가 Settings→Pages에서 Source "GitHub Actions" 선택 후 Re-run으로 성공.

### 참고 (같은 세션의 MJC-CAT 측 확인)

- Spark 요금제 "1달 뒤 삭제" 오해 해소 — Firestore 실측: 5/28 생성 문서 3개월 경과 정상 보존(30건+)
- MJC-CAT는 현 구조 유지(취창업팀 예산 주체 분리, GitHub Pages 상시 운영 확인 HTTP 200)

---

## 2026-08-27 — 프로젝트 발족 → 첫 커밋 (52d73a6)

하루에 기획부터 실동 프로토타입까지. 선행 프로젝트 MJC-CAT(자유전공 학과추천 PWA)의
형식·검증 자산을 재사용하되, **별개의 신규 독립 시스템**으로 구축(기존 시스템 무변경).

### 1) 기획·공식 문서

| 산출물 | 내용 |
|---|---|
| `CLAUDE.md` | 개발 기획서 v0.2 = 살아있는 명세. Read-Only 영역, 배점표, Level 규칙, Firestore 모델, 확정 체크리스트 13건 |
| `docs/MJC_..._개발제안서_v0.2.docx` | 계획안 v0.1 검토 → 수정·보완 12건 제안(명칭·버전 2 / 인증·보안 3 / 데이터·기능 4 / 개발 방식 3). 센터 표준 서식 |
| `docs/MJC_..._개발계획서_v0.2.docx` | v0.1 + 제안 반영 통합 계획서 10개 섹션(결재·공유용). 센터 표준 서식 |
| `docs/source/..._v0.1.docx` | 사용자 최초 계획안 원본 보존 |

### 2) STEP 1 설계 확정 산출물 (데이터 소스 6종, `data/`)

- `survey_items.json` — 기본 설문 6항목 배점표(JAS 만점 100) + 비점수 항목 + 자격증 입력 구조
- `level_rules.json` — Level 1~4 규칙: JAS 컷오프 70, 취업/희망시기 Gate, L4 실전신호, Route Tag, 상담 연계
- `diagnostic_bank.json` — 보조 진단문항 **27개 초안**(6영역 × DNA 속성). 전문가 검토 대기
- `recommendation_master.json` — 추천활동 10종 시드 (활동명·기간 센터 확정 대기)
- `result_templates.json` — Level 1~4 + 진학·창업 Route 결과 문구 템플릿
- `excel_columns.json` — Excel 5개 Sheet 컬럼 정의 (자격증 Long Format)

### 3) 판정 엔진 + 품질 게이트

- `lib/level_engine.js` — JAS·JRS·CDS 산출 + Gate 평가 + Level 판정 + 판정근거. **결정론**(동일 입력=동일 출력)
- `lib/weak_area.js` · `lib/recommendation_resolver.js` — 보완영역 추출 · Master 조회(취약 매칭 우선 정렬)
- `scripts/validate-data.mjs` — 데이터 무결성 **53건** (배점 합계·참조 무결성·템플릿·Excel 정의)
- `tests/test_level_engine.js` — 회귀 **15건** (96점 예시→L3, 실전신호→L4, 경계값 68/73, timing Gate, 비취업 Route 승급 금지, 결정론)

### 4) 학생 화면 (Vite + React + TS, MJC-CAT 패밀리룩, dev 포트 5174)

```
STEP 1 소개·동의(이어서 진행 모달) → STEP 2 기본정보·설문 → STEP 3 진단 27문항(자동 진행)
→ 결과지(Level 카드·JAS 게이지·영역바·보완영역·추천활동·상담 CTA·인쇄)
```
브라우저 실동 검증: L4 시나리오 클릭 통과, 콘솔 에러 0, `npm run build`(strict tsc) 통과.

### 5) 관리자 대시보드 (`#/admin`, 미리보기 모드)

- 가상 학생 40명을 **실제 엔진으로 판정**(시드 고정)한 mock 데이터
- 4개 섹션: 종합 현황(KPI·Level 분포·학과별) / 대상자 필터·명단(프리셋 6종 + 학생 상세 모달) /
  추천활동 관리(ON/OFF) / 데이터 다운로드(Sheet 5종 CSV, UTF-8 BOM)
- Firestore 연동 시 데이터 소스만 교체하는 구조

### 6) 발견·결정 사항

- **level1_fallback 도달 불가**: 취업 선택 시 JAS 최소 33이라 "JAS<30 → L1" 미발동.
  취업 방향 학생의 최저 Level=2가 의도인지 확인 필요 → 체크리스트 13번 등록
- 추천활동 시드 활성기간을 9/1 → 8/1로 앞당김(오늘 기준 기간 필터에 전부 걸리는 문제)
- 시범 단계는 **서버 전송 없음**(sessionStorage만). Firestore·인증은 체크리스트 10·11 확정 후

### 다음 할 일

- [ ] 센터 검토: 설문·진단 문항 문구, Level 컷오프 체감, 추천활동 목록, Excel 컬럼 (→ 수정 목록화)
- [ ] 확정 체크리스트 §12 (00~13) 순차 확정
- [ ] STEP 3: Firebase 프로젝트 + Firestore 저장 + Security Rules (인증 방식 확정 후)
- [ ] PWA manifest + service worker (본격 배포 전)
- [ ] GitHub 원격 저장소 연결 + CI(validate:data → test → build) 게이트
