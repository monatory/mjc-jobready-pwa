# 명지전문대학 진로·취업 상태진단 및 맞춤형 취업지원 PWA — 개발 기획서 (살아있는 명세)

> 이 파일은 매 세션 자동으로 로딩되는 프로젝트 컨텍스트이자 **개발 기획서 v0.2**입니다.
> 원본 계획안: `docs/source/MJC_진로취업_상태진단_맞춤형지원_시스템_개발계획안_v0.1.docx` (2026-08-27, AI융합진로지원센터)
> 코드를 작성하거나 데이터를 다루기 전에 반드시 전체를 읽고 시작하세요.
> 형식·운영 관례는 선행 프로젝트 **MJC-CAT**(`D. 자유전공) 학과추천_진단PWA/mjc-career-pwa`)의 CLAUDE.md 체계를 따른다.

---

## 1. 프로젝트 개요

명지전문대학 재학생의 **진로·취업 준비상태를 진단하고 맞춤형 취업지원으로 연결**하는 모바일 우선 PWA.
학생이 짧은 설문 + 보조 진단문항 + 자격·경험 입력을 마치면, 사전 정의된 배점·Gate 규칙으로
**Level 1~4(진로탐색 → 진로설정 → 취업준비 → 실전취업)**를 판정하고, 취약영역에 따라
잡카페 진로/취업컨설턴트·부서 프로그램으로 자동 연계한다. 관리자(센터)는 대상자 필터·통계·Excel 원자료 추출로 실무를 운영한다.

- **주관**: 학생지원처 AI융합진로지원센터
- **대상**: 전체 재학생 (파일럿은 일부 학과/학년 → 전학과 확대)
- **시스템 성격**: 심리검사가 아닌 **취업지원 운영을 위한 분류모형**. "정확한 성향 진단"이 아니라
  **구직의사가 높고 취업시기가 임박한 학생을 Level 3 이상으로 조기 발굴·연결**하는 것이 1차 목적.
- **운영 철학 (2026-08-30 사용자 확정)**: **1차 사용자는 잡카페 상담사**(행정관리자보다 우선).
  요즘 학생들은 참여율이 낮고 스스로 상담사에게 먼저 연락하지 않는다 — 그래서 이 시스템의 존재 이유는
  **상담사가 대상자를 찾아 먼저 연락(아웃리치)하는 것을 쉽게 만드는 것**. 기능 설계·우선순위 판단은
  항상 "상담사가 이 자료로 학생에게 더 쉽게 접근할 수 있는가"를 기준으로 한다.
  (구현: 휴대전화 필수 수집 + 연락 관리(연락상태·메모) + 연락 우선 큐 — §6.1)
- **가칭**: MJC-READY (확정 필요 — §12-00). 진단 버전 코드는 `MJC-CDI-x.x`(문항 DNA), 규칙 세트는 `MJC-DNA vX.X`.
- **선행 프로젝트와의 관계**: **기존 MJC-CAT(자유전공 학과추천)과 별개의 신규 독립 시스템** — 저장소·데이터·Firebase·배포 전부 분리.
  기존 시스템은 일절 변경하지 않으며, 패밀리룩(공통 헤더·STEP 인디케이터·모바일 UI 톤)과 검증된 코드 패턴만 **새 저장소로 복사**해 재사용한다.
  또한 학과추천(MJC-CAT)과 달리 **학번·성명을 수집하는 실명 시스템**이며 학기별 누적 관리가 핵심(§7 주의).

---

## 2. 절대 변경 금지(Read-Only) 영역

다음 항목은 **개발자(=Claude Code) 임의로 수정 금지**. 사용자(센터) 명시 지시가 있어야만 변경 가능.
v0.1 수치는 "가설"이지만, 보정은 **운영 거버넌스(전문가 검토·파일럿 분석) 영역**이지 개발 영역이 아니다.

| 영역 | 위치(예정) | 사유 |
|---|---|---|
| 기본 설문 배점표 (SCORE/GATE/FILTER/ACTION) | `data/survey_items.json` | §3.3 배점안. 전문가 검토·파일럿 전까지 v0.1 유지 |
| Level 1~4 판정 규칙 · JAS 컷오프(70점 기준안) | `data/level_rules.json` | 계획안 Level 정의 그대로 |
| 보조 진단문항 24~30개 + DNA 속성 | `data/diagnostic_bank.json` | 문항 확정 후 변별 분석 전까지 보정 금지 |
| 판정 엔진 산출 로직 | `lib/level_engine.js` | 결정론 원칙(§3.1-②) — 동일 응답 = 동일 Level 재현 |
| 진단 버전 체계 (`MJC-CDI-x.x`) | 코드/스키마 전반 | 학기·연도 간 데이터 비교 가능성 확보 |

수정이 필요해 보이면 **반드시 사용자에게 먼저 보고하고 승인 받기**.

---

## 3. 핵심 설계 (계획안 v0.1 확정 방향)

### 3.1 설계 원칙 6가지

| 원칙 | 설계 내용 |
|---|---|
| ① 구직활성도 중심 | 일반 진단문항보다 실제 취업의사·취업시기·지원수용성을 Level 판정에 더 크게 반영 |
| ② 결정론적 판정 | AI API 없이 배점·Gate 규칙만으로 Level 1~4가 동일하게 재현 |
| ③ 원자료 보존 | 분석점수와 별개로 학생이 선택한 설문값·자격증 상태를 모두 그대로 저장 |
| ④ 분류와 지원 연결 | Level 판정에서 끝내지 않고 취약영역 → 추천활동 코드 → 상담·프로그램까지 연결 |
| ⑤ 관리자 조정 가능 | 추천활동·상담유형·노출기간·우선순위는 관리자 화면에서 직접 등록·수정 |
| ⑥ 버전관리 | 문항·배점·판정기준은 버전으로 고정, 학기·연도 간 비교 가능 |

### 3.2 3계층 데이터 역할

| 데이터 | 역할 |
|---|---|
| **기본 설문** (구직활성도) | 대상자 발굴의 핵심 자료 — Level 판정 주 입력 |
| **보조 진단문항** (24~30개) | "왜 현재 단계에 머물러 있는지" 설명 + 추천활동 세분화 (판정의 절대 주체 아님) |
| **자격·경험 데이터** | 실전취업 가능성 판단 + 취업자산 관리자료 (점수 환산 없이 원자료 보존) |

### 3.3 기본 설문 배점표 (v0.1 가설 — Read-Only §2)

| 설문항목 | 선택값 | 배점(안) | 역할 |
|---|---|---|---|
| 진로방향 | 취업 / 진학·창업 / 미정 | 30 / 5 / 0 | SCORE + GATE |
| 취업준비 의지 | 바로 시작 / 천천히 / 미정 | 20 / 10 / 0 | SCORE |
| 학교지원 여부 | 적극적으로 / 필요할 때 / 혼자 | 15 / 8 / 0 | SCORE + ACTION |
| 취업 희망시기 | 졸업 전·3개월 / 6개월 / 1년 / 기타 | 20 / 16 / 8 / 3 | SCORE + GATE |
| 정부지원 연계의향 | 활용 / 잘 모름 / 관심 없음 | 10 / 5 / 0 | SCORE + FILTER |
| 상담 희망 | 희망 / 미희망 | 5 / 0 | SCORE + ACTION |

- **JAS(구직활성도) = 위 6개 항목 합산, 0~100점.**
- 예시: 취업(30)+바로 시작(20)+적극(15)+6개월 이내(16)+정부지원 활용(10)+상담 희망(5) = **96점**
  → 자격증·입사지원서가 부족해도 구직의사가 매우 높은 학생으로 Level 3 이상 대상.

**점수화하지 않는 항목** (FILTER/분류/원자료 전용):

| 항목 | 활용 방식 |
|---|---|
| 본가(거주지) 지역 | 서울/경기/인천/그 외 — **희망 취업 지역과 교차해 수도권 잔류형/연고지 복귀형 판별** (2026-08-28 취·창업팀 요구, 상담 매칭 핵심) |
| 희망 취업 지역 **(복수 선택)** | 서울/경기/인천/기타 — 채용처·프로그램 매칭 FILTER (구 "희망지역"에서 라벨 변경) |
| 전공연계 여부 | 전공진출형(Y) / 방향전환형(N) 지원경로 분류 (양자택일 — 단일) |
| 희망직무 분야 (RESEARCH, **복수 선택**) | 4대 범용직무군(사무행정/회계·세무/총무·안전관리/서비스)+전공/IT·디자인/미정/기타 — 직무로드맵 연구 직무군 축 정합 |
| 희망직무 | 직무별 대상자 검색, 채용공고·상담 매칭 (자유입력 상세) |
| 전환 시기·계기 (RESEARCH) | 방향전환형(N)일 때만 노출 2문항 — 시기는 단일, 계기는 **복수 선택** ("늦은 전환의 비용" 정량 검증) |
| 준비 장애요인 (RESEARCH, **복수 선택**) | 정보/자격/경험/시간/비용/없음 — FGI 주제(정보 공백·경험 증빙) 정량화 |
| 로드맵 수요 (RESEARCH) | 단계별 준비 경로 제공 시 활용 의향 — 로드맵 배포·프로그램 우선순위 근거 |
| 정보열람 동의 | 접근 권한 판단용. **Level 점수 반영 금지** (Consent 별도 관리) |
| 자격증 상세 | 보유/준비 중/목표 상태 + **분류(OA·사무/전공/어학/운전면허/기타)** 원자료 보존 (점수 환산 금지). 분류는 상담사 활용 핵심 — `certification_entry.category_values`. 운전면허 분류는 2026-08-30 사용자 지시로 추가(현장 채용조건에서 중요) |

> **정부지원 문항 부가 안내 (2026-08-28)**: `gov_link` 문항에 `info` 필드 — 국민취업지원제도·미래내일
> 일경험 간단 설명(펼치기) + "활용 희망 시 잡카페 취업컨설턴트가 참여 방법 안내·연계" 고지.
> 문항 텍스트에 두 제도 예시 병기. 배점(10/5/0)은 무변경.

> **연구 연계 문항 (2026-08-28 추가, RESEARCH 태그 5종)**: 교내 연구 「산업체 직무분석 기반 직무로드맵
> 구축 연구」(연구보고서 초안 v1.2, `B 연구과제/`)의 학생 수요 데이터를 이 시스템의 설문에 통합 수집한다.
> KEDI 자료가 학과 단위 집계라 불가능했던 **개인 수준 분석**(연구의 한계 1)을 보완하는 데이터원.
> 전부 선택 응답·비점수(FILTER/RESEARCH)로 Level 판정에 영향 없음. 연구 제공은 §7.3 투트랙(익명 추출)만.
> 문항 문구는 초안 — 연구책임자·센터 확정 대기(§12-15).
> **복수 선택(multi:true)**: 희망지역·희망직무 분야·전환 계기·준비 장애요인 4종. 값은 콤마 결합
> 문자열(예: `"SEOUL,GYEONGGI"`)로 저장하며, 라벨 변환(`surveyAnswerLabel`)이 " · " 연결로 표시.

### 3.4 3대 지표

| 지표 | 의미 | 역할 |
|---|---|---|
| **JAS** 구직활성도 | 취업의사·긴급도·학교지원 수용성·정부지원 의향 | Level 판정의 핵심 (가장 높은 비중) |
| **JRS** 취업준비도 | 희망직무, 자격·경험, 입사지원서, 채용탐색 등 | Level 3 ↔ Level 4 구분 |
| **CDS** 진로발달도 | 자기이해·직무정보·역량준비·실행행동 (진단문항) | 취약영역·추천행동 정교화 |

### 3.5 Level 1~4 정의 (지원 단계 — 총점 순위가 아님)

| Level | 단계 | 주요 판정 조건(안) | 기본 연계 |
|---|---|---|---|
| Level 1 | 진로탐색 | 진로방향 미정 또는 진로목표·자기이해 낮음 + 구직활성도 낮음 | 잡카페 진로컨설턴트 |
| Level 2 | 진로설정 | 진로방향은 있으나 희망직무·산업·전공연계 구체화 필요 | 잡카페 진로컨설턴트 |
| Level 3 | 취업준비 | 진로방향=취업 Gate 충족 + **JAS ≥ 70(기준안)** + 졸업 전~6개월 이내 취업의사 | 잡카페 취업컨설턴트 |
| Level 4 | 실전취업 | Level 3 조건 + 목표직무 구체화 + 최근 채용탐색/입사지원서/직무역량 실전 준비신호 | 잡카페 취업컨설턴트 |

- **Level 3 = "취업준비 완성"이 아니라 "취업의사가 높아 적극 지원이 필요한 학생"**.
- **비취업 Route**: 진학·창업 선택 학생은 Level 3~4로 자동 승급하지 않고
  Level 1~2 + 별도 **진학/창업 Route Tag**로 관리 (기본안).

### 3.6 문항 DNA 속성 (보조 진단문항)

| 속성 | 예시 | 활용 |
|---|---|---|
| `questionId` | Q08 | 문항 고유번호 |
| `domain` | 진로방향 | 영역별 점수 산출 |
| `subDomain` | 목표직무 구체성 | 세부 취약영역 분석 |
| `weight` | 1.5 | 문항 중요도 반영 |
| `gate` | G2 | 단계 진입 필수조건 여부 |
| `critical` | true | 특정 단계 핵심문항 여부 |
| `filterCode` | ACTIVE_JOB_SEEKER | 대상자 분류 |
| `actionCode` | JOB_TARGET_SETTING | 추천활동 Master 연결 |
| `version` | MJC-CDI-1.0 | 연도·학기 비교 및 변경이력 관리 |

### 3.7 추천활동 — Recommendation Master 방식

생성형 AI가 임의로 만들지 않는다. **부서가 사전 등록한 Master에서 Level·취약영역·학생조건을 조회해 반환**
→ 실제 운영하지 않는 프로그램이 노출되는 문제 원천 차단.

| 관리 필드 | 내용 예시 |
|---|---|
| `recommendationCode` | JOB_TARGET_SETTING |
| 적용 Level | Level 2, Level 3 |
| 적용 취약영역 | 직무정보 / 목표직무 구체성 |
| 활동명 | 잡카페 직무탐색 상담 |
| 담당 | 진로컨설턴트 또는 취업컨설턴트 |
| 우선순위 | 1~5 |
| 학생 노출 설명 | "희망직무를 1~2개로 구체화하고 준비계획을 세워보세요." |
| 활성기간 | 2026-09-01 ~ 2026-12-31 |
| 활성여부 | ON/OFF |

### 3.8 학생 결과 화면 구성

판정 알고리즘은 노출하지 않고 **현재 단계 · 근거 · 지금 할 행동**만 짧고 명확하게.
결과 문구는 **고정 템플릿만으로 운영 가능** — AI API는 선택적 설명 보조로만(식별정보 외부 전송 금지 §7).

| 결과요소 | 학생 노출 내용 |
|---|---|
| 현재 Level | Level 1~4 및 단계명 |
| 핵심 해석 | 왜 이 단계인지 2~3문장 (템플릿) |
| 구직활성도 | 0~100점 + 간단한 시각 표시 |
| 보완영역 | 가장 낮거나 미충족한 1~2개 영역 |
| 추천 행동 | 관리자 승인 활동 중 우선순위 2~3개 |
| 상담 연결 | L1·L2 → 진로컨설턴트 / L3·L4 → 취업컨설턴트 |

> 예시 — LEVEL 3 취업준비 단계
> "취업 의지가 높은 상태입니다. 졸업 후 6개월 이내 취업을 희망하고 있으며 학교의 취업지원 활용에도 적극적입니다."
> 구직활성도 96/100 | 보완영역: 희망직무 구체화, 자격·직무역량 준비
> 추천 행동 ① 잡카페 취업컨설턴트 상담 ② 희망직무 채용공고 5건 분석 ③ 기본 입사지원서 준비

---

## 4. 데이터 파일 설계 (data/ — 소스 vs 파생 분리, MJC-CAT §23 거버넌스 승계)

| 파일 | 성격 | 내용 | 상태 |
|---|---|---|---|
| `survey_items.json` | 소스 | 기본 설문 6항목 배점 + 비점수 항목(FILTER/CONSENT) + 자격증 입력 구조 — §3.3 | ✅ v0.1 초안 (2026-08-27) |
| `diagnostic_bank.json` | 소스 | 보조 진단문항 **27개** + DNA 속성(6영역) — §3.6. 문항 텍스트는 전문가 검토 대기 | ✅ v0.1 초안 |
| `level_rules.json` | 소스 | Level 1~4 판정 조건, JAS 컷오프 70, Gate 2종, L4 신호, Route Tag, 상담 연계 | ✅ v0.1 초안 |
| `recommendation_master.json` | 소스(시드) | 추천활동 10종 초기 목록 — 운영 중엔 Firestore가 진실, JSON은 시드 | ✅ v0.1 초안 (활동명·기간 센터 확정 대기) |
| `result_templates.json` | 소스 | Level 1~4 + 진학·창업 Route 템플릿 문구 + 법적 안내 | ✅ v0.1 초안 (문구 확정 대기) |
| `excel_columns.json` | 소스 | Excel 5개 Sheet 컬럼 정의 — §6.3 | ✅ v0.1 초안 |
| `certification_catalog.json` | 소스 | 자격증 분류·명칭 사전 (자동완성·통계용) | ⏳ 예정 (체크리스트 06 확정 후) |
| (파생) `survey_matrix.csv` 등 | 파생 | 검토용 평탄화 — `scripts/build-data.mjs`로 자동 생성 | ⏳ 예정 |

- **코드에 배점·문항·규칙 하드코딩 금지.** 모든 규칙은 data/에서 import.
- `scripts/validate-data.mjs`: 배점 합계=100, actionCode ↔ recommendationCode 참조 무결성,
  Level 규칙 참조 필드 존재 여부 등 CI 게이트 (MJC-CAT 단계 1·2 체계 재사용).

## 5. 핵심 구현물 설계 (lib/ — 순수 함수 + .d.ts 동반)

| 모듈 | 역할 | 상태 |
|---|---|---|
| `lib/level_engine.js` | `calcJas` / `calcDomainScores` / `calcIndices` / `evaluate` — JAS·JRS·CDS 산출 + Gate 평가 + Level 판정 + 판정근거(reasons). **결정론** | ✅ 구현 (2026-08-27) |
| `lib/recommendation_resolver.js` | `resolveRecommendations` — Master 필터(레벨·활성·기간) → 취약영역 매칭 우선 → priority → 코드순 정렬(결정론) 상위 3 | ✅ 구현 |
| `lib/weak_area.js` | `findWeakAreas` — 기준선 3.5 미만 영역 낮은 순 최대 2개 | ✅ 구현 |
| `lib/excel_export.js` (또는 src/admin) | 5개 Sheet 원자료 추출 — §6.3 | ⏳ STEP 5 |
| `scripts/validate-data.mjs` | 데이터 무결성 검증 **53건** (배점 합계 100·Gate/신호/action_code 참조·템플릿·Excel 정의). CI 게이트 예정 | ✅ 구현 |
| `tests/test_level_engine.js` | 회귀 **15건** — JAS 96 예시, L3/L4 분기, 경계값 68/73, timing Gate, 비취업 Route, 결정론, 보완영역·추천 연계 | ✅ 전부 통과 |

```bash
npm run validate:data   # 데이터 검증 53건
npm test                # 엔진 회귀 15건
```

**엔진 구현 중 발견 — 센터 확정 필요(§12-13 추가)**: `level_rules.json`의 level1_fallback(취업 방향인데 JAS<30 + 자기이해 낮음 → L1)은
현행 배점표에서 "진로방향=취업"만으로 이미 30점이므로 **실제로는 발동 불가**(취업 선택 학생의 JAS 최소값 = 33).
v0.1 배점 구조상 취업 방향 학생의 최저 Level은 2가 되며, 이것이 의도인지(방향이 있으면 최소 진로설정 단계) 파일럿 전 확인 필요.

## 6. 관리자 기능 설계

### 6.1 화면 영역

| 영역 | 주요 기능 |
|---|---|
| Dashboard | 전체 응답자, Level 분포, 구직활성도, 상담희망, **연락 대기**, 취업시기, 학과별 현황(연락대기 열 포함) |
| **연락·상담 관리 (아웃리치)** — `#/counsel` 전용 | **이 시스템의 존재 이유 반영 (2026-08-30)** — 학생이 먼저 오지 않으므로 상담사가 먼저 연락한다. 학생 클릭 시 **통합 상담 카드**(`CounselRecord.tsx`): ① 요약 스트립(상담희망·정부지원 의향·연락상태·외부연계·취업상태·상담 횟수·최근 메모 한눈에) ② 연락 기록(상태 5단계+메모) ③ **상담 회차 기록**(1·2회차… 누적, 날짜·내용·상담사, 삭제 가능)+최종 요약 ④ **외부기관 연계 파이프라인**(연계 희망→연계 완료→사후관리→종결, 등록부 기관 선택·연계일·메모) ⑤ **취업상태 등록**(구직중/취업+취업처/진학/창업 등). 명단: 연락처(tel:)·연락상태·외부연계·취업 배지 열 + **📞 연락 우선 큐** + **🔗 연계 사후관리 큐** + 상세 필터(연락 상태·외부연계 단계·취업상태). 저장은 `outreach.ts` 단일 스토어(merge, 처리자·일시 자동). **상담사 워크스페이스에서만 노출 — 담당자(행정) 화면·CSV에는 없음(§6.4)** |
| **연계기관·취업처 관리** — `#/counsel` 전용 | 외부기관 등록부(`agencies.ts`+`AgencyManager.tsx`) — 구분(연계기관/취업처)·**기관명·연락처·담당자·사업명**·비고 등록·수정·삭제. 연계 기록의 기관 선택과 취업처 자동완성의 소스. 상담사 공유 자원 (2026-08-30) |
| 대상자 필터 | Level, JAS, 학과, 학년 + **상세 필터**(2026-08-28 구현, 08-30 확장): 진로방향·상담희망·전공연계·본가지역·희망 취업 지역·희망직무 분야·희망시기·정부지원 의향·자격증 보유상태·자격 분류(OA/전공/어학/운전면허/기타). 프리셋 칩은 **복수 선택 AND 결합**(예: 취업희망+상담희망+정부지원). **필터 결과 CSV 다운로드**(운영용 실명/연구용 익명) |
| 계정·권한 (2026-08-30) | 로그인 게이트 + **4역할 완전 분리**: 마스터(개발자) / 담당자(행정) / 상담사 관리자 / 상담사. 담당자는 `#/admin`(일반 관리+다운로드)만, 상담사 계열은 히든 워크스페이스 `#/counsel`만 — **담당자는 상담사 페이지·연락 기록에 접근 불가**. §6.4 참조 |
| 학생 상세 | 설문 원응답, 자격증, 영역점수, Level 판정근거, 추천활동, 상담연계 상태 |
| 추천활동 관리 | 단계/영역별 활동 등록·수정·비활성화, 담당자, 노출기간, 우선순위 |
| 진단버전 관리 | 문항·배점·Gate 기준 버전 열람 + 새 버전 생성 |
| Excel 다운로드 | 현재 필터 결과 또는 전체 데이터 시트별 원자료 추출 |

### 6.2 빠른 필터 프리셋

프리셋 칩은 **복수 선택 시 AND 결합** (2026-08-30 — 예: 취업희망+상담희망+정부지원 연계희망 동시 적용).

| 프리셋 | 조건 |
|---|---|
| 취업지원 우선대상 | 진로=취업 AND JAS≥70 AND 희망시기≤6개월 |
| 취업희망 (08-30 추가) | 진로방향=취업 |
| 상담희망 | 상담희망=Y AND 정보열람동의=Y |
| 정부지원 연계희망 | 정부지원 활용의향=Y |
| Level 3 | 높은 구직의사 + 준비보완 필요 |
| Level 4 | 실제 지원 가능한 실전취업 대상 |
| 자격 미보유/준비중 | 자격증 상태별 + 학과·직무 결합 (상세 필터로 제공) |

### 6.4 계정·권한 체계 — 상담사/담당자 완전 분리 (2026-08-30 사용자 확정, 시범: 로컬 인증)

**핵심 원칙: "담당자(행정)는 상담사 페이지를 볼 수 없어야 한다."** 상담사 워크스페이스(`#/counsel`)는
어떤 화면에도 링크하지 않는 **히든 페이지** — 상담사 계열 로그인 시에만 자동 진입.

| 역할 | 등록·승인 | 진입 화면 | 접근 범위 |
|---|---|---|---|
| 마스터 (개발자=사용자) | 내장 계정 — 아이디·비밀번호는 코드에 해시로 고정(auth.ts, 문서 비기재). **어떤 계정 목록에도 비노출·삭제 불가·화면에서 비밀번호 변경 불가** (2026-08-30) | `#/admin` (양쪽 전환 버튼) | 전체 + 담당자·상담사 계정 관리 + 상담사 관리자 지정/해제 |
| **상담사 관리자** | 상담사로 가입 → 마스터가 워크스페이스에서 승격 | `#/counsel` | 연락 관리 + **상담사 계정 등록·관리**(승인·비활성·삭제) |
| 상담사 | 가입 신청 → 상담사 관리자(또는 마스터) 승인 | `#/counsel` | 연락 관리(큐·연락상태·상담 메모 공유) |
| 담당자(행정) | 가입 신청 → 마스터 승인 | `#/admin` | 종합 현황·명단(연락 기록 **비노출**)·추천활동·데이터 다운로드. `#/counsel` 접근 시 강제 리다이렉트 |

**연락 기록(연락상태·상담 메모) 노출 범위** — 상담사 워크스페이스 화면·CSV에만 존재:
관리자 화면 명단/모달/상세필터에서 제거, 담당자용 CSV(`exportSheet` 기본)와 연구용 익명 CSV에서 컬럼 자체 제거.

- 구현: `auth.ts`(4역할 + `SECTION_ROLES` + `isCounselSide`/`homeRoute`/`toggleCounselorLead`) ·
  `CounselDesk.tsx`(워크스페이스, 초록 테마) · `StudentsPanel.tsx`(명단 공용, `showOutreach` 스위치) ·
  `Accounts.tsx`(역할군별 계정 패널) · `Dashboard.tsx`(행정 전용) · `PasswordModal.tsx`(공용)
- **시범 한계**: localStorage 로컬 계정 + SHA-256 해시(브라우저별 독립). 클라이언트 권한 분리는 UI 수준 —
  **실명 수집 전 Firebase Auth + Security Rules로 서버 강제 필수**(§7.2). 연락 기록 공유도 Firestore 연동 시 실시간화.

### 6.3 Excel 다운로드 (부가 기능이 아닌 **실무관리 핵심 기능**)

| Sheet | 주요 컬럼 |
|---|---|
| 01_학생상태 | 학번, 성명, **연락처**, 학과, 학년, 학기, 진로방향, JAS, JRS, CDS, Level, 희망시기, 희망직무, 상담희망, **연락상태, 상담메모** |
| 02_설문원자료 | 모든 설문항목 원 응답값, 응답일시, 수정일시, 설문버전 |
| 03_자격증현황 | 학번, 자격증명, 분류, 상태(보유/준비/목표), 취득/목표일, 비고 |
| 04_진단점수 | 문항별 원점수, 영역점수, Gate 통과여부, Critical 미충족, 진단버전 |
| 05_추천활동 | 학생별 추천코드, 활동명, 담당, 우선순위, 활성여부, 상담연계 상태 |

- **자격증 = Long Format** (자격증 1건 = 1행, 별도 Sheet). 쉼표 합치기 금지 — 학과별 보유율·특정 자격 검색·통계분석 대비.
- UTF-8 BOM(Excel 한글 호환)은 MJC-CAT `csvExport.ts` 방식 승계. 다중 Sheet 필요 시 xlsx 라이브러리 검토(§12-09와 함께 확정).

## 7. Firestore 데이터 모델 + 개인정보·보안

### 7.1 Collection 설계 (8종)

| Collection | 핵심 저장정보 |
|---|---|
| `students` | 학생 기본정보(학번·성명), 학과, 학년, 권한·식별정보 |
| `surveyResponses` | 학기별 기본 설문 원응답, JAS 산출값, 수정이력 |
| `certifications` | 학생별 자격증 단위 레코드 (보유/준비/목표) |
| `diagnosticTests` | 문항, DNA 속성, 배점·Gate 규칙, 버전 |
| `diagnosticResults` | 문항응답, 영역점수, JRS/CDS, Level, 판정근거 |
| `recommendationMaster` | 추천코드, 적용단계·영역, 담당, 설명, 기간, 우선순위 |
| `studentRecommendations` | 학생 결과시점에 적용된 추천활동 **스냅샷** |
| `adminSettings` | 학기, 기준점, 활성버전, 관리자 설정 |

- **문서키 규칙**: 학기별 반복응답은 `"2026-2_학번"` 형태 키로 해당 학기 최신값 업데이트,
  변경이력은 `history`로 별도 보존. 무한 문서 생성 금지.

### 7.2 개인정보·보안 원칙 (MJC-CAT §7과 다른 점 주의!)

**이 시스템은 학번·성명을 수집하는 실명 시스템**이다 (MJC-CAT 시범운영의 익명 체계와 다름).

- 정보열람 동의는 Level 점수에 반영하지 않고 **별도 Consent 값**으로 관리
- 학생 화면은 **본인 결과만** 조회. 관리자 화면은 인증된 교직원 권한별 접근범위 제한
- Firebase Security Rules로 학생/관리자 읽기·쓰기 분리 (본 운영 시 학내 SSO 연동 검토)
- Excel 다운로드는 **관리자 권한 전용** + 다운로드 로그/감사이력 관리 검토
- AI API를 향후 연결해도 **학번·성명 등 식별정보를 외부 모델로 전송하지 않는 구조** 원칙
- 외부 분석 도구(GA, Mixpanel 등) 연결 금지 (MJC-CAT 관례 승계)

### 7.3 운영 계정·동의·연구 활용 — 최종 확정 (2026-08-28)

**① 계정 구조 — "만드는 손은 개인, 명의는 센터"**

| 자산 | 소유자 | 개발 멤버 |
|---|---|---|
| Firebase 프로젝트 (신규 별도 생성 예정) | 운영센터 신규 Firebase 계정 (별도 가입 예정, 2026-08-28 결정) | `monatory82@gmail.com` (편집자 추가) |
| GitHub 저장소 (원격 연결 시) | 센터 계정 | 〃 |

> ~~**Firebase 프로젝트 생성 보류 (2026-08-28)**~~ → **변경 (2026-08-30 사용자 결정)**: 별도 가입을
> 기다리지 않고 **기존 Firebase 계정(MJC-CAT 관리 계정)에 새 프로젝트 `mjc-ready-pwa`를 생성**해 쓴다.
> 처음엔 기존 mjc-career-pwa 프로젝트 공용을 검토했으나, MJC-CAT Rules(학번·이름 차단 익명 설계)와
> 실명 시스템이 상충하고 Rules 배포가 실서비스를 건드릴 위험이 있어 **프로젝트 분리**로 확정
> (사용자: "기존 룰과 상충되면 별도로 해도 좋다"). 연동 코드·Rules·가이드는 구현 완료 —
> `src/lib/firebase.ts`에 config만 붙여넣으면 클라우드 모드로 전환되고, 그 전에는 로컬 모드로 동작.
> **설정 절차: `docs/FIREBASE_SETUP.md` (10분).**

- 실명(학번·성명) 시스템이므로 개인 계정 소유 금지 — 인수인계·감사 대비, 처음부터 센터 명의.
- Firebase 프로젝트는 **AI진로포트폴리오 등 다른 시스템과 절대 합치지 않는다** (데이터·규칙 분리).
  유료 기능(Cloud Functions 등) 필요 시점에 **센터의 기존 Blaze 결제 계정을 이 프로젝트에 연결**
  (결제 계정 1개 ↔ 프로젝트 N개 구조, 청구서에 프로젝트별 분리 표기 — 예산 주체가 센터로 동일하므로 문제 없음).

**② 동의 화면 — 필수 동의 1개(체크박스 1개) + 4요소 고지** (`src/student/Start.tsx` 반영 완료)

- 4요소: 수집 항목(학번·성명·학과학년·**휴대전화**·응답·자격증) / 이용 목적(진단·상담 연계·취업지원 개선) /
  보유 기간(**졸업 후 3년 — 잠정, 센터 확정 필요**) / 거부권(불이익 없음, 단 서비스 이용 불가)
- 연구 활용은 **고지 문구**로 처리: "개인을 알아볼 수 없게 처리한 통계는 교내 연구·정책 자료로 활용될 수 있습니다"
  — 익명·통계 자료는 개인정보가 아니므로 별도 동의 불요. 학생 화면은 설문 안내 수준으로 단순 유지.
- 별도 "연구 동의" 체크박스는 **개인 식별 형태의 외부 발표 계획이 생길 때만** 추가 (그 경우 IRB 심의 동반).

**③ 연구 활용 — 투트랙 (운영=실명 / 연구=익명)**

```
센터 운영 트랙 (실명)              연구 트랙 (익명)
학번·성명 포함 원본                학번→익명 일련번호(R001…), 성명 컬럼 제거
→ 상담 연계·성과관리               → 교내 교수 연구·논문·통계
→ 필수 동의로 커버                 → 고지 문구로 커버
```

- 관리자 다운로드에 **"연구용 CSV (익명)"** 버튼 구현 완료 (`csvExport.ts` `exportSheetForResearch`).
  교내 연구(교수 연구 설문 포함) 제공은 반드시 이 추출본만 사용. 일반 설문 연구(척도 분석·집단 비교·회귀)는
  익명 데이터로 전부 가능 — 개인 단위 실명 제공이 필요해지면 ②의 선택 동의 경로로 전환.
- 운영용(실명) Excel/CSV는 **개인정보 파일** — 접근 제한 폴더에만 보관, 공유 시 주의, 다운로드 로그 검토(§7.2).
- 본격 수집 전 교내 개인정보보호 담당 부서에 수집 항목·목적·보유기간 1장 정리 확인 권장.

**④ 순서 원칙 (재확인)**: 실명 수집(Firestore 연동) 시작 전에 인증(Auth)·Security Rules 강화 선행 — §12-11·12.

## 8. 기술 스택 (MJC-CAT 패밀리룩 승계)

| 항목 | 선택 | 비고 |
|---|---|---|
| 프론트 | Vite + React + TypeScript, HashRouter | MJC-CAT 동일 |
| PWA | `vite-plugin-pwa` (autoUpdate + skipWaiting) | 〃 |
| 백엔드 | Firebase Firestore (asia-northeast3) | 〃. 단, **실명 데이터이므로 Auth 필수** — 시범부터 Anonymous Auth 이상 |
| 판정 | 전부 클라이언트 순수 함수 (결정론) | AI API 의존 없음 |
| 차트 | Recharts | 〃 |
| 배포 | **개발기 GitHub Pages 가동 중 (2026-08-28)** → 본 운영 학내 도메인 | 저장소 `github.com/monatory/mjc-jobready-pwa` · 라이브 `https://monatory.github.io/mjc-jobready-pwa/` · main push 시 Actions 자동 배포(검증 53→테스트 15→빌드). 서버 전송 없는 프로토타입이라 공개 무해. ⚠ 실명 수집 시작 전 Security Rules·Auth 강화 선행 |
| PDF/인쇄 | `window.print()` + `@media print` | jsPDF 금지 (한글 폰트 회피, MJC-CAT §21) |
| 코드 컨벤션 | camelCase, 한국어 주석, JSON snake_case, 코드 상수 SNAKE_CASE | 〃 |
| CI | `npm run build`(tsc -b 포함) + validate-data + 회귀 테스트 통과 후에만 push | MJC-CAT §17.2 사고 회고 승계 |

## 9. 학생 화면 흐름 (안)

✅ **구현 완료 (2026-08-27)** — Vite + React + TS, HashRouter, dev 포트 **5174**(MJC-CAT 5173과 분리)

```
STEP 1 / 3  소개·동의        (#/)           MJC-READY 히어로 + 4통계 + 개인정보 4요소 고지·동의 체크(§7.3, 점수화 제외)
                                            진행 중 검사 있으면 "이어서 진행 / 처음부터" 모달
STEP 2 / 3  기본 정보·설문   (#/survey)     A 기본정보(학번·성명·학과·학년·휴대전화, 필수 — 휴대전화는 상담사
                                            아웃리치 채널, 2026-08-30 추가) + B 설문 6문항(배점표)
                                            + C 추가정보(희망지역·전공연계·희망직무·진학창업 구분(조건부)·자격증 행 추가)
STEP 3 / 3  진로준비 진단    (#/diagnostic) 27문항 × 5점 척도, 화면당 1문항 + 자동 진행 + 진행률 바 + 이전/다음
결과지                        (#/result)    Level 카드(Route 라벨) + JAS 반원 게이지(70+ 초록) + JRS/CDS
                                            + 6영역 바(취약 주황) + 보완영역 + 추천활동 2~3(담당 배지)
                                            + 상담 CTA(L1·2 진로 / L3·4 취업) + 법적 문구 + window.print
```

- 진입 가드: 동의 없이 /survey 진입 → STEP 1로, 설문 없이 /diagnostic 진입 → STEP 2로, 데이터 없이 /result → 빈 안내 카드
- sessionStorage 키: `mjc_ready_consent / profile / survey / unscored / certs / diag` — `clearAll()` 일괄 정리, `getResumeState()` 복귀 판정
- 시범 프로토타입 단계: **서버 전송 없음**(브라우저에만 저장) — Firestore 연동은 STEP 3(인증 방식 확정) 후
- 실행: `npm run dev` (5174) · 검증: `npm run build`(tsc 포함) + `npm run validate:data` + `npm test`

## 10. 타당성 확보 절차 (통계적 + 업무적)

| 단계 | 주요 내용 |
|---|---|
| 1. 전문가 검토 | 설문 배점, 문항 DNA, Level 정의·추천활동 연결 적절성 |
| 2. 파일럿 운영 | 일부 학과/학년 실제 입력 + Level 분포 확인 |
| 3. 문항·규칙 분석 | 문항 반응, 영역 신뢰도, Level 쏠림 여부 |
| 4. 업무성과 검증 | L3·L4 학생의 상담 참여·입사지원·취업성과 ↔ 분류결과 관련성 |
| 5. 기준 보정 | JAS 컷오프, Level 4 조건, 가중치·추천규칙 조정 |
| 6. 버전 확정 | **MJC-DNA v1.0** 고정, 이후 변경은 신규 버전 |

## 11. 개발 로드맵 (STEP 1~7)

| 단계 | 주요 산출물 | 핵심 확인사항 | 상태 |
|---|---|---|---|
| STEP 1. 설계 확정 | 설문 DNA 설계표, Level 규칙표, Excel 컬럼 정의 | 무엇을 점수화/필터/원자료로 둘지 확정 (§12 체크리스트) | ✅ **초안 완료 (2026-08-27)** — data/ 6종 + 엔진 3종 + 검증 53건 + 회귀 15건 전부 통과. 센터 확정(§12) 대기 |
| STEP 2. 화면 설계 | 학생 입력·결과, 관리자 Dashboard 와이어프레임 | MJC-CAT 패밀리룩 + 모바일 사용성 | ✅ **학생 화면은 실동 프로토타입으로 대체 (2026-08-27)** — 관리자 화면만 잔여 |
| STEP 3. Firebase 설계 | Collection/문서키, Security Rules | 학생별·학기별 중복 및 버전관리 | — |
| STEP 4. 핵심 개발 | 설문저장, 판정엔진, 결과화면, 추천활동 Master | **AI 없이 Level 결과 재현 가능 여부** | 🔶 **학생 흐름 완성 (2026-08-27)** — STEP1 소개·동의(이어서 진행 모달) → STEP2 기본정보·설문 → STEP3 진단 27문항(자동 진행) → 결과지(Level·JAS 게이지·영역바·보완영역·추천활동·상담 CTA·인쇄). 브라우저 실동 검증 완료(L4 시나리오·콘솔 에러 0). 잔여: Firestore 저장·관리자 |
| STEP 5. 관리자 기능 | 필터, 통계, 추천활동 관리, Excel 다운로드 | 센터 실무자가 개발자 없이 운영 가능한지 | 🔶 **미리보기(mock) 모드 완성 (2026-08-27)** — `#/admin` 4개 섹션: ① 종합 현황(KPI 5·Level 분포·학과별 현황) ② 대상자 필터·명단(프리셋 6종 §6-2·Level/학과/검색·JAS 내림차순·행 클릭 → 학생 상세 모달: 지표·판정근거·영역점수·원응답·추천활동) ③ 추천활동 관리(Master 표+ON/OFF 로컬 전환) ④ 데이터 다운로드(Sheet 5종 CSV, UTF-8 BOM, `excel_columns.json` 주입). 데이터: `src/admin/mockStudents.ts` 가상 40명을 **실제 엔진으로 판정**(시드 고정 결정론). Firestore 연동 시 데이터 소스만 교체 |
| STEP 6. Pilot/보정 | 파일럿 데이터 분석·컷오프 조정 | Level 분포·상담성과·취업성과 검증 | — |
| STEP 7. 정식 운영 | v1.0 고정, 전학과 확대 | 정기 분석·환류체계 연결 | — |

## 12. 개발 착수 전 확정 필요 체크리스트 (계획안 v0.1 + 기획 추가)

| # | 항목 | 상태 |
|---|---|---|
| 00 | (기획 추가) 시스템 공식 명칭 — 가칭 MJC-READY. 버전 코드 표기 통일(`MJC-CDI` 문항 vs `MJC-DNA` 규칙) | ☐ |
| 01 | 기본 설문 문항 및 선택지 최종 확정 | ☐ |
| 02 | 각 선택값의 SCORE / GATE / FILTER / ACTION DNA 확정 | ☐ |
| 03 | JAS 배점 및 Level 3 기준점(예: 70점) 확정 | ☐ |
| 04 | Level 4 실전취업 필수·선택 조건 확정 | ☐ |
| 05 | 24~30개 보조 진단문항과 영역 정의 | ☐ |
| 06 | 자격증 입력 필드 및 상태값(보유/준비/목표) 확정 | ☐ |
| 07 | 학생 결과페이지 노출 문구·추천행동 형식 확정 | ☐ |
| 08 | Recommendation Master 초기 활동 목록 확정 | ☐ |
| 09 | 관리자 필터 및 Excel Sheet/컬럼 최종 정의 (CSV vs xlsx 다중시트 포함) | ☐ |
| 10 | Firebase 문서키·버전·권한 구조 확정 | ☐ |
| 11 | (기획 추가) 학생 인증 방식 — 학번 자가입력 vs 학내 SSO vs 인증코드 (실명 데이터의 위·변조 방지) | ☐ |
| 12 | (기획 추가) 시범 배포 위치 — 실명 수집 전 Security Rules 수준과 배포 도메인 결정 | ☐ |
| 13 | (구현 중 발견) level1_fallback 도달 불가 이슈 — 취업 선택 시 JAS 최소 33이라 "JAS<30 → L1" 미발동. 취업 방향 학생의 최저 Level=2가 의도인지 확인 (§5 참조) | ☐ |
| 14 | 동의 화면 문구(4요소+연구 고지)·연구 활용 방식(투트랙)·운영 계정 구조(센터 명의) — §7.3 | ✅ 확정 (2026-08-28). 보유기간 "졸업 후 3년"만 잠정 |
| 15 | 연구 연계 문항 5종(희망직무 분야·전환 시기·전환 계기·준비 장애요인·로드맵 수요) 문구 확정 — §3.3 표 하단 참조. 초안 반영 완료, 연구책임자 검토 대기 | ☐ |

## 13. 기대효과

| 대상 | 기대효과 |
|---|---|
| 학생 | 짧고 직관적인 입력으로 현재 단계와 바로 해야 할 행동 확인 |
| AI융합진로지원센터 | 높은 구직의사 학생 조기 발굴 → 진로·취업컨설턴트 우선 연결 |
| 학과/교수 | 학과별 Level 분포·진로방향·자격·희망직무 기반 맞춤형 학생지도 |
| 대학 | 지원 투입 → 상담 → 프로그램 → 입사지원 → 취업 전주기 데이터 축적 |
| 성과관리 | 학기·연도별 대상자·성과의 객관적 데이터 분석·환류 |

## 14. 이 문서 유지보수

이 CLAUDE.md는 **프로젝트의 살아있는 명세**다. 다음 변경 시 반드시 갱신:
설문 항목·배점·Level 규칙 변경(버전 갱신 동반), 데이터 파일 추가, Firestore 모델/Security Rules 변경,
확정 체크리스트(§12) 항목 확정, 배포 URL 변경. 문서가 코드보다 뒤처지면 다음 세션의 컨텍스트가 깨진다.

### 폴더 구조 (현재)

```
E. 진로취업) 상태진단_맞춤형지원PWA/
└── mjc-jobready-pwa/            ← 개발 저장소 (예정)
    ├── CLAUDE.md                ← 이 문서 (기획서 v0.2 = 살아있는 명세)
    └── docs/
        ├── source/              ← 원본 계획안 v0.1 (docx) 보존
        ├── MJC_진로취업_상태진단_시스템_개발제안서_v0.2.docx   ← 수정·보완 12건 제안 (센터 서식)
        └── MJC_진로취업_상태진단_시스템_개발계획서_v0.2.docx   ← 통합 개발 계획서 (v0.1 + 제안 반영, 센터 서식)
```

### 공식 문서 이력

| 문서 | 내용 | 상태 |
|---|---|---|
| 계획안 v0.1 (docx) | 사용자 최초 작성 — 배점·Level·Master·Excel·Firestore 골격 | 보존 (`docs/source/`) |
| 개발 제안서 v0.2 (docx) | v0.1 검토 → 수정·보완 12건 + MJC-CAT 자산 재사용 방식 제안 | 작성 완료 (2026-08-27) |
| 개발 계획서 v0.2 (docx) | v0.1 + 제안 12건 반영 통합본 — 10개 섹션 (결재·공유용) | 작성 완료 (2026-08-27) |
| CLAUDE.md (이 문서) | 살아있는 개발 명세 — 코드·데이터 작업의 진실 기준 | 상시 갱신 |
